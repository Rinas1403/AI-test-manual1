# Test Cases — `AUTH` · API · Part 01 — Đăng ký · Đăng nhập — 25 TC · độ hạt GỘP

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_AUTH_SUMMARY.md](../../TEST_CASES_AUTH_SUMMARY.md) — Coverage · 4 vòng · ISO 25010 · Automation · Nhật ký |
| **Nguồn requirement** | [requirements_auth_api.md](../../../../../requirements/_book-api/auth/api/requirements_auth_api.md) · REQ dùng chung ở index [REQUIREMENTS_AUTH_SUMMARY.md](../../../../../requirements/_book-api/auth/REQUIREMENTS_AUTH_SUMMARY.md) mục 3 · bản đồ API [api_map.md](../../../../../requirements/_book-api/_discovery/api_map.md) |
| **Base URL** | `https://book.anhtester.com` — production, server duy nhất · spec `openapi: 3.0.0` · `info.version 1.0.0` · snapshot [`openapi_2026-09-19.json`](../../../../../requirements/_book-api/_discovery/sources/openapi_2026-09-19.json) (`sha256 32bb8b39…3f09` — khớp spec online 20-09-2026) |
| **Auth** | `BearerAuth` — HTTP bearer JWT qua header `Authorization`. `register` · `login` · `refetch-token` **công khai** (`security: []`) |
| **Dải TC ID** | `BK_AUTH_TC_059` → `BK_AUTH_TC_081` · `BK_AUTH_TC_106` · `BK_AUTH_TC_107` — dải chung toàn module `AUTH` (Mobile `001` → `058`). `106` · `107` bổ sung 20-09-2026 khi tách `TC_069` — **nối tiếp** dải, không chèn giữa |
| **Part liền kề** | Tiếp theo: [part_02_api_phien_ho_so.md](part_02_api_phien_ho_so.md) (`TC_082` → `TC_105` · `TC_108`) |

> Đọc **Dữ liệu dùng chung (API)**, **Dependencies & Execution Order** và **Dọn dữ liệu** ở [index](../../TEST_CASES_AUTH_SUMMARY.md) trước khi chạy. Tài khoản `userA` · `userB`… là tài khoản **do chính lượt chạy tạo** — 🚫 không dùng tài khoản có sẵn nào, không đăng nhập / sửa / xoá `user@example.com` (`RISK-BK-AUTH-03`).
>
> **Quy ước:** `Auto Type = API` nên TC được ghi mã HTTP, header, JSON nguyên văn ở phần chính. `@KnownBug` = TC viết theo kỳ vọng **đúng** đã chốt, hệ thống hiện đang sai → **dự kiến FAIL**, không được sửa TC để né bug. `⚪ Chờ chốt <AMB>` = kỳ vọng theo spec, AMB chưa được PO trả lời.

---

## Endpoint Catalog (part này)

| # | Method | Path | Auth | Mô tả | Số Test Cases |
|---|---|---|---|---|---|
| 1 | POST | `/api/register` | 🌐 Công khai | Đăng ký tài khoản — body `name` · `email` · `password` (bắt buộc) · `avatarUrl` · `phone` · `address` | 10 (`059` → `068`) |
| 2 | POST | `/api/login` | 🌐 Công khai | Đăng nhập — body `email` · `password`; trả `accessToken` + 2 cookie | 15 (`069` → `081` · `106` · `107`) |

---

## 1. `POST /api/register`

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_059 | REQ-BK-AUTH-01, REQ-BK-AUTH-02 | AUTH | High | Đăng ký thành công với body JSON đủ trường, không cần token | Email ở Test Data chưa từng đăng ký | 1. Gửi `POST /api/register`, header `Content-Type: application/json`, **không** có header `Authorization`, body theo Test Data<br>2. Đọc status<br>3. Đọc body<br>4. Đọc header response | `{"name":"Auto Auth API 1790150420","email":"auto_auth_1790150420_reg@auto.test","password":"Auto@12345","phone":"0912345678","address":"12 Tràng Tiền"}` | 1. Request được xử lý, **không** bị `401`<br>2. `201`<br>3. Object có khoá `msg` kiểu string, **chứa** `Register successfully`<br>4. **Không** có `Set-Cookie` (đăng ký không mở phiên) | Critical | Yes | API | @API, @Smoke, @CriticalPath |
| BK_AUTH_TC_060 | REQ-BK-AUTH-10 | AUTH | Medium | Đăng ký nhận đủ 3 content-type khai trong spec | Email mỗi biến thể chưa từng đăng ký | 1. Gửi `POST /api/register` với body hợp lệ, mã hoá theo content-type của biến thể<br>2. Đọc status và `msg` | **Bảng biến thể** — content-type · email:<br>`a` `application/json` · `auto_auth_1790150421_json@auto.test`<br>`b` `application/x-www-form-urlencoded` · `auto_auth_1790150422_form@auto.test`<br>`c` `multipart/form-data` · `auto_auth_1790150423_multi@auto.test`<br>Mọi biến thể: `name` = `Auto Auth API CT`, `password` = `Auto@12345` | **Mọi biến thể cho cùng kết quả:**<br>2. `201` · `msg` chứa `Register successfully`<br><br>⚠️ `c` chưa gọi thật (REQ-10 ghi `❔`) | Medium | Yes | API | @API, @Regression, @NeedsVerify |
| BK_AUTH_TC_061 | REQ-BK-AUTH-04 | AUTH | High | Thiếu hoặc sai kiểu `name` bị từ chối | — | 1. Gửi `POST /api/register` JSON theo biến thể<br>2. Đọc status<br>3. Đọc `fields` của body | **Bảng biến thể:**<br>`a` Không có khoá `name` → `{"email":"auto_auth_1790150424@auto.test","password":"Auto@12345"}`<br>`b` `name` sai kiểu → `{"name":123,"email":"auto_auth_1790150424@auto.test","password":"Auto@12345"}` | **Mọi biến thể cho cùng kết quả:**<br>2. `422`<br>3. `msg` = `Invalid data.` · `fields.name` là mảng có ≥ 1 chuỗi (quan sát `a`: `Expected property 'name' to be string but found: undefined`) · **không** tạo tài khoản (gửi lại với `name` hợp lệ → `201`)<br><br>⚠️ `b` chưa gọi thật | High | Yes | API | @API, @Regression, @NeedsVerify |
| BK_AUTH_TC_062 | REQ-BK-AUTH-05 | AUTH | High | Thiếu `email` bị từ chối với lỗi thiếu field — ⚪ Chờ chốt `AMB-BK-AUTH-03` | Tài khoản `user@example.com` **có thể** đang tồn tại — TC **không** đăng nhập / sửa / xoá nó | 1. Gửi `POST /api/register` JSON **không** có khoá `email`<br>2. Đọc status<br>3. Đọc body | `{"name":"Auto Auth API 1790150425","password":"Auto@12345"}` | 2. `422`<br>3. `fields.email` báo **thiếu field** (thông báo kiểu `Expected property 'email' …`) · **không** tạo tài khoản nào<br><br>⚪ **Dự kiến FAIL** theo hiện trạng (F-19): server dùng `default` `user@example.com` → `422` `{"msg":"Email already exists.",…}`. Ghi nguyên văn body khi FAIL; kết luận lỗi hay thiết kế chờ `AMB-BK-AUTH-03` | High | Yes | API | @API, @Regression, @PendingAMB |
| BK_AUTH_TC_063 | REQ-BK-AUTH-06 | AUTH | High | Thiếu `password` bị từ chối | — | 1. Gửi `POST /api/register` JSON **không** có khoá `password`<br>2. Đọc status<br>3. Đọc body | `{"name":"Auto Auth API 1790150426","email":"auto_auth_1790150426@auto.test"}` | 2. `422`<br>3. `msg` = `Invalid data.` · `fields.password` là mảng chứa `Expected property 'password' to be string but found: undefined` · không tạo tài khoản | High | Yes | API | @API, @Regression |
| BK_AUTH_TC_064 | REQ-BK-AUTH-07 | AUTH | High | `email` sai định dạng bị từ chối | — | 1. Gửi `POST /api/register` JSON, `email` theo biến thể, `name` = `Auto Auth API 1790150427`, `password` = `Auto@12345`<br>2. Đọc status<br>3. Đọc `fields.email` | **Bảng biến thể** — `email`:<br>`a` `khong-phai-email`<br>`b` `auto_auth@`<br>`c` `@auto.test`<br>`d` `auto@auth@auto.test`<br>`e` `auto auth@auto.test`<br>`f` `' OR 1=1--` | **Mọi biến thể cho cùng kết quả:**<br>2. `422`<br>3. `fields.email` **chứa** `Property 'email' should be email` · không tạo tài khoản<br><br>⚠️ `b`→`f` chưa gọi thật | High | Yes | API | @API, @Regression, @Security, @NeedsVerify |
| BK_AUTH_TC_065 | REQ-BK-AUTH-08 | AUTH | High | Đăng ký trùng email bị từ chối | Setup: `userA` đã đăng ký (`auto_auth_1790150430_a@auto.test`) | 1. Gửi `POST /api/register` JSON với **đúng** email của `userA`, `name` khác<br>2. Đọc status<br>3. Đọc body | `{"name":"Auto Auth API Dup","email":"auto_auth_1790150430_a@auto.test","password":"Khac@12345"}` | 2. `422` (hệ thống dùng `422`, **không** `409` — REQ-08)<br>3. Body đúng nguyên văn `{"msg":"Email already exists.","fields":{"email":["Email already exists."]}}` · mật khẩu của `userA` **không** đổi (đăng nhập lại bằng `Auto@12345` → `200`) | High | Yes | API | @API, @Regression |
| BK_AUTH_TC_066 | REQ-BK-AUTH-09 | AUTH | High | Kiểm trùng email không phân biệt hoa thường | Setup: `userA` đã đăng ký bằng email **chữ thường** | 1. Gửi `POST /api/register` với email của `userA` theo biến thể<br>2. Đọc status và body | **Bảng biến thể** — `email`:<br>`a` Viết hoa toàn bộ → `AUTO_AUTH_1790150430_A@AUTO.TEST`<br>`b` Hoa thường lẫn → `Auto_Auth_1790150430_A@Auto.Test` | **Mọi biến thể cho cùng kết quả:**<br>2. `422` · `msg` = `Email already exists.` · không có tài khoản thứ hai<br><br>⚠️ `b` chưa gọi thật | High | Yes | API | @API, @Regression, @NeedsVerify |
| BK_AUTH_TC_067 | REQ-BK-AUTH-01, REQ-BK-AUTH-35 | AUTH | High | Mass Assignment — trường đặc quyền gửi kèm khi đăng ký không có tác dụng | Email ở Test Data chưa từng đăng ký | 1. Gửi `POST /api/register` JSON có thêm các trường ngoài spec (Test Data)<br>2. Đọc status<br>3. `POST /api/login` bằng email + mật khẩu vừa đăng ký<br>4. `GET /api/me` với Bearer vừa nhận<br>5. Đọc body | `{"name":"Auto Auth API Mass","email":"auto_auth_1790150428_mass@auto.test","password":"Auto@12345","role":"admin","isAdmin":true,"isActive":false,"id":"custom-id-1790150428","config":{"theme":"hacked"}}` | 2. `201` — hoặc `422` từ chối trường lạ (ghi lại status thật)<br>3. Nếu bước 2 là `201`: `200` — **đăng nhập được** (`isActive:false` **không** khoá tài khoản)<br>5. Body **chỉ** 7 khoá `id` · `name` · `email` · `avatarUrl` · `phone` · `address` · `config` (REQ-35) · `id` **khác** `custom-id-1790150428` · **không** có khoá `role` / `isAdmin`<br><br>⚠️ Chưa gọi thật. `config` nhận giá trị gì là `AMB-BK-10` — **không** chấm | High | Yes | API | @API, @Security, @NeedsVerify |
| BK_AUTH_TC_068 | REQ-BK-AUTH-01, REQ-BK-AUTH-36 | AUTH | High | Chuỗi tấn công và chuỗi rất dài ở `name` — không lỗi máy chủ, lưu và trả lại nguyên văn | Email mỗi biến thể chưa từng đăng ký | 1. Gửi `POST /api/register` JSON, `name` theo biến thể, `password` = `Auto@12345`<br>2. Đọc status<br>3. Nếu `201`: đăng nhập, gọi `GET /api/me`<br>4. So `name` trả về với `name` đã gửi | **Bảng biến thể** — `name` · `email`:<br>`a` `<script>alert(1)</script>` · `auto_auth_1790150441_xss@auto.test`<br>`b` `Robert'); DROP TABLE users;--` · `auto_auth_1790150442_sqli@auto.test`<br>`c` `{"$gt":""}` (chuỗi) · `auto_auth_1790150443_nosql@auto.test`<br>`d` Chuỗi `A` dài **10.000** ký tự · `auto_auth_1790150444_long@auto.test` | **Mọi biến thể cho cùng kết quả:**<br>2. `201` hoặc `422` có `fields.name` — **tuyệt đối không** `500`, không treo quá 10 giây, body lỗi không chứa stack trace / tên bảng<br>4. Nếu đã tạo: `name` trả về **bằng đúng** chuỗi đã gửi (không bị cắt, không bị mã hoá HTML như `&lt;`)<br><br>⚠️ Chưa gọi thật. Không có REQ độ dài `name` phía API (`AMB-BK-AUTH-14`) — `d` chỉ chấm độ bền | High | Yes | API | @API, @Security, @Boundary, @NeedsVerify |

---

## 2. `POST /api/login`

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_069 | REQ-BK-AUTH-11 | AUTH | High | Đăng nhập thành công trả đủ `msg` · `accessToken` · `exp` | Setup: `userA` đã đăng ký | 1. Gửi `POST /api/login` JSON với email + mật khẩu của `userA`<br>2. Đọc status<br>3. Đọc body<br>4. Tách `accessToken` theo dấu `.` | `{"email":"auto_auth_1790150430_a@auto.test","password":"Auto@12345"}` | 2. `200`<br>3. Body có **đủ** 3 khoá kiểu string: `msg` (**chứa** `Login successfully`) · `accessToken` · `exp` (không rỗng — quan sát `6d`, **không** assert giá trị)<br>4. Đúng **3** đoạn, mỗi đoạn chỉ gồm ký tự base64url (`A-Z a-z 0-9 - _`) | Critical | Yes | API | @API, @Smoke, @CriticalPath |
| BK_AUTH_TC_070 | REQ-BK-AUTH-23 | AUTH | Medium | Đăng nhập nhận đủ 3 content-type khai trong spec | Setup: `userA` đã đăng ký | 1. Gửi `POST /api/login` với `email` + `password` của `userA`, mã hoá theo biến thể<br>2. Đọc status và `msg` | **Bảng biến thể** — content-type:<br>`a` `application/json`<br>`b` `multipart/form-data`<br>`c` `application/x-www-form-urlencoded` | **Mọi biến thể cho cùng kết quả:**<br>2. `200` · `msg` chứa `Login successfully` · có `accessToken`<br><br>⚠️ `c` chưa gọi thật (REQ-23 ghi `❔`) | Medium | Yes | API | @API, @Regression, @NeedsVerify |
| BK_AUTH_TC_071 | REQ-BK-AUTH-13 | AUTH | High | Access token từ đăng nhập dùng được và trỏ đúng tài khoản | Setup: `userA` và `userB` đã đăng ký | 1. Đăng nhập `userA` → lấy `accessToken` (tokenA)<br>2. `GET /api/me` với `Authorization: Bearer <tokenA>`<br>3. Đăng nhập `userB` → tokenB, gọi `GET /api/me` với tokenB | `userA`: `auto_auth_1790150430_a@auto.test` · `userB`: `auto_auth_1790150431_b@auto.test` · mật khẩu `Auto@12345` | 2. `200` · `email` = `auto_auth_1790150430_a@auto.test`<br>3. `200` · `email` = `auto_auth_1790150431_b@auto.test` — token của ai trả dữ liệu người đó, không lẫn | High | Yes | API | @API, @Smoke, @Security |
| BK_AUTH_TC_072 | REQ-BK-AUTH-14 | AUTH | Medium | Email đăng nhập không phân biệt hoa thường | Setup: `userA` đăng ký bằng email chữ thường | 1. Gửi `POST /api/login`, `email` theo biến thể, `password` = `Auto@12345`<br>2. Đọc status | **Bảng biến thể** — `email`:<br>`a` `AUTO_AUTH_1790150430_A@AUTO.TEST`<br>`b` `Auto_Auth_1790150430_A@Auto.Test` | **Mọi biến thể cho cùng kết quả:**<br>2. `200` · body có `accessToken` · `GET /api/me` với token đó trả `email` **chữ thường** `auto_auth_1790150430_a@auto.test`<br><br>⚠️ `b` và phần `GET /api/me` chưa gọi thật | Medium | Yes | API | @API, @Regression, @NeedsVerify |
| BK_AUTH_TC_073 | REQ-BK-AUTH-15 | AUTH | High | Sai mật khẩu bị từ chối, không mở phiên | Setup: `userA` đã đăng ký, mật khẩu `Auto@12345` | 1. Gửi `POST /api/login`, email của `userA`, `password` theo biến thể<br>2. Đọc status<br>3. Đọc body<br>4. Đọc header response | **Bảng biến thể** — `password`:<br>`a` Sai hẳn → `Sai@12345`<br>`b` Chỉ khác hoa thường → `auto@12345`<br>`c` Thừa dấu cách cuối → `Auto@12345 ` | **Mọi biến thể cho cùng kết quả:**<br>2. `400`<br>3. Đúng nguyên văn `{"msg":"Invalid password.","fields":{"password":["Invalid password, please try again."]}}`<br>4. **Không** có `Set-Cookie`<br><br>⚠️ `b` `c` chưa gọi thật | High | Yes | API | @API, @Regression, @NeedsVerify |
| BK_AUTH_TC_074 | REQ-BK-AUTH-16 | AUTH | High | Email chưa đăng ký hoặc tài khoản đã xoá bị từ chối | Biến thể `b`: setup `userC` đã đăng ký rồi **xoá** bằng token của chính nó (`DELETE /api/user/{id}`) | 1. Gửi `POST /api/login`, `email` theo biến thể, `password` = `Auto@12345`<br>2. Đọc status<br>3. Đọc body | **Bảng biến thể** — `email`:<br>`a` Chưa từng đăng ký → `auto_auth_1790150435_none@auto.test`<br>`b` Tài khoản vừa xoá → `auto_auth_1790150436_c@auto.test` | **Mọi biến thể cho cùng kết quả:**<br>2. `404`<br>3. Đúng nguyên văn `{"msg":"User not found.","fields":{"email":["Email not found, please register."]}}` · không `Set-Cookie`<br><br>ℹ️ Status + message khác `TC_073` → lộ email đã đăng ký (F-20 · `RISK-BK-AUTH-06`, `AMB-BK-AUTH-09` 🟡). TC chốt hành vi hiện tại | High | Yes | API | @API, @Regression |
| BK_AUTH_TC_075 | REQ-BK-AUTH-17 | AUTH | High | Thiếu `email` khi đăng nhập bị từ chối với lỗi thiếu field — ⚪ Chờ chốt `AMB-BK-AUTH-03` | — (TC **không** tạo, sửa, xoá tài khoản nào) | 1. Gửi `POST /api/login` JSON chỉ có `password`<br>2. Đọc status<br>3. Đọc body | `{"password":"Auto@12345"}` | 2. `422`<br>3. `fields.email` báo thiếu field<br><br>⚪ **Dự kiến FAIL** theo hiện trạng (F-19): server dùng `user@example.com` → `400` `Invalid password.`. **Chạy đúng 1 lần mỗi lượt** — mỗi lần chạy là một lần đăng nhập sai vào tài khoản không do QA sở hữu | High | Yes | API | @API, @Regression, @PendingAMB |
| BK_AUTH_TC_076 | REQ-BK-AUTH-18 | AUTH | High | Thiếu `password` khi đăng nhập bị từ chối | Setup: `userA` đã đăng ký | 1. Gửi `POST /api/login` JSON chỉ có `email` của `userA`<br>2. Đọc status<br>3. Đọc body | `{"email":"auto_auth_1790150430_a@auto.test"}` | 2. `422`<br>3. `msg` = `Invalid data.` · `fields.password` chứa `Expected property 'password' to be string but found: undefined` · không `Set-Cookie` | High | Yes | API | @API, @Regression |
| BK_AUTH_TC_077 | REQ-BK-AUTH-19 | AUTH | High | `email` sai định dạng khi đăng nhập bị từ chối | — | 1. Gửi `POST /api/login` JSON, `email` theo biến thể, `password` = `Auto@12345`<br>2. Đọc status và `fields.email` | **Bảng biến thể** — `email`:<br>`a` `khong-phai-email`<br>`b` `auto_auth@`<br>`c` `@auto.test`<br>`d` `auto@auth@auto.test` | **Mọi biến thể cho cùng kết quả:**<br>2. `422` · `fields.email` **chứa** `Property 'email' should be email`<br><br>⚠️ `b`→`d` chưa gọi thật | High | Yes | API | @API, @Regression, @NeedsVerify |
| BK_AUTH_TC_078 | REQ-BK-AUTH-20 | AUTH | High | Cookie `refetchToken` được set với đủ cờ bảo mật | Setup: `userA` đã đăng ký | 1. Gửi `POST /api/login` hợp lệ cho `userA`<br>2. Tìm header `Set-Cookie` có tên `refetchToken`<br>3. Đọc các thuộc tính của cookie đó | Body: email + mật khẩu của `userA` | 2. Có **đúng một** `Set-Cookie: refetchToken=<chuỗi không rỗng>`<br>3. Có **đủ** `HttpOnly` · `Secure` · `SameSite=Strict` · `Path=/` · có thuộc tính `Max-Age` (**không** assert giá trị — `AMB-BK-AUTH-10`) | High | Yes | API | @API, @Security, @Regression |
| BK_AUTH_TC_079 | REQ-BK-AUTH-21 | AUTH | Medium | Cookie `accessToken` mang cùng giá trị với `accessToken` trong body | Setup: `userA` đã đăng ký | 1. Gửi `POST /api/login` hợp lệ cho `userA`<br>2. Lấy giá trị cookie `accessToken` từ `Set-Cookie`<br>3. So với `body.accessToken` của **cùng** response | Body: email + mật khẩu của `userA` | 2. Có `Set-Cookie: accessToken=<JWT>`<br>3. Hai giá trị **bằng nhau** từng ký tự | Medium | Yes | API | @API, @Regression |
| BK_AUTH_TC_080 | REQ-BK-AUTH-22 | AUTH | High | Cookie `accessToken` có cờ `HttpOnly` · `Secure` · `SameSite` | Setup: `userA` đã đăng ký | 1. Gửi `POST /api/login` hợp lệ cho `userA`<br>2. Đọc thuộc tính của `Set-Cookie: accessToken=…` | Body: email + mật khẩu của `userA` | 2. Có **đủ** `HttpOnly` · `Secure` · `SameSite` (giá trị `Strict` hoặc `Lax`)<br><br>🐞 **@KnownBug — dự kiến FAIL:** hiện cookie chỉ có `Path=/` (F-03). `AMB-BK-AUTH-04` đã chốt 20-09-2026: yêu cầu **áp dụng** → mở bug, **không** hạ kỳ vọng | High | Yes | API | @API, @Security, @KnownBug |
| BK_AUTH_TC_081 | REQ-BK-AUTH-15 | AUTH | High | Chuỗi tấn công ở `password` không vượt được đăng nhập, không lỗi máy chủ | Setup: `userA` đã đăng ký | 1. Gửi `POST /api/login` JSON, `email` của `userA`, `password` theo biến thể<br>2. Đọc status và body | **Bảng biến thể** — `password`:<br>`a` `' OR '1'='1`<br>`b` `' OR 1=1--`<br>`c` `{"$ne":null}` (dạng **object** JSON, không phải chuỗi)<br>`d` Chuỗi `A` dài **10.000** ký tự | **Mọi biến thể cho cùng kết quả:**<br>2. **Không** `200`, **không** có `accessToken` · status là `400` `Invalid password.` hoặc `422` `Invalid data.` · **tuyệt đối không** `500`, body không chứa stack trace<br><br>⚠️ Chưa gọi thật | High | Yes | API | @API, @Security, @NeedsVerify |
| BK_AUTH_TC_106 | REQ-BK-AUTH-03 | AUTH | High | Tài khoản vừa đăng ký đăng nhập được ngay | Email ở Test Data chưa từng đăng ký | 1. Gửi `POST /api/register` JSON theo Test Data<br>2. **Ngay sau đó** gửi `POST /api/login` với đúng `email` + `password` vừa đăng ký<br>3. Đọc status và body của bước 2 | `{"name":"Auto Auth API New 1790150448","email":"auto_auth_1790150448_new@auto.test","password":"Auto@12345"}` | 1. `201` · `Register successfully`<br>3. `200` · body có `accessToken` (chuỗi không rỗng) — tác tạo của đăng ký dùng được, không cần kích hoạt hay chờ | Critical | Yes | API | @API, @Smoke, @CriticalPath |
| BK_AUTH_TC_107 | REQ-BK-AUTH-12 | AUTH | High | Đăng nhập là endpoint công khai — không phụ thuộc header `Authorization` | Setup: `userA` đã đăng ký | 1. Gửi `POST /api/login` với email + mật khẩu của `userA`, header `Authorization` theo biến thể<br>2. Đọc status | **Bảng biến thể** — header `Authorization`:<br>`a` Không gửi header<br>`b` Gửi token rác → `Bearer abc.def.ghi` | **Mọi biến thể cho cùng kết quả:**<br>2. `200` · có `accessToken` — **không** bị `401` (spec khai `security: []`)<br><br>⚠️ `b` chưa gọi thật | High | Yes | API | @API, @Regression, @NeedsVerify |
