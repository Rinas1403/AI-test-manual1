# Test Cases — `AUTH` · API · Part 02 — Làm mới token · Đăng xuất · Thông tin & Hồ sơ — 25 TC · độ hạt GỘP

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_AUTH_SUMMARY.md](../../TEST_CASES_AUTH_SUMMARY.md) — Coverage · 4 vòng · ISO 25010 · Automation · Nhật ký |
| **Nguồn requirement** | [requirements_auth_api.md](../../../../../requirements/_book-api/auth/api/requirements_auth_api.md) · bản đồ API [api_map.md](../../../../../requirements/_book-api/_discovery/api_map.md) |
| **Base URL** | `https://book.anhtester.com` · spec snapshot [`openapi_2026-09-19.json`](../../../../../requirements/_book-api/_discovery/sources/openapi_2026-09-19.json) |
| **Auth** | `DELETE /api/logout` · `GET /api/me` · `PATCH /api/profile`: 🔒 `BearerAuth` (kế thừa cấp gốc) · `POST /api/refetch-token`: 🌐 công khai, **chỉ** đọc cookie `refetchToken` |
| **Dải TC ID** | `BK_AUTH_TC_082` → `BK_AUTH_TC_105` · `BK_AUTH_TC_108` (bổ sung 20-09-2026 khi tách `TC_082`) |
| **Part liền kề** | Trước: [part_01_api_dang_ky_dang_nhap.md](part_01_api_dang_ky_dang_nhap.md) (`TC_059` → `TC_081` · `TC_106` · `TC_107`) |

> **Ký hiệu dùng trong bảng:** `tokenA` = `body.accessToken` của lần `POST /api/login` gần nhất bằng `userA` · `rtA` = giá trị cookie `refetchToken` của **cùng** response đó. Mỗi TC tự đăng nhập lại ở bước đầu — **không** dùng token của TC khác (TC độc lập, chạy song song được).
>
> 🐞 **5 TC `@KnownBug` của `PATCH /api/profile` (TC_098 · 099 · 102 · 103 · 104)** dự kiến FAIL vì F-16 — `AMB-BK-AUTH-01` đã chốt 20-09-2026: **lỗi cần báo**. Chạy bình thường, FAIL thì dẫn về một bug chung, **không** sửa kỳ vọng.

---

## Endpoint Catalog (part này)

| # | Method | Path | Auth | Mô tả | Số Test Cases |
|---|---|---|---|---|---|
| 3 | POST | `/api/refetch-token` | 🌐 Công khai (cookie `refetchToken` bắt buộc) | Cấp `accessToken` mới từ refresh token trong cookie | 5 (`082` → `085` · `108`) |
| 4 | DELETE | `/api/logout` | 🔒 Bearer | Đăng xuất; có cookie `refetchToken` thì yêu cầu xoá cookie | 5 (`086` → `090`) |
| 5 | GET | `/api/me` | 🔒 Bearer | Thông tin người dùng hiện tại (7 khoá) | 7 (`091` → `097`) |
| 6 | PATCH | `/api/profile` | 🔒 Bearer | Cập nhật hồ sơ / đổi mật khẩu — **đang hỏng (F-16)** | 7 (`098` → `104`) |
| — | *(nhiều)* | — | — | Hình dạng body lỗi dùng chung (error envelope) | 1 (`105`) |

---

## 3. `POST /api/refetch-token`

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_082 | REQ-BK-AUTH-24 | AUTH | High | Làm mới token thành công, trả đủ `msg` · `accessToken` · `exp` | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → lấy `rtA`<br>2. Gửi `POST /api/refetch-token` với header `Cookie: refetchToken=<rtA>`<br>3. Đọc status<br>4. Đọc body | Cookie: `rtA` của bước 1 | 3. `200`<br>4. Đủ 3 khoá string: `msg` (**chứa** `Refetch token successfully`) · `accessToken` (JWT 3 đoạn) · `exp` (không rỗng, **không** assert giá trị) | Critical | Yes | API | @API, @Smoke, @CriticalPath |
| BK_AUTH_TC_083 | REQ-BK-AUTH-25 | AUTH | High | Access token mới từ làm mới token dùng được, đúng tài khoản | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → `tokenA`, `rtA`<br>2. `POST /api/refetch-token` với `Cookie: refetchToken=<rtA>` → lấy `accessToken` mới (`tokenA2`)<br>3. `GET /api/me` với `Authorization: Bearer <tokenA2>` | Cookie: `rtA` | 2. `200`<br>3. `200` · `email` = `auto_auth_1790150430_a@auto.test` | High | Yes | API | @API, @Regression |
| BK_AUTH_TC_084 | REQ-BK-AUTH-27 | AUTH | High | Thiếu cookie `refetchToken` bị từ chối — kể cả khi có Bearer hợp lệ | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → `tokenA`<br>2. Gửi `POST /api/refetch-token` theo biến thể<br>3. Đọc status và body | **Bảng biến thể:**<br>`a` Không cookie, không `Authorization`<br>`b` Không cookie, có `Authorization: Bearer <tokenA>` hợp lệ | **Mọi biến thể cho cùng kết quả:**<br>3. `422` · `msg` = `Invalid data.` · `fields.refetchToken` **chứa** `Property 'refetchToken' is missing` | High | Yes | API | @API, @Regression |
| BK_AUTH_TC_085 | REQ-BK-AUTH-28 | AUTH | High | Cookie `refetchToken` không hợp lệ bị từ chối | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → `tokenA`, `rtA`<br>2. Gửi `POST /api/refetch-token` với `Cookie: refetchToken=<giá trị theo biến thể>`<br>3. Đọc status và body | **Bảng biến thể** — giá trị cookie:<br>`a` Chuỗi rác → `abc`<br>`b` Access token đặt nhầm vào cookie → `<tokenA>`<br>`c` `rtA` bị sửa ký tự cuối (đổi `A`↔`B`, hoặc thêm `x`)<br>`d` Chuỗi rỗng → `refetchToken=` | **Mọi biến thể cho cùng kết quả:**<br>3. `404` · body đúng nguyên văn `{"msg":"Invalid token."}` · **không** có `accessToken`<br><br>⚠️ `c` `d` chưa gọi thật — `d` có thể ra `422` như thiếu cookie (ghi lại, đưa vào AMB) | High | Yes | API | @API, @Regression, @Security, @NeedsVerify |
| BK_AUTH_TC_108 | REQ-BK-AUTH-26 | AUTH | High | Làm mới token là endpoint công khai — chỉ dựa vào cookie, không phụ thuộc header `Authorization` | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → lấy `rtA`<br>2. Gửi `POST /api/refetch-token` với `Cookie: refetchToken=<rtA>` và header `Authorization` theo biến thể<br>3. Đọc status | **Bảng biến thể** — header `Authorization`:<br>`a` Không gửi header<br>`b` Gửi token rác → `Bearer abc.def.ghi` | **Mọi biến thể cho cùng kết quả:**<br>3. `200` · có `accessToken` — **không** bị `401` (spec khai `security: []`)<br><br>⚠️ `b` chưa gọi thật | High | Yes | API | @API, @Regression, @NeedsVerify |

---

## 4. `DELETE /api/logout`

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_086 | REQ-BK-AUTH-29 | AUTH | High | Đăng xuất thành công bằng Bearer hợp lệ | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → `tokenA`<br>2. Gửi `DELETE /api/logout` với `Authorization: Bearer <tokenA>`, **không** gửi cookie<br>3. Đọc status và body<br>4. Đọc header response | — | 3. `200` · `msg` **chứa** `Logout successfully`<br>4. **Không** có `Set-Cookie` (không gửi cookie thì không có gì để xoá) | Critical | Yes | API | @API, @Smoke, @CriticalPath |
| BK_AUTH_TC_087 | REQ-BK-AUTH-32 | AUTH | Medium | Đăng xuất kèm cookie thì yêu cầu trình duyệt xoá cookie `refetchToken` | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → `tokenA`, `rtA`<br>2. Gửi `DELETE /api/logout` với `Authorization: Bearer <tokenA>` **và** `Cookie: refetchToken=<rtA>`<br>3. Đọc `Set-Cookie` của response | — | 2. `200`<br>3. Có `Set-Cookie: refetchToken=` với giá trị **rỗng** · có `Max-Age=0` (quan sát kèm `Expires=Thu, 01 Jan 1970 00:00:00 GMT`). **Không** chấm việc có / không xoá cookie `accessToken` (`AMB-BK-AUTH-13`) | Medium | Yes | API | @API, @Regression, @Security |
| BK_AUTH_TC_088 | REQ-BK-AUTH-30 | AUTH | High | Đăng xuất không có token bị từ chối | — | 1. Gửi `DELETE /api/logout` **không** có `Authorization`<br>2. Đọc status và body | — | 2. `401` · body là object **rỗng** `{}` (khác các endpoint khác có `msg` — đúng schema 401 của spec) | High | Yes | API | @API, @Regression, @Security |
| BK_AUTH_TC_089 | REQ-BK-AUTH-31 | AUTH | High | Đăng xuất với token không hợp lệ bị từ chối | Setup: `userA` đã đăng ký (cho biến thể `b`) | 1. Gửi `DELETE /api/logout` với header `Authorization` theo biến thể<br>2. Đọc status và body | **Bảng biến thể** — header `Authorization`:<br>`a` Chuỗi rác → `Bearer abc.def.ghi`<br>`b` `tokenA` bị sửa 1 ký tự ở phần chữ ký (đoạn thứ 3)<br>`c` Thiếu tiền tố → `<tokenA>` (không có chữ `Bearer`)<br>`d` Sai scheme → `Basic YWJjOmRlZg==` | **Mọi biến thể cho cùng kết quả:**<br>2. `401` · body `{}`<br><br>⚠️ `b`→`d` chưa gọi thật | High | Yes | API | @API, @Regression, @Security, @NeedsVerify |
| BK_AUTH_TC_090 | REQ-BK-AUTH-33 | AUTH | High | Sau đăng xuất, `refetchToken` của phiên đó không còn làm mới được token | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → `tokenA`, `rtA`<br>2. `POST /api/refetch-token` với `Cookie: refetchToken=<rtA>` — xác nhận cookie đang sống<br>3. `DELETE /api/logout` với `Bearer <tokenA>` + `Cookie: refetchToken=<rtA>`<br>4. `POST /api/refetch-token` lại với **cùng** `rtA` | Cookie: `rtA` | 2. `200` (trạng thái sạch: cookie còn hiệu lực trước khi đăng xuất)<br>3. `200` · `Logout successfully`<br>4. **Không** phải `200` — kỳ vọng `404` `{"msg":"Invalid token."}` như REQ-28<br><br>🐞 **@KnownBug — dự kiến FAIL:** bước 4 hiện vẫn `200` (F-17). `AMB-BK-AUTH-02` đã chốt 20-09-2026: **lỗi cần báo** | High | Yes | API | @API, @Security, @KnownBug |

---

## 5. `GET /api/me`

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_091 | REQ-BK-AUTH-34 | AUTH | High | Lấy thông tin người dùng hiện tại | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → `tokenA`<br>2. Gửi `GET /api/me` với `Authorization: Bearer <tokenA>`<br>3. Đọc status<br>4. Đọc khoá và kiểu của body | — | 3. `200`<br>4. Có đủ khoá `id` · `name` · `email` · `avatarUrl` · `phone` · `address` (đều string) · `config` (có mặt, không chấm kiểu — `AMB-BK-10`) · `id` không rỗng | Critical | Yes | API | @API, @Smoke, @CriticalPath |
| BK_AUTH_TC_092 | REQ-BK-AUTH-35 | AUTH | High | Thông tin người dùng không lộ mật khẩu hay khoá ngoài danh sách | Setup: `userA` đã đăng ký | 1. Đăng nhập `userA`, gọi `GET /api/me`<br>2. Liệt kê toàn bộ khoá ở cấp gốc của body<br>3. Tìm trong **toàn bộ** body (kể cả lồng trong `config`) | — | 2. **Đúng 7** khoá: `id` · `name` · `email` · `avatarUrl` · `phone` · `address` · `config` — không hơn<br>3. **Không** có khoá `password` / `hash` / `salt` / `refetchToken`; không có chuỗi `Auto@12345` hay chuỗi bắt đầu `$2` (dạng băm bcrypt) | High | Yes | API | @API, @Security, @Regression |
| BK_AUTH_TC_093 | REQ-BK-AUTH-36 | AUTH | High | Thông tin trả về khớp dữ liệu đã đăng ký | Tài khoản mỗi biến thể **vừa** đăng ký bằng Test Data | 1. `POST /api/register` với body theo biến thể<br>2. Đăng nhập bằng email + `Auto@12345`<br>3. `GET /api/me`<br>4. So từng trường với dữ liệu đã gửi | **Bảng biến thể:**<br>`a` Đủ trường → `name` `Auto Auth API Full`, `email` `auto_auth_1790150445_full@auto.test`, `phone` `0912345678`, `address` `12 Tràng Tiền`<br>`b` Chỉ trường bắt buộc → `name` `Auto Auth API Min`, `email` `auto_auth_1790150446_min@auto.test`<br>`c` Unicode + emoji → `name` `Nguyễn Thị Ánh 😀`, `email` `auto_auth_1790150447_vn@auto.test`, `address` `Số 5, ngõ 12 — Hà Nội` | **Mọi biến thể cho cùng kết quả:**<br>4. `name` · `email` · `phone` · `address` **bằng đúng** giá trị đã gửi (không mất dấu, không mã hoá) · trường không gửi trả `""` · `config` = `{}` (tài khoản mới)<br><br>⚠️ `c` chưa gọi thật | High | Yes | API | @API, @Regression, @Localization, @NeedsVerify |
| BK_AUTH_TC_094 | REQ-BK-AUTH-37 | AUTH | High | Lấy thông tin khi không có token bị từ chối | — | 1. Gửi `GET /api/me` **không** có `Authorization`<br>2. Đọc status và body | — | 2. `401` · body đúng nguyên văn `{"msg":"Missing or invalid Authorization header"}` | High | Yes | API | @API, @Regression, @Security |
| BK_AUTH_TC_095 | REQ-BK-AUTH-38 | AUTH | High | Lấy thông tin với token không hợp lệ bị từ chối | Setup: `userA` đã đăng ký (cho biến thể `b` `c`) | 1. Gửi `GET /api/me` với header `Authorization` theo biến thể<br>2. Đọc status và body | **Bảng biến thể:**<br>`a` `Bearer abc.def.ghi`<br>`b` `tokenA` sửa 1 ký tự phần chữ ký<br>`c` `tokenA` sửa phần payload (đoạn 2) rồi giữ chữ ký cũ<br>`d` JWT thuật toán `none`: `Bearer eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJ4In0.` | **Mọi biến thể cho cùng kết quả:**<br>2. `401` · `msg` = `Unauthorized` · **không** trả dữ liệu người dùng nào<br><br>⚠️ `b`→`d` chưa gọi thật — nếu `d` trả `200` → **FAIL nghiêm trọng**, mở bug ngay | High | Yes | API | @API, @Security, @NeedsVerify |
| BK_AUTH_TC_096 | REQ-BK-AUTH-39 | AUTH | High | Endpoint cần xác thực chỉ nhận token qua header, không nhận qua cookie | Setup: `userA` đã đăng ký | 1. `POST /api/login` bằng `userA` → `tokenA`<br>2. Gửi `GET /api/me` **chỉ** với `Cookie: accessToken=<tokenA>`, **không** có `Authorization`<br>3. Đọc status và body | — | 3. `401` · `msg` = `Missing or invalid Authorization header` | High | Yes | API | @API, @Security, @Regression |
| BK_AUTH_TC_097 | REQ-BK-AUTH-40 | AUTH | High | Token của tài khoản đã bị xoá không còn dùng được | Setup: `userD` **vừa** đăng ký (`auto_auth_1790150437_d@auto.test`) | 1. Đăng nhập `userD` → `tokenD`<br>2. `GET /api/me` với `tokenD` → ghi lại `id`<br>3. `DELETE /api/user/<id của bước 2>` với `Bearer <tokenD>` (xoá **chính** `userD`)<br>4. `GET /api/me` lại với **cùng** `tokenD` | Tài khoản: `userD` — **chỉ** xoá tài khoản do chính TC tạo | 2. `200` · `email` = email của `userD`<br>3. `200` (xoá thành công)<br>4. `401` · `msg` = `User no longer exists` | High | Yes | API | @API, @Security, @Regression |

---

## 6. `PATCH /api/profile`

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_098 | REQ-BK-AUTH-41 | AUTH | High | Cập nhật tên hiển thị qua hồ sơ | Setup: `userA` đã đăng ký | 1. Đăng nhập `userA` → `tokenA`<br>2. Gửi `PATCH /api/profile` JSON với `Bearer <tokenA>`<br>3. Đọc status và body | `{"name":"Auto Auth API Renamed 1790150430"}` | 3. `200` · body có `msg` (string)<br><br>🐞 **@KnownBug — dự kiến FAIL:** hiện `422` `{"msg":"Invalid data.","fields":{"fields":["Property 'fields' is missing","Expected object"]}}` với mọi body (F-16) | High | Yes | API | @API, @Regression, @KnownBug |
| BK_AUTH_TC_099 | REQ-BK-AUTH-42 | AUTH | High | Thông tin người dùng phản ánh tên vừa cập nhật | Vừa chạy `TC_098` với `userA` | 1. `GET /api/me` với `tokenA`<br>2. Đọc `name` | — | 2. `name` = `Auto Auth API Renamed 1790150430`<br><br>🐞 **@KnownBug — dự kiến FAIL** theo `TC_098` (F-16): `name` không đổi | High | Yes | API | @API, @Regression, @KnownBug |
| BK_AUTH_TC_100 | REQ-BK-AUTH-43 | AUTH | High | Cập nhật hồ sơ khi không có token bị từ chối | — | 1. Gửi `PATCH /api/profile` JSON `{"name":"Hacker"}` **không** có `Authorization`<br>2. Đọc status và body | `{"name":"Hacker"}` | 2. `401` · `msg` = `Missing or invalid Authorization header` | High | Yes | API | @API, @Security, @Regression |
| BK_AUTH_TC_101 | REQ-BK-AUTH-44 | AUTH | Medium | Cập nhật hồ sơ với `email` sai định dạng bị từ chối | Setup: `userA` đã đăng ký | 1. Đăng nhập `userA` → `tokenA`<br>2. Gửi `PATCH /api/profile` với `Bearer <tokenA>`, body theo biến thể<br>3. Đọc status và `fields.email`<br>4. `GET /api/me` | **Bảng biến thể** — body:<br>`a` `{"email":"khong-phai-email"}`<br>`b` `{"email":"auto_auth@"}` | **Mọi biến thể cho cùng kết quả:**<br>3. `422` · `fields.email` **chứa** `Property 'email' should be email`<br>4. `email` **vẫn** là email cũ của `userA`<br><br>⚠️ `b` chưa gọi thật | Medium | Yes | API | @API, @Regression, @NeedsVerify |
| BK_AUTH_TC_102 | REQ-BK-AUTH-45 | AUTH | High | Đổi mật khẩu qua hồ sơ | Setup: `userE` **vừa** đăng ký (`auto_auth_1790150438_e@auto.test`, mật khẩu `Auto@12345`) — tài khoản riêng để không làm hỏng `userA` | 1. Đăng nhập `userE` → `tokenE`<br>2. Gửi `PATCH /api/profile` với `Bearer <tokenE>`<br>3. Đọc status | `{"password":"Moi@67890","password_old":"Auto@12345"}` | 3. `200` · body có `msg`<br><br>🐞 **@KnownBug — dự kiến FAIL:** hiện `422` `fields.fields` (F-16) | High | Yes | API | @API, @Security, @KnownBug |
| BK_AUTH_TC_103 | REQ-BK-AUTH-46 | AUTH | High | Đăng nhập được bằng mật khẩu mới sau khi đổi | Vừa chạy `TC_102` | 1. `POST /api/login` với email `userE` + mật khẩu mới<br>2. Đọc status | `{"email":"auto_auth_1790150438_e@auto.test","password":"Moi@67890"}` | 2. `200` · có `accessToken`<br><br>🐞 **@KnownBug — dự kiến FAIL:** hiện `400` `Invalid password.` vì mật khẩu chưa đổi (F-16) | High | Yes | API | @API, @Security, @KnownBug |
| BK_AUTH_TC_104 | REQ-BK-AUTH-47 | AUTH | High | Mật khẩu cũ bị từ chối sau khi đổi | Vừa chạy `TC_102` | 1. `POST /api/login` với email `userE` + mật khẩu **cũ**<br>2. Đọc status và body | `{"email":"auto_auth_1790150438_e@auto.test","password":"Auto@12345"}` | 2. `400` · `msg` = `Invalid password.`<br><br>🐞 **@KnownBug — dự kiến FAIL:** hiện `200` (F-16) | High | Yes | API | @API, @Security, @KnownBug |

---

## 7. Quy ước chung

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_105 | REQ-BK-AUTH-48 | AUTH | Medium | Body lỗi kiểm tra dữ liệu có hình dạng `msg` + `fields`, khoá của `fields` là tên field vi phạm | Setup: `userA` đã đăng ký (cho biến thể `f`) | 1. Gửi request theo biến thể<br>2. Đọc status<br>3. Kiểm hình dạng body | **Bảng biến thể** — request → field vi phạm:<br>`a` `POST /api/register` thiếu `name` → `name`<br>`b` `POST /api/register` thiếu `password` → `password`<br>`c` `POST /api/login` thiếu `password` → `password`<br>`d` `POST /api/login` `email` = `khong-phai-email` → `email`<br>`e` `POST /api/refetch-token` không cookie → `refetchToken`<br>`f` `PATCH /api/profile` (Bearer `userA`) `{"email":"khong-phai-email"}` → `email` | **Mọi biến thể cho cùng kết quả:**<br>2. `422`<br>3. Body là object có `msg` (string) và `fields` (object) · `fields` có **đúng khoá** là tên field vi phạm ghi ở biến thể · giá trị là **mảng string** không rỗng · **không** có khoá lạ như `fields.fields` | Medium | Yes | API | @API, @Regression |
