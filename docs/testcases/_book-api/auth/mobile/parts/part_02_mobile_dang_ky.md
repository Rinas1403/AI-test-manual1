# Test Cases — `AUTH` · Mobile (Android) · Part 02 — Đăng ký — 33 TC · độ hạt GỘP

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_AUTH_SUMMARY.md](../../TEST_CASES_AUTH_SUMMARY.md) — cách đọc bộ TC · Dữ liệu dùng chung · Assumptions · Coverage · Evidence · 4 vòng · bộ chạy · Nhật ký |
| **Nguồn requirement** | [requirements_auth_mobile.md](../../../../../requirements/_book-api/auth/mobile/requirements_auth_mobile.md) · REQ dùng chung ở index [REQUIREMENTS_AUTH_SUMMARY.md](../../../../../requirements/_book-api/auth/REQUIREMENTS_AUTH_SUMMARY.md) mục 3 |
| **Nền tảng** | Android — emulator `Pixel_10_Pro_XL_API_37` · Android 17 · màn hình `1344×2992` · **hướng dọc** (trừ `TC_057`) · locale `en-US` · app `book.anhtester.com` `1.0 (versionCode 1)`. iOS **chưa khảo sát** |
| **Dải TC ID** | `BK_AUTH_TC_026` → `BK_AUTH_TC_058` — dải chung toàn module `AUTH` |
| **Part liền kề** | Trước: [part_01_mobile_dang_nhap.md](part_01_mobile_dang_nhap.md) (`TC_001` → `TC_025`) |

> ⚠️ **TC đăng ký tạo tài khoản thật trên production** (server duy nhất `book.anhtester.com`). Mọi email dùng tiền tố `auto_auth_` và **phải dọn** sau khi chạy — xem mục **Dữ liệu dùng chung · Dọn dữ liệu** ở [index](../../TEST_CASES_AUTH_SUMMARY.md).
>
> 💡 **Mẹo kiểm lỗi mà không tạo tài khoản:** ở các TC validation, cố ý **để trống ô Email** (hoặc một ô bắt buộc khác không phải ô đang kiểm) — form bị app chặn, không gửi lên máy chủ, nên không sinh tài khoản rác.

---

## Vòng 1 — Smoke

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_026 | REQ-BK-AUTH-65, REQ-BK-AUTH-77, REQ-BK-AUTH-79 | AUTH | High | Màn hình Sign up hiển thị đủ thành phần, đúng nhóm, đúng thứ tự và trạng thái mặc định | `S0` · đang ở Sign in | 1. Bấm chữ xanh `Get started`<br>2. Không chạm vào ô nào, đối chiếu từ trên xuống các mục `1`→`4` của Bảng kiểm<br>3. Cuộn xuống cuối màn hình, đối chiếu mục `5`→`7` | — (chỉ quan sát) | 1. Màn hình Sign up hiện ra<br>2–3. **Bảng kiểm — mọi mục phải đạt:**<br>`1` Đầu màn hình: logo `ANHTESTER` bên trái · chữ `Need help?` bên phải · **không** có nút avatar, **không** có thanh điều hướng đáy màn hình<br>`2` Tiêu đề `Sign up` · dưới là dòng `Already have an account?` kèm chữ xanh lá `Sign in`<br>`3` Nhóm có nhãn `Infomation` (đúng nguyên văn, xem **ASM-BK-AUTH-01**) chứa theo thứ tự: ô `Name *` · ô `Phone` (**không** có `*`)<br>`4` Ngay dưới, nhóm lồng nhãn `Address` chứa theo thứ tự: ô `Division` (mũi tên ⌄ bên phải) · ô `Ward` (mũi tên ⌄) · ô `Address` (ô cao, nhiều dòng). Không ô nào có `*`<br>`5` Nhóm nhãn `Account` chứa theo thứ tự: ô `Email *` · ô `Password *` · ô `Password Confirmation *` — hai ô mật khẩu có biểu tượng **mắt nhắm** cuối ô<br>`6` Nút nền đen `Register` có biểu tượng người-dấu-cộng, nằm **lệch phải**, dưới nhóm Account — trên màn hình dọc phải **cuộn** mới thấy<br>`7` (REQ-65 · 77 · 79) Mọi ô đều **trống** khi vừa mở; ô `Ward` và `Address` có chữ gợi ý **nhạt hơn** ô `Division` (đang khoá — hành vi khoá kiểm ở `TC_048`) | High | Yes | UI | @Smoke, @Android |
| BK_AUTH_TC_027 | REQ-BK-AUTH-66 | AUTH | High | Bấm "Get started" trên Sign in mở Sign up | `S0` · đang ở Sign in | 1. Bấm chữ xanh `Get started` ở dòng `Don’t have an account?`<br>2. Quan sát màn hình | — | 2. Màn hình Sign up hiện ra — tiêu đề `Sign up`, thấy nhóm `Infomation` với ô `Name *` | High | Yes | UI | @Smoke, @Android |
| BK_AUTH_TC_028 | REQ-BK-AUTH-67 | AUTH | Medium | Bấm "Sign in" trên Sign up quay về Sign in | `S0` · đang ở Sign up (mở từ `Get started`) | 1. Bấm chữ xanh `Sign in` ở dòng `Already have an account?`<br>2. Quan sát màn hình | — | 2. Màn hình Sign in hiện ra — tiêu đề `Sign in`, ô `Email address *`, nút `Login account` | Medium | Yes | UI | @Smoke, @Android |
| BK_AUTH_TC_029 | REQ-BK-AUTH-01, REQ-BK-AUTH-72 | AUTH | High | Đăng ký thành công chỉ với các trường bắt buộc | `S0` · đang ở Sign up, mọi ô trống · email ở Test Data **chưa** từng đăng ký. **TC này tạo tài khoản `TK-A`** dùng cho Part 01 | 1. Nhập ô `Name` theo Test Data<br>2. Để trống ô `Phone`, `Division`, `Ward`, `Address`<br>3. Nhập ô `Email` theo Test Data<br>4. Nhập ô `Password` theo Test Data<br>5. Nhập ô `Password Confirmation` giống hệt Password<br>6. Ẩn bàn phím, cuộn xuống, bấm `Register`<br>7. Quan sát màn hình và thông báo nổi<br>8. Nhìn hai ô của màn hình vừa hiện | Name: `Auto Auth 1790150400`<br>Email: `auto_auth_1790150400@auto.test`<br>Password: `Auto@12345`<br>Password Confirmation: `Auto@12345` | 2. Các ô không bắt buộc để trống **không** gây lỗi<br>6. Không có dòng lỗi đỏ nào dưới các ô<br>7. Chuyển sang màn hình **Sign in** · thông báo nổi ✓ xanh `Register successfully.`<br>8. Ô `Email address` và `Password` của Sign in đều **trống** (app **không** điền sẵn email vừa đăng ký) | Critical | Yes | UI | @Smoke, @CriticalPath, @Android |
| BK_AUTH_TC_030 | REQ-BK-AUTH-01, REQ-BK-AUTH-76, REQ-BK-AUTH-78, REQ-BK-AUTH-79 | AUTH | High | Đăng ký thành công khi điền đủ mọi trường, kể cả địa chỉ | `S0` · đang ở Sign up, mọi ô trống · email ở Test Data chưa từng đăng ký | 1. Nhập ô `Name`, `Phone` theo Test Data<br>2. Bấm ô `Division`, chọn `Thành phố Hà Nội`<br>3. Bấm ô `Ward`, chọn `Phường Hoàn Kiếm`<br>4. Bấm ô `Address`, nhập theo Test Data<br>5. Nhập `Email`, `Password`, `Password Confirmation` theo Test Data<br>6. Ẩn bàn phím, cuộn xuống, bấm `Register`<br>7. Quan sát màn hình và thông báo nổi | Name: `Auto Auth Full 1790150401`<br>Phone: `0912345678`<br>Division: `Thành phố Hà Nội`<br>Ward: `Phường Hoàn Kiếm`<br>Address: `12 Tràng Tiền`<br>Email: `auto_auth_1790150401_full@auto.test`<br>Password / Confirmation: `Auto@12345` | 2. Ô Division hiện `Thành phố Hà Nội`, có nút `×` bên cạnh mũi tên<br>3. Ô Ward hiện `Phường Hoàn Kiếm`<br>4. Ô Address nhận chữ, hiện `12 Tràng Tiền`<br>6. Không có dòng lỗi đỏ nào<br>7. Chuyển sang Sign in · thông báo ✓ `Register successfully.`<br><br>⚠️ Bước 4 và bước 7 (đăng ký có địa chỉ) chưa có evidence | High | Yes | UI | @Smoke, @Android, @NeedsVerify |
| BK_AUTH_TC_031 | REQ-BK-AUTH-03, REQ-BK-AUTH-59, REQ-BK-AUTH-60 | AUTH | High | Dữ liệu vừa đăng ký hiện đúng sau khi đăng nhập | Vừa chạy xong `TC_030` · đang ở Sign in | 1. Nhập Email `auto_auth_1790150401_full@auto.test`, Password `Auto@12345`<br>2. Bấm `Login account`<br>3. Đọc thẻ thứ hai dưới banner Dashboard<br>4. Bấm nút avatar, đọc hai dòng đầu menu | Email: `auto_auth_1790150401_full@auto.test`<br>Password: `Auto@12345` | 2. Vào tab Dashboard, thông báo `Login successfully.`<br>3. Thẻ ghi đúng `Welcome Auto Auth Full 1790150401` — không sai dấu cách, không thiếu ký tự<br>4. Dòng đầu là Name, dòng hai là email `auto_auth_1790150401_full@auto.test` (có thể bị cắt `…` ở cuối — phần nhìn thấy phải khớp)<br><br>🔧 Ghi chú kỹ thuật: Phone · Division · Ward · Address **không** hiển thị ở màn hình nào đã khảo sát (Profile chưa khảo sát) → đối chiếu bằng `GET /api/me` với token của chính tài khoản: `phone` = `0912345678`, địa chỉ khớp Test Data của `TC_030` | High | Yes | UI | @Smoke, @Android, @TechCheck |

---

## Vòng 2 — Functional

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_032 | REQ-BK-AUTH-68 | AUTH | High | Sign up: Name bỏ trống hoặc chỉ có khoảng trắng thì bị chặn | Đang ở Sign up, mọi ô trống. Chạy lại từ bước 1 cho từng biến thể (xoá trắng form giữa các lượt) | 1. Nhập ô `Name` theo biến thể<br>2. Nhập `Email`: `auto_auth_1790150410@auto.test`<br>3. Nhập `Password` và `Password Confirmation`: `Auto@12345`<br>4. Cuộn xuống, bấm `Register`<br>5. Cuộn lên, nhìn ô Name | **Bảng biến thể** — Name:<br>`a` Để trống<br>`b` Chỉ 3 dấu cách → `···` (`·` = 1 dấu cách) | **Mọi biến thể cho cùng kết quả:**<br>4. Vẫn ở Sign up, **không** có thông báo `Register successfully.`<br>5. Ô Name viền đỏ, dưới ô hiện `Name is required.` · các ô Email, Password, Password Confirmation **không** có dòng lỗi<br><br>⚠️ `b` theo **ASM-BK-AUTH-03** — chưa khảo sát. Nếu `b` đăng ký thành công → ghi nhận, mở bug, **dọn** tài khoản vừa tạo | High | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_033 | REQ-BK-AUTH-69 | AUTH | High | Sign up: bỏ trống Email thì bị chặn | Đang ở Sign up, mọi ô trống | 1. Nhập `Name`: `Auto Auth 1790150411`<br>2. Để trống ô `Email`<br>3. Nhập `Password` và `Password Confirmation`: `Auto@12345`<br>4. Cuộn xuống, bấm `Register`<br>5. Nhìn ô Email và các ô khác | Name: `Auto Auth 1790150411`<br>Email: *(để trống)*<br>Password / Confirmation: `Auto@12345` | 4. Vẫn ở Sign up, không có thông báo nổi<br>5. Ô Email viền đỏ, dưới ô hiện `Email is required.` · Name, Password, Password Confirmation **không** có dòng lỗi | High | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_034 | REQ-BK-AUTH-70 | AUTH | High | Sign up: bỏ trống Password thì bị chặn | Đang ở Sign up, mọi ô trống | 1. Nhập `Name`: `Auto Auth 1790150412`<br>2. Nhập `Email`: `auto_auth_1790150412@auto.test`<br>3. Để trống ô `Password`<br>4. Nhập `Password Confirmation`: `Auto@12345`<br>5. Cuộn xuống, bấm `Register`<br>6. Nhìn ô Password | Name: `Auto Auth 1790150412`<br>Email: `auto_auth_1790150412@auto.test`<br>Password: *(để trống)*<br>Confirmation: `Auto@12345` | 5. Vẫn ở Sign up, **không** có thông báo `Register successfully.`<br>6. Ô Password viền đỏ, dưới ô hiện `Password is required.`<br><br>⚠️ Chưa có evidence cho tổ hợp này — ghi nhận thêm ô Password Confirmation có báo `Password confirmation does not match.` hay không (không chấm FAIL vì điểm này) | High | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_035 | REQ-BK-AUTH-71 | AUTH | High | Sign up: bỏ trống Password Confirmation thì bị chặn | Đang ở Sign up, mọi ô trống | 1. Nhập `Name`: `Auto Auth 1790150413`<br>2. Nhập `Email`: `auto_auth_1790150413@auto.test`<br>3. Nhập `Password`: `Auto@12345`<br>4. Để trống ô `Password Confirmation`<br>5. Cuộn xuống, bấm `Register`<br>6. Nhìn ô Password Confirmation | Name: `Auto Auth 1790150413`<br>Email: `auto_auth_1790150413@auto.test`<br>Password: `Auto@12345`<br>Confirmation: *(để trống)* | 5. Vẫn ở Sign up, **không** có thông báo `Register successfully.`<br>6. Ô Password Confirmation viền đỏ, dưới ô hiện `Password confirmation is required.` · ô Password **không** có dòng lỗi | High | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_036 | REQ-BK-AUTH-72, REQ-BK-AUTH-68, REQ-BK-AUTH-69, REQ-BK-AUTH-70, REQ-BK-AUTH-71 | AUTH | Medium | Sign up: gửi form trống — đúng 4 ô bắt buộc báo lỗi, ô tuỳ chọn không báo | Đang ở Sign up, mọi ô trống | 1. Không nhập gì, cuộn xuống, bấm `Register`<br>2. Đối chiếu từ trên xuống từng ô | — (để trống toàn bộ) | 1. Vẫn ở Sign up, không có thông báo nổi<br>2. Có lỗi đúng 4 ô: `Name` → `Name is required.` · `Email` → `Email is required.` · `Password` → `Password is required.` · `Password Confirmation` → `Password confirmation is required.`<br>Ô `Phone`, `Division`, `Ward`, `Address` **không** viền đỏ, **không** có dòng lỗi | Medium | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_037 | REQ-BK-AUTH-73 | AUTH | High | Name độ dài hợp lệ (1 → 250 ký tự) không báo lỗi | Đang ở Sign up, mọi ô trống. Chạy lại từ bước 1 cho từng biến thể | 1. Nhập ô `Name` theo biến thể (dán chuỗi đã chuẩn bị sẵn)<br>2. **Để trống** ô `Email` (để form không gửi đi)<br>3. Cuộn xuống, bấm `Register`<br>4. Cuộn lên, nhìn vùng dưới ô Name | **Bảng biến thể** — Name:<br>`a` 1 ký tự → `B`<br>`b` 249 ký tự → `B` lặp 249 lần<br>`c` **250** ký tự (đúng biên) → `B` lặp 250 lần | **Mọi biến thể cho cùng kết quả:**<br>3. Vẫn ở Sign up (bị chặn bởi Email trống — đúng dự kiến)<br>4. Ô Name **không** viền đỏ, **không** có dòng lỗi dưới ô · ô Email báo `Email is required.`<br><br>⚠️ `a`, `b` chưa có evidence · `c` đã khảo sát bằng đọc chữ trong ô | High | Yes | UI | @Regression, @Android, @Boundary, @NeedsVerify |
| BK_AUTH_TC_038 | REQ-BK-AUTH-73 | AUTH | High | Name vượt 250 ký tự bị chặn, ô không tự cắt chữ | Đang ở Sign up, mọi ô trống. Chạy lại từ bước 1 cho từng biến thể | 1. Dán vào ô `Name` chuỗi theo biến thể<br>2. **Để trống** ô `Email`<br>3. Cuộn xuống, bấm `Register`<br>4. Cuộn lên, nhìn vùng dưới ô Name<br>5. Bấm vào ô Name, đưa con trỏ về cuối chuỗi | **Bảng biến thể** — Name:<br>`a` **251** ký tự (biên + 1) → `N` lặp 251 lần<br>`b` 300 ký tự → `N` lặp 300 lần | **Mọi biến thể cho cùng kết quả:**<br>3. Vẫn ở Sign up<br>4. Ô Name viền đỏ, dưới ô hiện `Name must be less than 250 characters.`<br>5. Ô giữ **đủ** chuỗi đã dán — app **không** tự cắt bớt<br><br>ℹ️ Câu thông báo nói *less than 250* nhưng 250 ký tự vẫn hợp lệ (`TC_037-c`) — lệch chữ, chờ PO (`AMB-BK-AUTH-14`). TC chấm theo hành vi hiện tại<br><br>🔧 Ghi chú kỹ thuật (cần Appium Inspector): độ dài `text` của ô Name = `251` / `300` | High | Yes | UI | @Regression, @Android, @Boundary, @TechCheck |
| BK_AUTH_TC_039 | REQ-BK-AUTH-01 | AUTH | Medium | Name có dấu tiếng Việt, emoji, ký tự đặc biệt đăng ký được và hiển thị nguyên văn | `S0` · đang ở Sign up. Chạy lại từ bước 1 cho từng biến thể (mỗi biến thể một email riêng) | 1. Nhập ô `Name` theo biến thể<br>2. Nhập `Email` theo biến thể<br>3. Nhập `Password` và `Password Confirmation`: `Auto@12345`<br>4. Bấm `Register`<br>5. Đăng nhập bằng email vừa đăng ký, Password `Auto@12345`<br>6. Đọc thẻ `Welcome …` trên Dashboard | **Bảng biến thể** — Name · Email:<br>`a` Tiếng Việt có dấu → `Nguyễn Thị Ánh Tuyết 1790150402` · `auto_auth_1790150402_vn@auto.test`<br>`b` Emoji → `Auto 😀🎉 1790150403` · `auto_auth_1790150403_emoji@auto.test`<br>`c` Ký tự đặc biệt → `O'Brien & <Co> 1790150404` · `auto_auth_1790150404_special@auto.test` | **Mọi biến thể cho cùng kết quả:**<br>4. Chuyển sang Sign in, thông báo ✓ `Register successfully.`<br>5. Vào Dashboard, thông báo `Login successfully.`<br>6. Thẻ ghi `Welcome ` + Name **đúng nguyên văn** — không mất dấu, không hiện ô vuông / dấu `?`, không hiện mã kiểu `&amp;` hay `&lt;`<br><br>⚠️ Chưa có evidence | Medium | Yes | UI | @Regression, @Android, @Localization, @NeedsVerify |
| BK_AUTH_TC_040 | REQ-BK-AUTH-74 | AUTH | High | Sign up: Email sai định dạng bị app chặn | Đang ở Sign up, mọi ô trống. Chạy lại từ bước 1 cho từng biến thể | 1. Nhập `Name`: `Auto Auth 1790150414`<br>2. Nhập ô `Email` theo biến thể<br>3. Nhập `Password` và `Password Confirmation`: `Auto@12345`<br>4. Cuộn xuống, bấm `Register`<br>5. Nhìn vùng dưới ô Email và cuối màn hình | **Bảng biến thể** — Email:<br>`a` Không có `@` → `khong-phai-email`<br>`b` Thiếu tên miền → `auto_auth@`<br>`c` Thiếu phần trước `@` → `@auto.test`<br>`d` Hai dấu `@` → `auto@auth@auto.test`<br>`e` Tên miền không có phần đuôi → `auto_auth@auto` | **Mọi biến thể cho cùng kết quả:**<br>4. Vẫn ở Sign up, **không** có thông báo nổi nào (yêu cầu chưa tới máy chủ)<br>5. Ô Email viền đỏ, dưới ô hiện `Invalid email address`<br><br>⚠️ `b`→`e` chưa có evidence. Nếu `e` **đăng ký thành công** → ghi lại, **dọn** tài khoản vừa tạo, đưa vào AMB (không chấm FAIL) | High | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_041 | REQ-BK-AUTH-08, REQ-BK-AUTH-81 | AUTH | High | Đăng ký trùng email bị từ chối, lỗi hiện cả dưới ô Email lẫn thông báo nổi | `TK-A` đã đăng ký (email chữ thường) · `S0` · đang ở Sign up, mọi ô trống | 1. Nhập `Name`: `Auto Auth Dup 1790150400`<br>2. Nhập `Email` = email của `TK-A`<br>3. Nhập `Password` và `Password Confirmation`: `Auto@12345`<br>4. Cuộn xuống, bấm `Register`<br>5. Quan sát cuối màn hình<br>6. Nhìn vùng dưới ô Email<br>7. Đợi thông báo nổi tự đóng, nhìn nút `Register` | Email: `auto_auth_1790150400@auto.test` | 4. Vẫn ở Sign up, **không** chuyển sang Sign in<br>5. Thông báo nổi **!** đỏ `Email already exists.`<br>6. Ô Email viền đỏ, dưới ô hiện `Email already exists.`<br>7. Nút `Register` hiện đầy đủ, bấm được<br><br>ℹ️ Trong lúc thông báo nổi còn hiện, nó **che** nút Register (`AMB-BK-AUTH-18`) — không chấm FAIL | High | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_042 | REQ-BK-AUTH-09, REQ-BK-AUTH-81 | AUTH | High | Kiểm trùng email không phân biệt hoa thường | `TK-A` đã đăng ký bằng email **chữ thường** · `S0` · mở Sign up **mới** (vào từ Sign in bằng `Get started`, mọi ô trống) | 1. Nhập `Name`: `Auto Auth Dup 1790150400`<br>2. Nhập `Email` = email của `TK-A` **viết hoa toàn bộ**<br>3. Nhập `Password` và `Password Confirmation`: `Auto@12345`<br>4. Cuộn xuống, bấm `Register`<br>5. Quan sát thông báo nổi và vùng dưới ô Email | Email: `AUTO_AUTH_1790150400@AUTO.TEST` | 4. Vẫn ở Sign up, **không** tạo tài khoản mới<br>5. Thông báo nổi **!** đỏ `Email already exists.` · dưới ô Email cũng hiện `Email already exists.` | High | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_043 | REQ-BK-AUTH-75 | AUTH | High | Mật khẩu xác nhận khác mật khẩu thì bị chặn | Đang ở Sign up, mọi ô trống. Chạy lại từ bước 1 cho từng biến thể | 1. Nhập `Name`: `Auto Auth 1790150415`<br>2. Nhập `Email`: `auto_auth_1790150415@auto.test`<br>3. Nhập `Password` theo biến thể<br>4. Nhập `Password Confirmation` theo biến thể<br>5. Cuộn xuống, bấm `Register`<br>6. Nhìn vùng dưới ô Password Confirmation | **Bảng biến thể** — Password · Confirmation:<br>`a` Khác hẳn → `a` · `b`<br>`b` Chỉ khác hoa thường → `Auto@12345` · `auto@12345`<br>`c` Thừa 1 dấu cách cuối → `Auto@12345` · `Auto@12345·` (`·` = 1 dấu cách) | **Mọi biến thể cho cùng kết quả:**<br>5. Vẫn ở Sign up, **không** có thông báo `Register successfully.`<br>6. Ô Password Confirmation viền đỏ, dưới ô hiện `Password confirmation does not match.` · ô Password **không** báo lỗi<br><br>⚠️ `b`, `c` chưa có evidence | High | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_044 | REQ-BK-AUTH-65 | AUTH | Medium | Nút mắt ở ô Password (Sign up) bật/tắt hiện mật khẩu | Đang ở Sign up, mọi ô trống | 1. Nhập ô `Password`: `Auto@12345`<br>2. Bấm biểu tượng mắt cuối ô Password<br>3. Bấm biểu tượng mắt thêm lần nữa<br>4. Nhìn ô Password Confirmation | Password: `Auto@12345` | 1. Ô hiện 10 dấu chấm · biểu tượng mắt nhắm<br>2. Ô hiện rõ `Auto@12345` · biểu tượng mắt mở<br>3. Ô trở lại dấu chấm · mắt nhắm<br>4. Ô Password Confirmation **không** bị ảnh hưởng (vẫn trống, vẫn biểu tượng mắt nhắm)<br><br>⚠️ Chưa có evidence | Medium | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_045 | REQ-BK-AUTH-65 | AUTH | Medium | Nút mắt ở ô Password Confirmation (Sign up) bật/tắt hiện mật khẩu | Đang ở Sign up, mọi ô trống | 1. Nhập ô `Password Confirmation`: `Auto@12345`<br>2. Bấm biểu tượng mắt cuối ô Password Confirmation<br>3. Bấm biểu tượng mắt thêm lần nữa<br>4. Nhìn ô Password | Password Confirmation: `Auto@12345` | 1. Ô hiện 10 dấu chấm · mắt nhắm<br>2. Ô hiện rõ `Auto@12345` · mắt mở<br>3. Ô trở lại dấu chấm · mắt nhắm<br>4. Ô Password **không** bị ảnh hưởng<br><br>⚠️ Chưa có evidence | Medium | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_046 | REQ-BK-AUTH-72 | AUTH | Low | Phone không bắt buộc và hiện không kiểm định dạng | Đang ở Sign up, mọi ô trống. Chạy lại từ bước 1 cho từng biến thể | 1. Nhập ô `Phone` theo biến thể<br>2. **Để trống** ô `Name` (để form không gửi đi)<br>3. Cuộn xuống, bấm `Register`<br>4. Cuộn lên, nhìn vùng dưới ô Phone | **Bảng biến thể** — Phone:<br>`a` Số di động VN → `0912345678`<br>`b` Có mã quốc gia và dấu cách → `+84 912 345 678`<br>`c` Chữ và gạch nối → `abc-xyz` | **Mọi biến thể cho cùng kết quả:**<br>3. Vẫn ở Sign up (bị chặn bởi Name trống — đúng dự kiến)<br>4. Ô Phone **không** viền đỏ, **không** có dòng lỗi; giá trị hiện đúng như đã nhập<br><br>ℹ️ `c` được chấp nhận là hành vi hiện tại — **không** có quy tắc định dạng (`AMB-BK-AUTH-15` 🟠). PO chốt có quy tắc thì TC này phải sửa · ⚠️ `a`, `b` chưa có evidence | Low | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_047 | REQ-BK-AUTH-76 | AUTH | Medium | Division mở danh sách tỉnh/thành, gõ để lọc và chọn được | Đang ở Sign up, mọi ô trống | 1. Bấm ô `Division`<br>2. Đọc các mục đầu danh sách<br>3. Gõ `Cao`<br>4. Bấm `Cao Bằng`<br>5. Nhìn ô Division | Chuỗi lọc: `Cao` | 1. Danh sách thả xuống mở ra **kèm bàn phím**; ô Division có con trỏ, mũi tên đổi thành ⌃ và có nút `×`<br>2. Danh sách **chứa** `Thành phố Hà Nội`, `Cao Bằng`, `Tuyên Quang` (không chấm tổng số mục)<br>3. Danh sách thu gọn, còn `Cao Bằng`, **không** còn `Thành phố Hà Nội`<br>4. Danh sách đóng, bàn phím ẩn<br>5. Ô Division hiện `Cao Bằng`, có nút `×` bên cạnh mũi tên<br><br>⚠️ Bước 3 chưa có evidence | Medium | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_048 | REQ-BK-AUTH-77, REQ-BK-AUTH-79 | AUTH | High | Ward khoá tới khi chọn Division, Address khoá tới khi chọn Ward | Đang ở Sign up, mọi ô trống | 1. Bấm ô `Ward`<br>2. Bấm ô `Address`<br>3. Bấm ô `Division`, chọn `Thành phố Hà Nội`<br>4. Bấm ô `Address`<br>5. Bấm ô `Ward`, chọn `Phường Hoàn Kiếm`<br>6. Bấm ô `Address`, gõ `12 Tràng Tiền` | Division: `Thành phố Hà Nội`<br>Ward: `Phường Hoàn Kiếm`<br>Address: `12 Tràng Tiền` | 1. **Không** mở danh sách, không hiện bàn phím — ô Ward đang khoá<br>2. **Không** hiện bàn phím, không gõ được — ô Address đang khoá<br>3. Ô Division hiện `Thành phố Hà Nội`<br>4. Ô Address **vẫn khoá** — Ward còn trống<br>5. Ô Ward mở được danh sách, sau khi chọn hiện `Phường Hoàn Kiếm`<br>6. Ô Address nhận chữ, hiện `12 Tràng Tiền`<br><br>🔧 Ghi chú kỹ thuật (cần Appium Inspector): ô `address-ward` mang `enabled=false` ở bước 1, `true` sau bước 3 · ô `address` mang `enabled=false` ở bước 2 và 4, `true` sau bước 5 | High | Yes | UI | @Regression, @Android, @TechCheck |
| BK_AUTH_TC_049 | REQ-BK-AUTH-78 | AUTH | Medium | Ward liệt kê đúng phường của Division đã chọn | Đang ở Sign up, mọi ô trống | 1. Bấm ô `Division`, chọn `Thành phố Hà Nội`<br>2. Bấm ô `Ward`<br>3. Đọc các mục đầu danh sách | Division: `Thành phố Hà Nội` | 2. Danh sách mở ra kèm bàn phím; ô Ward có con trỏ và nút `×`<br>3. Danh sách **chứa** `Phường Hoàn Kiếm`, `Phường Cửa Nam`, `Phường Ba Đình` (không chấm tổng số mục) | Medium | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_050 | REQ-BK-AUTH-80 | AUTH | High | Đổi hoặc xoá Division thì Ward đã chọn bị xoá, Address khoá lại | Đang ở Sign up · đã chọn Division `Thành phố Hà Nội` và Ward `Phường Hoàn Kiếm`. Chạy lại từ đầu cho từng biến thể | 1. Thao tác trên ô `Division` theo biến thể<br>2. Nhìn ô `Ward`<br>3. Bấm ô `Address` | **Bảng biến thể** — thao tác:<br>`a` Đổi → bấm ô Division, chọn `Cao Bằng`<br>`b` Xoá → bấm nút `×` trong ô Division | **Mọi biến thể cho cùng kết quả:**<br>2. Ô Ward **trống**, hiện lại chữ gợi ý `Ward`<br>3. Ô Address **không** nhận chữ — đã khoá lại<br>(`a`: ô Division hiện `Cao Bằng` · `b`: ô Division trống, hiện chữ gợi ý `Division`)<br><br>⚠️ `b` chưa có evidence | High | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_051 | REQ-BK-AUTH-82 | AUTH | Low | Nút Back ở Sign up về Sign in ngay, không hỏi xác nhận | Đang ở Sign up (mở từ `Get started`) | 1. Nhập `Name`: `Auto Auth 1790150416` và `Email`: `auto_auth_1790150416@auto.test`<br>2. Bấm nút **Back** của hệ thống Android<br>3. Quan sát màn hình | Name: `Auto Auth 1790150416`<br>Email: `auto_auth_1790150416@auto.test` | 2. **Không** có hộp thoại xác nhận nào ("bỏ dữ liệu?"…)<br>3. Màn hình Sign in hiện ra ngay, hai ô trống | Low | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_052 | REQ-BK-AUTH-83 | AUTH | Medium | Rời Sign up rồi quay lại thì dữ liệu đã nhập còn nguyên, dòng lỗi cũ không còn | `TK-A` đã đăng ký · đang ở Sign up, mọi ô trống | 1. Nhập Name, Email (email của `TK-A`), Password, Password Confirmation theo Test Data<br>2. Bấm `Register` → có lỗi `Email already exists.` dưới ô Email<br>3. Bấm nút **Back** → về Sign in<br>4. Bấm `Get started`<br>5. Nhìn 4 ô đã nhập và vùng dưới ô Email | Name: `Auto Auth Dup 1790150400`<br>Email: `auto_auth_1790150400@auto.test`<br>Password / Confirmation: `Auto@12345` | 2. Dưới ô Email có `Email already exists.`<br>3. Màn hình Sign in hiện ra<br>4. Màn hình Sign up hiện ra<br>5. Name, Email **vẫn** còn giá trị đã nhập · Password và Password Confirmation **vẫn** có dấu chấm · dưới ô Email **không** còn dòng lỗi, ô không viền đỏ<br><br>ℹ️ Giữ cả mật khẩu là hành vi quan sát — chờ PO xác nhận (`AMB-BK-AUTH-17`) | Medium | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_053 | REQ-BK-AUTH-01 | AUTH | Medium | Bấm Register hai lần liên tiếp chỉ tạo một tài khoản | `S0` · đang ở Sign up, mọi ô trống · email ở Test Data chưa từng đăng ký | 1. Nhập Name, Email, Password, Password Confirmation theo Test Data<br>2. Ẩn bàn phím, cuộn xuống<br>3. Bấm `Register` **hai lần liên tiếp** trong vòng dưới 1 giây<br>4. Đợi 5 giây, quan sát màn hình và thông báo nổi<br>5. Bấm `Get started`, đăng ký lại **đúng** email đó (Name `Auto Auth Twice 1790150405`, Password `Auto@12345`) | Name: `Auto Auth Twice 1790150405`<br>Email: `auto_auth_1790150405_twice@auto.test`<br>Password / Confirmation: `Auto@12345` | 3. App không treo, không đóng đột ngột<br>4. Đang ở Sign in · chỉ **một** thông báo ✓ `Register successfully.` · **không** có thông báo `Email already exists.` nào<br>5. Bị từ chối: `Email already exists.` — xác nhận tài khoản đã được tạo đúng một lần<br><br>⚠️ Chưa có evidence · `RISK-BK-AUTH-08`: lần bấm đầu đôi khi không gửi form<br><br>🔧 Ghi chú kỹ thuật: đếm số tài khoản có email `auto_auth_1790150405_twice@auto.test` bằng `GET /api/user` — phải đúng **1** | Medium | Partial | UI | @Regression, @Android, @RaceCondition, @TechCheck, @NeedsVerify |
| BK_AUTH_TC_054 | REQ-BK-AUTH-01, REQ-BK-AUTH-03, REQ-BK-AUTH-11, REQ-BK-AUTH-62 | AUTH | High | Kịch bản trọn vòng: đăng ký → đăng nhập → đăng xuất → đăng nhập lại | `S0` · email ở Test Data chưa từng đăng ký · đang ở tab Dashboard | 1. Bấm avatar → `Get started` → đăng ký với Test Data<br>2. Ở Sign in, đăng nhập bằng đúng email + mật khẩu vừa đăng ký<br>3. Bấm avatar → `Logout`<br>4. Đăng nhập lại bằng cùng email + mật khẩu<br>5. Đọc thẻ `Welcome …` và nút avatar | Name: `Auto Auth Flow 1790150406`<br>Email: `auto_auth_1790150406_flow@auto.test`<br>Password / Confirmation: `Auto@12345` | 1. Về Sign in, thông báo ✓ `Register successfully.`<br>2. Vào Dashboard, thông báo `Login successfully.`, thẻ `Welcome Auto Auth Flow 1790150406`<br>3. Về Sign in, hai ô trống<br>4. Vào Dashboard, thông báo `Login successfully.`<br>5. Thẻ vẫn ghi `Welcome Auto Auth Flow 1790150406` · nút avatar hiện chữ `A` | Critical | Yes | UI | @Regression, @CriticalPath, @Android, @E2E |

---

## Vòng 3 — Technical

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_055 | REQ-BK-AUTH-01 | AUTH | High | Chuỗi tấn công ở ô Name được lưu như chữ thường, không chạy thành mã | `S0` · đang ở Sign up. Chạy lại từ bước 1 cho từng biến thể (mỗi biến thể một email riêng) | 1. Nhập ô `Name` theo biến thể<br>2. Nhập `Email` theo biến thể<br>3. Nhập `Password` và `Password Confirmation`: `Auto@12345`<br>4. Bấm `Register`<br>5. Đăng nhập bằng email vừa đăng ký, Password `Auto@12345`<br>6. Quan sát Dashboard trong 5 giây, đọc thẻ `Welcome …`<br>7. Bấm nút avatar, đọc dòng đầu menu | **Bảng biến thể** — Name · Email:<br>`a` Tiêm script → `<script>alert(1)</script>` · `auto_auth_1790150407_xss1@auto.test`<br>`b` Thẻ ảnh độc → `<img src=x onerror=alert(1)>` · `auto_auth_1790150408_xss2@auto.test`<br>`c` Tiêm SQL → `Robert'); DROP TABLE users;--` · `auto_auth_1790150409_sqli@auto.test` | **Mọi biến thể cho cùng kết quả:**<br>4. Chuyển sang Sign in, thông báo ✓ `Register successfully.` — hoặc bị chặn có thông báo rõ ràng (ghi lại nguyên văn)<br>6. **Không** bật hộp thoại / cửa sổ cảnh báo nào · app không đóng đột ngột · thẻ hiện `Welcome ` + chuỗi **nguyên văn** như chữ thường, không có ảnh vỡ<br>7. Dòng đầu menu hiện chuỗi nguyên văn (có thể bị cắt `…`), không có hộp thoại<br><br>⚠️ Chưa có evidence. Hộp thoại `1` bật lên ở bất kỳ bước nào → **FAIL mức nghiêm trọng**, mở bug ngay | High | Yes | UI | @Regression, @Security, @Android, @NeedsVerify |

---

## Vòng 4 — Non-functional (đặc thù mobile)

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_056 | REQ-BK-AUTH-84 | AUTH | Medium | Đưa app xuống nền rồi quay lại, dữ liệu form còn nguyên | Đang ở Sign up, mọi ô trống | 1. Nhập `Name` và `Email` theo Test Data<br>2. Bấm nút **Home** của Android (app xuống nền)<br>3. Đợi 5 giây<br>4. Mở lại app từ danh sách ứng dụng gần đây<br>5. Nhìn ô Name và ô Email | Name: `Auto Auth Bg 1790150417`<br>Email: `auto_auth_1790150417_bg@auto.test` | 4. App trở lại đúng màn hình Sign up, **không** về Dashboard, **không** mở lại từ đầu<br>5. Ô Name vẫn là `Auto Auth Bg 1790150417` · ô Email vẫn là `auto_auth_1790150417_bg@auto.test`<br><br>🔧 Ghi chú kỹ thuật: automation dùng `runAppInBackground(Duration.ofSeconds(5))` thay cho bước 2–4 | Medium | Yes | UI | @Regression, @Android, @Lifecycle, @TechCheck |
| BK_AUTH_TC_057 | REQ-BK-AUTH-85 | AUTH | Medium | Xoay ngang màn hình Sign up được, dữ liệu form còn nguyên | Đang ở Sign up, mọi ô trống · **tự động xoay** của máy đang **bật** | 1. Nhập `Name` theo Test Data<br>2. Xoay máy sang **ngang**<br>3. Quan sát bố cục<br>4. Cuộn lên đầu, nhìn ô Name<br>5. Xoay máy về dọc, nhìn lại ô Name | Name: `Auto Auth Land 1790150418` | 2. Giao diện xoay theo, chiếm hết bề ngang màn hình, không bị đóng hay tải lại từ đầu<br>3. Khối form nằm giữa màn hình, logo `ANHTESTER` và `Need help?` vẫn ở đầu · ô nhập không bị cắt chữ gợi ý<br>4. Ô Name vẫn là `Auto Auth Land 1790150418`<br>5. Giao diện về dọc, ô Name vẫn giữ nguyên giá trị<br><br>⚠️ Bước 5 chưa có evidence · ảnh ngang đã cuộn qua ô Name — giá trị Name được xác nhận bằng đọc chữ trong ô<br><br>🔧 Ghi chú kỹ thuật: automation dùng `driver.rotate(ScreenOrientation.LANDSCAPE)` | Medium | Yes | UI | @Regression, @Android, @Orientation, @TechCheck, @NeedsVerify |
| BK_AUTH_TC_058 | REQ-BK-AUTH-86 | AUTH | Medium | Bàn phím mở không che ô đang nhập | Đang ở Sign up, mọi ô trống | 1. Cuộn xuống cuối form<br>2. Chạm ô `Password Confirmation`<br>3. Nhìn vị trí ô đang nhập so với bàn phím<br>4. Gõ `Auto@12345`<br>5. Nhìn nút `Register` | Password Confirmation: `Auto@12345` | 2. Bàn phím mở ra ở nửa dưới màn hình<br>3. Ô `Password Confirmation` nằm **trọn phía trên** bàn phím, nhìn thấy cả nhãn lẫn nội dung<br>4. Thấy từng dấu chấm xuất hiện trong ô khi gõ<br>5. Nút `Register` cũng nằm trên bàn phím, bấm được mà không cần ẩn bàn phím<br><br>🔧 Ghi chú kỹ thuật: `dumpsys input_method` → `keyboardShown=true`; toạ độ đáy ô (`bounds`) nhỏ hơn toạ độ đỉnh vùng bàn phím (quan sát: ô `[195,1113][1011,1281]`, bàn phím từ `y=1723` trên màn `1344×2992`) | Medium | Yes | UI | @Regression, @Android, @Keyboard, @TechCheck |
