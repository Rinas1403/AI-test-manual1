# Test Cases — `AUTH` · Mobile (Android) · Part 01 — Đăng nhập · Đăng xuất · Phiên — 25 TC · độ hạt GỘP

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_AUTH_SUMMARY.md](../../TEST_CASES_AUTH_SUMMARY.md) — cách đọc bộ TC · Dữ liệu dùng chung · Assumptions · Coverage · Evidence · 4 vòng · bộ chạy · Nhật ký |
| **Nguồn requirement** | [requirements_auth_mobile.md](../../../../../requirements/_book-api/auth/mobile/requirements_auth_mobile.md) · REQ dùng chung ở index [REQUIREMENTS_AUTH_SUMMARY.md](../../../../../requirements/_book-api/auth/REQUIREMENTS_AUTH_SUMMARY.md) mục 3 |
| **Nền tảng** | Android — emulator `Pixel_10_Pro_XL_API_37` · Android 17 · màn hình `1344×2992` · **hướng dọc** · locale `en-US` · app `book.anhtester.com` `1.0 (versionCode 1)`. iOS **chưa khảo sát** — không chạy bộ này trên iOS |
| **Dải TC ID** | `BK_AUTH_TC_001` → `BK_AUTH_TC_025` — dải chung toàn module `AUTH` |
| **Part liền kề** | Tiếp theo: [part_02_mobile_dang_ky.md](part_02_mobile_dang_ky.md) (`TC_026` → `TC_057`) |

> Đọc mục **Cách đọc bộ TC này** và **Dữ liệu dùng chung** ở [index](../../TEST_CASES_AUTH_SUMMARY.md) trước khi chạy — quy ước biến thể `a`/`b`/`c`, tài khoản `TK-A`, trạng thái xuất phát `S0`/`S1`, dòng `🔧 Ghi chú kỹ thuật`.

---

## Vòng 1 — Smoke

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_001 | REQ-BK-AUTH-49 | AUTH | High | Màn hình Sign in hiển thị đủ thành phần và đúng trạng thái mặc định | `S0` — chưa đăng nhập, đang ở tab Dashboard | 1. Bấm nút avatar tròn góc phải trên đầu màn hình<br>2. Không chạm vào ô nào, đối chiếu lần lượt từng mục trong Bảng kiểm<br>3. Riêng mục `6`: gõ `Wrong@123` vào ô Password rồi quan sát | Password gõ ở mục `6`: `Wrong@123` | 1. Màn hình Sign in hiện ra<br>2. **Bảng kiểm — mọi mục phải đạt:**<br>`1` Đầu màn hình: logo `ANHTESTER` · dòng phụ `Automation Testing` bên trái · chữ `Need help?` bên phải. **Không** có nút avatar<br>`2` Khối giữa màn hình có tiêu đề `Sign in` · dưới tiêu đề là dòng `Don’t have an account?` kèm chữ xanh lá `Get started`<br>`3` Hai ô nhập theo thứ tự từ trên xuống: `Email address *` rồi `Password *` — dấu `*` màu xanh lá. Cả hai ô **trống**<br>`4` Cuối ô Password có biểu tượng **mắt nhắm**<br>`5` Nút nền đen chữ trắng `Login account` nằm dưới ô Password, rộng hết khối<br>`6` Gõ vào ô Password hiện toàn dấu chấm tròn, **không** hiện chữ<br>`7` Đáy màn hình **không** có thanh điều hướng `Dashboard · User · Book · Promotion · File`<br><br>🔧 Ghi chú kỹ thuật (cần Appium Inspector): ô Password mang `password=true` | High | Yes | UI | @Smoke, @Android, @TechCheck |
| BK_AUTH_TC_002 | REQ-BK-AUTH-50 | AUTH | High | Chưa đăng nhập, bấm nút avatar ở tab nào cũng mở Sign in | `S0` — chưa đăng nhập. Chạy lại từ bước 1 cho **từng** biến thể | 1. Bấm tab ở thanh điều hướng đáy màn hình theo biến thể<br>2. Quan sát nút avatar góc phải trên<br>3. Bấm nút avatar<br>4. Quan sát màn hình | **Bảng biến thể** — tab xuất phát:<br>`a` `Dashboard`<br>`b` `User`<br>`c` `Book`<br>`d` `Promotion`<br>`e` `File` | **Mọi biến thể cho cùng kết quả:**<br>2. Nút avatar hiện biểu tượng **người xám** (chưa có chữ cái)<br>4. Màn hình Sign in hiện ra — thấy tiêu đề `Sign in`, ô `Email address *`, nút `Login account` | High | Yes | UI | @Smoke, @Android |
| BK_AUTH_TC_003 | REQ-BK-AUTH-51 | AUTH | Medium | Chưa đăng nhập, bấm thẻ "Book management sign in" trên Dashboard mở Sign in | `S0` — chưa đăng nhập, đang ở tab Dashboard | 1. Cuộn tới thẻ thứ hai dưới banner<br>2. Đọc nội dung thẻ<br>3. Bấm vào thẻ<br>4. Quan sát màn hình | — (chỉ điều hướng) | 2. Thẻ có tiêu đề `Book management sign in` và dòng mô tả tiếng Việt bắt đầu bằng `Đăng nhập vào trang quản lý sách`<br>4. Màn hình Sign in hiện ra — tiêu đề `Sign in`, hai ô trống | Medium | Yes | UI | @Smoke, @Android |
| BK_AUTH_TC_004 | REQ-BK-AUTH-11, REQ-BK-AUTH-03, REQ-BK-AUTH-59 | AUTH | High | Tài khoản vừa đăng ký đăng nhập thành công, Dashboard chào đúng tên | `TK-A` **vừa đăng ký** (trong cùng lượt chạy) và chưa đăng nhập lần nào. Đang ở màn hình Sign in, hai ô trống | 1. Nhập ô `Email address` theo Test Data<br>2. Nhập ô `Password` theo Test Data<br>3. Bấm `Login account`<br>4. Quan sát thông báo nổi cuối màn hình<br>5. Đọc thẻ thứ hai dưới banner Dashboard<br>6. Nhìn nút avatar góc phải trên | Email: `auto_auth_1790150400@auto.test`<br>Password: `Auto@12345`<br>(Name đã đăng ký của `TK-A`: `Auto Auth 1790150400`) | 3. Chuyển sang tab **Dashboard** — tab `Dashboard` ở đáy màn hình đang được chọn (chữ xanh)<br>4. Thông báo nổi có dấu ✓ xanh, nội dung `Login successfully.`<br>5. Thẻ ghi `Welcome Auto Auth 1790150400` — **không** còn thẻ `Book management sign in`<br>6. Nút avatar hiện chữ cái `A` (chữ cái đầu của Name) | Critical | Yes | UI | @Smoke, @CriticalPath, @Android |
| BK_AUTH_TC_005 | REQ-BK-AUTH-60 | AUTH | High | Đã đăng nhập, menu avatar hiện đúng tài khoản và đủ mục | `S1` — `TK-A` đang đăng nhập, đang ở tab Dashboard | 1. Bấm nút avatar góc phải trên<br>2. Đối chiếu lần lượt từng mục trong Bảng kiểm | — (chỉ quan sát) | 1. Một menu thả xuống mở ra dưới nút avatar<br>2. **Bảng kiểm — mọi mục phải đạt:**<br>`1` Dòng đầu (chữ đậm) là Name `Auto Auth 1790150400`, dòng thứ hai (chữ xám) là email `auto_auth_1790150400@auto.test`. Chuỗi dài hơn bề ngang menu bị cắt bằng dấu `…`, **không** tràn ra ngoài<br>`2` Bốn mục có biểu tượng theo đúng thứ tự: `Home` · `Profile` · `Settings` · `Exit app`<br>`3` Cuối menu, tách bằng đường kẻ mờ, là mục `Logout` chữ **đỏ** | High | Yes | UI | @Smoke, @Android |
| BK_AUTH_TC_006 | REQ-BK-AUTH-62 | AUTH | High | Đăng xuất từ menu avatar đưa về Sign in với hai ô trống | `S1` — `TK-A` đang đăng nhập, đang ở tab `File` | 1. Bấm nút avatar góc phải trên<br>2. Bấm mục `Logout`<br>3. Quan sát chỗ mục `Logout` ngay sau khi bấm<br>4. Đợi màn hình chuyển xong<br>5. Nhìn hai ô nhập | — | 2. Menu vẫn mở, chưa đóng ngay<br>3. Chữ `Logout` được thay bằng **vòng xoay chờ**<br>4. Chuyển sang màn hình Sign in<br>5. Ô `Email address` và ô `Password` đều **trống** | Critical | Yes | UI | @Smoke, @CriticalPath, @Android |

---

## Vòng 2 — Functional

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_007 | REQ-BK-AUTH-52 | AUTH | High | Sign in: bỏ trống Email thì bị chặn ngay trên app | Đang ở Sign in, hai ô trống | 1. Để trống ô `Email address`<br>2. Nhập ô `Password` theo Test Data<br>3. Bấm `Login account`<br>4. Nhìn ô Email và vùng ngay dưới nó<br>5. Nhìn ô Password và cuối màn hình | Email: *(để trống)*<br>Password: `Auto@12345` | 3. Vẫn ở màn hình Sign in, **không** chuyển màn hình<br>4. Viền và nhãn ô Email chuyển **đỏ**; ngay dưới ô hiện dòng đỏ `Email is required.`<br>5. Ô Password **không** có dòng lỗi; cuối màn hình **không** có thông báo nổi nào | High | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_008 | REQ-BK-AUTH-53 | AUTH | High | Sign in: bỏ trống Password thì bị chặn ngay trên app | Đang ở Sign in, hai ô trống | 1. Nhập ô `Email address` theo Test Data<br>2. Để trống ô `Password`<br>3. Bấm `Login account`<br>4. Nhìn ô Password và vùng ngay dưới nó<br>5. Nhìn ô Email và cuối màn hình | Email: `auto_auth_1790150400@auto.test`<br>Password: *(để trống)* | 3. Vẫn ở màn hình Sign in<br>4. Viền và nhãn ô Password chuyển **đỏ**; dưới ô hiện dòng đỏ `Password is required.`<br>5. Ô Email **không** có dòng lỗi; **không** có thông báo nổi | High | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_009 | REQ-BK-AUTH-52, REQ-BK-AUTH-53 | AUTH | Medium | Sign in: gửi form trống thì cả hai ô cùng báo lỗi | Đang ở Sign in, hai ô trống | 1. Không nhập gì, bấm `Login account`<br>2. Đối chiếu hai ô nhập | — (để trống cả hai) | 1. Vẫn ở Sign in, không có thông báo nổi<br>2. Cả hai ô viền đỏ: dưới ô Email là `Email is required.`, dưới ô Password là `Password is required.` · nút `Login account` bị đẩy xuống dưới hai dòng lỗi | Medium | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_010 | REQ-BK-AUTH-54 | AUTH | High | Sign in: Email sai định dạng bị app chặn, không gửi lên máy chủ | Đang ở Sign in, hai ô trống | 1. Nhập ô `Email address` theo biến thể<br>2. Nhập ô `Password`: `Auto@12345`<br>3. Bấm `Login account`<br>4. Nhìn vùng dưới ô Email<br>5. Nhìn cuối màn hình trong 3 giây | **Bảng biến thể** — Email:<br>`a` Không có `@` → `khong-phai-email`<br>`b` Thiếu tên miền → `auto_auth@`<br>`c` Thiếu phần trước `@` → `@auto.test`<br>`d` Hai dấu `@` → `auto@auth@auto.test`<br>`e` Có dấu cách ở giữa → `auto auth@auto.test`<br>`f` Tên miền không có phần đuôi → `auto_auth@auto` | **Mọi biến thể cho cùng kết quả:**<br>3. Vẫn ở màn hình Sign in<br>4. Viền ô Email đỏ, dưới ô hiện `Invalid email address` (không có dấu chấm cuối)<br>5. **Không** xuất hiện thông báo nổi nào (`User not found.` hay `Invalid password.` đều không có — yêu cầu chưa tới máy chủ)<br><br>⚠️ `b`→`f` chưa có evidence — chỉ `a` đã khảo sát. `f` hợp lệ theo một số chuẩn email — nếu app **cho qua** và nhận `User not found.`, ghi lại nguyên văn (không chấm FAIL, đưa vào AMB) | High | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_011 | REQ-BK-AUTH-55 | AUTH | Medium | Sau lần gửi đầu, dòng lỗi dưới ô cập nhật ngay khi gõ, không cần bấm lại | Đang ở Sign in, hai ô trống | 1. Không nhập gì, bấm `Login account`<br>2. Nhập ô `Password`: `Auto@12345` — **không** bấm nút<br>3. Nhập ô `Email address`: `khong-phai-email` — **không** bấm nút<br>4. Xoá ô Email rồi nhập `auto_auth_1790150400@auto.test` — **không** bấm nút | Password: `Auto@12345`<br>Email bước 3: `khong-phai-email`<br>Email bước 4: `auto_auth_1790150400@auto.test` | 1. Hiện `Email is required.` và `Password is required.`<br>2. Dòng `Password is required.` **biến mất** ngay khi gõ, ô Password hết viền đỏ<br>3. Dòng dưới ô Email **đổi** thành `Invalid email address`<br>4. Dòng lỗi dưới ô Email biến mất, ô hết viền đỏ — ⚠️ chưa có evidence cho bước 4 | Medium | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_012 | REQ-BK-AUTH-56 | AUTH | Medium | Nút mắt ở ô Password bật/tắt hiện mật khẩu dạng chữ | Đang ở Sign in, hai ô trống | 1. Nhập ô `Password`: `Wrong@123`<br>2. Nhìn ô Password và biểu tượng cuối ô<br>3. Bấm biểu tượng mắt<br>4. Bấm biểu tượng mắt thêm lần nữa | Password: `Wrong@123` | 2. Ô hiện 9 dấu chấm tròn · biểu tượng **mắt nhắm**<br>3. Ô hiện rõ chữ `Wrong@123` · biểu tượng đổi thành **mắt mở**<br>4. Ô trở lại 9 dấu chấm · biểu tượng trở lại mắt nhắm — ⚠️ chưa có evidence cho bước 4<br><br>🔧 Ghi chú kỹ thuật (cần Appium Inspector): thuộc tính `password` của ô đổi `true` → `false` → `true` | Medium | Yes | UI | @Regression, @Android, @TechCheck, @NeedsVerify |
| BK_AUTH_TC_013 | REQ-BK-AUTH-15 | AUTH | High | Sai mật khẩu bị từ chối, chỉ báo bằng thông báo nổi | `TK-A` đã đăng ký · đang ở Sign in, hai ô trống | 1. Nhập ô `Email address` theo Test Data<br>2. Nhập ô `Password` theo Test Data<br>3. Bấm `Login account`<br>4. Quan sát cuối màn hình<br>5. Nhìn vùng dưới hai ô nhập | Email: `auto_auth_1790150400@auto.test`<br>Password: `Sai@12345` | 3. Vẫn ở màn hình Sign in, **không** vào Dashboard<br>4. Thông báo nổi có biểu tượng **!** đỏ, nội dung `Invalid password.`, có nút `×` bên phải<br>5. **Không** có dòng lỗi dưới ô nào — đặc biệt **không** có câu `Invalid password, please try again.` | High | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_014 | REQ-BK-AUTH-16 | AUTH | High | Email chưa đăng ký bị từ chối | Đang ở Sign in, hai ô trống | 1. Nhập ô `Email address` theo Test Data<br>2. Nhập ô `Password` theo Test Data<br>3. Bấm `Login account`<br>4. Quan sát cuối màn hình<br>5. Nhìn vùng dưới ô Email | Email: `auto_notexist_1790150400@auto.test` (chưa từng đăng ký)<br>Password: `Auto@12345` | 3. Vẫn ở màn hình Sign in<br>4. Thông báo nổi biểu tượng **!** đỏ, nội dung `User not found.`<br>5. **Không** có dòng lỗi dưới ô Email — **không** có câu `Email not found, please register.`<br><br>ℹ️ Câu `User not found.` khác `Invalid password.` của `TC_013` → lộ email nào đã đăng ký (`RISK-BK-AUTH-06`). TC chốt hành vi hiện tại để phát hiện khi đổi | High | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_015 | REQ-BK-AUTH-57 | AUTH | Medium | Đăng nhập thất bại giữ nguyên dữ liệu đã nhập, không bắt nhập lại | `TK-A` đã đăng ký · đang ở Sign in, hai ô trống. Chạy lại từ bước 1 cho từng biến thể | 1. Nhập ô `Email address` theo biến thể<br>2. Nhập ô `Password` theo biến thể<br>3. Bấm `Login account`<br>4. Đợi thông báo nổi xuất hiện<br>5. Nhìn ô Email và ô Password | **Bảng biến thể:**<br>`a` Sai mật khẩu → Email `auto_auth_1790150400@auto.test` · Password `Sai@12345`<br>`b` Email chưa đăng ký → Email `auto_notexist_1790150400@auto.test` · Password `Auto@12345` | **Mọi biến thể cho cùng kết quả:**<br>4. Có thông báo nổi báo lỗi (`a`: `Invalid password.` · `b`: `User not found.`)<br>5. Ô Email **vẫn** chứa đúng email vừa nhập · ô Password **vẫn** có dãy dấu chấm (không bị xoá trắng) | Medium | Yes | UI | @Regression, @Android |
| BK_AUTH_TC_016 | REQ-BK-AUTH-58 | AUTH | Medium | Thông báo nổi tự đóng sau tối đa 10 giây, đóng tay được bằng nút × | `TK-A` đã đăng ký · đang ở Sign in. Chạy lại từ bước 1 cho từng biến thể | 1. Nhập Email `auto_auth_1790150400@auto.test`, Password `Sai@12345`<br>2. Bấm `Login account`<br>3. Ngay khi thông báo `Invalid password.` xuất hiện, bấm giờ và thao tác theo biến thể<br>4. Quan sát cuối màn hình | **Bảng biến thể:**<br>`a` Không chạm gì, chờ<br>`b` Bấm nút `×` bên phải thông báo | 2. Thông báo nổi `Invalid password.` xuất hiện cuối màn hình<br>4. `a`: thông báo **tự biến mất** trong vòng **10 giây** · `b`: thông báo biến mất **ngay** khi bấm `×`<br><br>⚠️ `b` chưa có evidence — ảnh chỉ cho thấy nút `×` có mặt | Medium | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_017 | REQ-BK-AUTH-14 | AUTH | Medium | Email đăng nhập không phân biệt hoa thường | `TK-A` đã đăng ký bằng email **chữ thường** · `S0` · đang ở Sign in. Chạy lại từ bước 1 cho từng biến thể (đăng xuất giữa các lượt) | 1. Nhập ô `Email address` theo biến thể<br>2. Nhập ô `Password`: `Auto@12345`<br>3. Bấm `Login account`<br>4. Quan sát màn hình và thông báo nổi | **Bảng biến thể** — Email:<br>`a` Viết hoa toàn bộ → `AUTO_AUTH_1790150400@AUTO.TEST`<br>`b` Hoa thường lẫn lộn → `Auto_Auth_1790150400@Auto.Test`<br>`c` Thừa 2 dấu cách đầu và cuối → `··auto_auth_1790150400@auto.test··` (`·` = 1 dấu cách) | **Mọi biến thể cho cùng kết quả:**<br>3. Vào tab Dashboard, thẻ ghi `Welcome Auto Auth 1790150400`<br>4. Thông báo nổi ✓ xanh `Login successfully.`<br><br>⚠️ `b` chưa có evidence · `c` theo **ASM-BK-AUTH-02** (khoảng trắng đầu/cuối được bỏ qua) — chưa khảo sát | Medium | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_018 | REQ-BK-AUTH-64 | AUTH | Low | Nút Back hệ thống ở Sign in quay về đúng tab trước đó | `S0` — chưa đăng nhập. Chạy lại từ bước 1 cho từng biến thể | 1. Bấm tab theo biến thể ở thanh đáy màn hình<br>2. Bấm nút avatar → màn hình Sign in hiện ra<br>3. Bấm nút **Back** của hệ thống Android (vuốt từ mép trái, hoặc phím ◁)<br>4. Quan sát màn hình | **Bảng biến thể** — tab xuất phát:<br>`a` `File`<br>`b` `Book` | **Mọi biến thể cho cùng kết quả:**<br>3. Sign in đóng lại, **không** thoát app<br>4. Về lại đúng tab xuất phát (`a`: tiêu đề `File management`, tab `File` đang chọn) · thanh điều hướng đáy màn hình hiện lại<br><br>⚠️ `b` chưa có evidence | Low | Yes | UI | @Regression, @Android, @NeedsVerify |
| BK_AUTH_TC_019 | REQ-BK-AUTH-11 | AUTH | Medium | Bấm Login account hai lần liên tiếp vẫn vào Dashboard một lần, không lỗi | `TK-A` đã đăng ký · `S0` · đang ở Sign in | 1. Nhập Email và Password theo Test Data<br>2. Ẩn bàn phím<br>3. Bấm `Login account` **hai lần liên tiếp** trong vòng dưới 1 giây<br>4. Đợi 5 giây, quan sát màn hình và thông báo nổi | Email: `auto_auth_1790150400@auto.test`<br>Password: `Auto@12345` | 3. App không treo, không đóng đột ngột<br>4. Đang ở tab Dashboard, thẻ `Welcome Auto Auth 1790150400` · chỉ thấy **một** thông báo `Login successfully.` · **không** có thông báo lỗi nào<br><br>⚠️ Chưa có evidence. Lưu ý `RISK-BK-AUTH-08`: đôi khi lần bấm đầu không gửi form — nếu bấm một lần mà **không** có phản hồi, ghi lại và mở bug | Medium | Partial | UI | @Regression, @Android, @RaceCondition, @NeedsVerify |

---

## Vòng 3 — Technical

> App **không** có vai trò (`AMB-BK-01`): quyền trên Android chỉ phân theo **trạng thái** chưa đăng nhập / đã đăng nhập (ma trận ở index requirements mục 6). Lỗ hổng phía máy chủ (token sống sau đăng xuất — F-17 · F-23) thuộc TC API, **không** kiểm ở đây.

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_020 | REQ-BK-AUTH-60, REQ-BK-AUTH-59 | AUTH | High | Đã đăng nhập thì mọi lối vào Sign in / Sign up đều đóng | `S1` — `TK-A` đang đăng nhập, đang ở tab Dashboard | 1. Đọc thẻ thứ hai dưới banner Dashboard<br>2. Bấm vào thẻ đó<br>3. Bấm nút avatar góc phải trên<br>4. Rà toàn bộ menu vừa mở | — (chỉ quan sát) | 1. Thẻ ghi `Welcome Auto Auth 1790150400` — **không** còn thẻ `Book management sign in`<br>2. **Không** mở màn hình Sign in<br>3. Mở menu tài khoản (Name + email), **không** mở Sign in<br>4. Menu **không** có mục nào dẫn tới `Sign in` hay `Sign up` / `Get started` | High | Yes | UI | @Regression, @Security, @Android |
| BK_AUTH_TC_021 | REQ-BK-AUTH-62 | AUTH | High | Sau đăng xuất, nút Back và mở lại app không đưa về phiên cũ | `S1` — `TK-A` đang đăng nhập, đang ở tab Dashboard | 1. Bấm avatar → `Logout`, đợi màn hình Sign in hiện ra<br>2. Bấm nút **Back** của hệ thống<br>3. Nếu vẫn ở trong app: bấm nút avatar góc phải trên<br>4. Buộc dừng app (Cài đặt → Ứng dụng → `book.anhtester.com` → **Buộc dừng**), rồi mở lại app từ màn hình chính<br>5. Nhìn thẻ thứ hai dưới banner Dashboard và nút avatar | — | 2. **Không** quay về Dashboard ở trạng thái đã đăng nhập — **không** thấy thẻ `Welcome Auto Auth 1790150400`<br>3. Bấm avatar mở màn hình **Sign in**, không mở menu tài khoản<br>5. Thẻ ghi `Book management sign in` · nút avatar là biểu tượng **người xám**, không có chữ cái<br><br>⚠️ Chưa có evidence cho bước 2→5 | High | Yes | UI | @Regression, @Security, @Android, @NeedsVerify |
| BK_AUTH_TC_022 | REQ-BK-AUTH-54 | AUTH | High | Chuỗi tấn công ở ô Email bị app chặn như email sai định dạng | Đang ở Sign in, hai ô trống | 1. Nhập ô `Email address` theo biến thể<br>2. Nhập ô `Password`: `Auto@12345`<br>3. Bấm `Login account`<br>4. Quan sát toàn màn hình trong 5 giây | **Bảng biến thể** — Email:<br>`a` Tiêm SQL → `' OR 1=1--`<br>`b` Tiêm SQL có `@` → `admin'--@auto.test`<br>`c` Tiêm script → `<script>alert(1)</script>`<br>`d` Thẻ ảnh độc → `<img src=x onerror=alert(1)>@auto.test` | **Mọi biến thể cho cùng kết quả:**<br>3. Vẫn ở Sign in, **không** vào Dashboard<br>4. Dưới ô Email hiện `Invalid email address` · **không** bật hộp thoại / cửa sổ cảnh báo nào · app không đóng đột ngột · chuỗi hiển thị trong ô **nguyên văn** như đã gõ<br><br>⚠️ Chưa có evidence. Nếu biến thể nào **qua** được kiểm định dạng của app mà có phản hồi máy chủ — ghi lại nguyên văn thông báo và mở bug | High | Yes | UI | @Regression, @Security, @Android, @NeedsVerify |
| BK_AUTH_TC_023 | REQ-BK-AUTH-15 | AUTH | High | Chuỗi tấn công ở ô Password không vượt qua được đăng nhập | `TK-A` đã đăng ký · đang ở Sign in, hai ô trống | 1. Nhập ô `Email address`: `auto_auth_1790150400@auto.test`<br>2. Nhập ô `Password` theo biến thể<br>3. Bấm `Login account`<br>4. Quan sát màn hình và thông báo nổi | **Bảng biến thể** — Password:<br>`a` `' OR '1'='1`<br>`b` `' OR 1=1--`<br>`c` `<script>alert(1)</script>`<br>`d` `{"$ne": null}` | **Mọi biến thể cho cùng kết quả:**<br>3. Vẫn ở Sign in, **không** vào Dashboard<br>4. Thông báo nổi **!** đỏ `Invalid password.` · **không** bật hộp thoại cảnh báo nào · app không đóng đột ngột<br><br>⚠️ Chưa có evidence | High | Yes | UI | @Regression, @Security, @Android, @NeedsVerify |

---

## Vòng 4 — Non-functional (đặc thù mobile)

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BK_AUTH_TC_024 | REQ-BK-AUTH-61 | AUTH | High | Phiên đăng nhập được giữ sau khi tắt hẳn app | `S1` — `TK-A` đang đăng nhập, đang ở tab Dashboard | 1. Mở danh sách ứng dụng gần đây và vuốt bỏ app, **hoặc** Cài đặt → Ứng dụng → `book.anhtester.com` → **Buộc dừng**<br>2. Mở lại app từ màn hình chính<br>3. Nhìn thẻ thứ hai dưới banner Dashboard<br>4. Nhìn nút avatar góc phải trên | — | 2. App mở vào tab **Dashboard**, **không** hiện màn hình Sign in<br>3. Thẻ ghi `Welcome Auto Auth 1790150400`<br>4. Nút avatar hiện chữ cái `A`<br><br>🔧 Ghi chú kỹ thuật: automation dùng `terminateApp` + `activateApp` (Appium) thay cho thao tác tay ở bước 1 | High | Yes | UI | @Regression, @Android, @Lifecycle, @TechCheck |
| BK_AUTH_TC_025 | REQ-BK-AUTH-63 | AUTH | High | Mất mạng khi đăng nhập: không vào hệ thống, dữ liệu đã nhập còn nguyên | `TK-A` đã đăng ký · `S0` · đang ở Sign in, hai ô trống | 1. Kéo thanh thông báo xuống, **tắt Wi-Fi** và **tắt Dữ liệu di động** (hoặc bật Chế độ máy bay)<br>2. Xác nhận thanh trạng thái hiện biểu tượng mất sóng<br>3. Nhập Email và Password theo Test Data<br>4. Bấm `Login account` **2 lần**, cách nhau 1–2 giây<br>5. Đợi ít nhất 5 giây, quan sát màn hình<br>6. Bật lại Wi-Fi, đợi có sóng, bấm `Login account` **1 lần** | Email: `auto_auth_1790150400@auto.test`<br>Password: `Auto@12345` | 2. Biểu tượng sóng có dấu `!` / không có Wi-Fi<br>4. App không treo, không đóng đột ngột<br>5. Vẫn ở màn hình Sign in · ô Email còn nguyên email · ô Password còn dãy dấu chấm · **không** vào Dashboard. (Hiện app **không** hiện thông báo lỗi mạng nào — `AMB-BK-AUTH-16`; TC **không** chấm FAIL vì thiếu thông báo)<br>6. Vào tab Dashboard, thông báo `Login successfully.` xuất hiện ngay<br><br>🔧 Ghi chú kỹ thuật: tắt mạng bằng `adb shell svc wifi disable` + `adb shell svc data disable`; xác nhận bằng `ping` tới `book.anhtester.com` thất bại. Automation dùng `mobile: setConnectivity` (UiAutomator2) | High | Yes | UI | @Regression, @Android, @Network, @TechCheck |
