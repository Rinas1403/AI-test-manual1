# Test Cases — `CUST` · Web · Part 02 — Vòng 2 · Danh sách, tìm kiếm, bộ lọc, xuất, thao tác hàng loạt (22 TC)

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_CUSTOMERS_SUMMARY.md](../../TEST_CASES_CUSTOMERS_SUMMARY.md) — cách đọc · quy ước `P-*`, `KH-TEST`, `D1` · Assumptions · Bảng đối soát |
| **Nguồn requirement** | [requirements_customers_web.md](../../../../requirements/customers/web/requirements_customers_web.md) — mục 3.1 · 3.2 · 3.3 |
| **Nền tảng** | Web — Google Chrome, viewport desktop `1600×750` |
| **Dải TC ID trong part** | `CRM_CUST_TC_017` → `CRM_CUST_TC_038` |
| **Part liền kề** | ← [part_01_web_smoke.md](part_01_web_smoke.md) · → [part_03_web_bieu_mau.md](part_03_web_bieu_mau.md) |

> ⏳ **Bảng nạp dữ liệu bất đồng bộ** (`RISK-CUST-05`): sau mỗi thao tác sắp xếp / tìm / đổi trang, **đợi dòng trạng thái `Showing …` đổi giá trị** rồi mới đọc bảng. Đọc ngay sẽ thấy dữ liệu cũ và báo FAIL giả.
>
> 🚫 **`Mass Delete` KHÔNG BAO GIỜ chạy trên môi trường dùng chung** (`RISK-CUST-02`, `RISK-CUST-08`) — kể cả khi chỉ tích chọn khách hàng test của mình. `CRM_CUST_TC_037` gắn `@PersonalOnly`.

---

## Nhóm E — Sắp xếp, số dòng, phân trang

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_017 | REQ-CUST-04 | CUST | Medium | Sắp xếp tăng/giảm theo cột Company, #, Date Created | `P-ADMIN`, đang ở `/admin/clients`, trang vừa nạp (mặc định sắp theo `Company` tăng dần). Chạy lại từ bước 1 cho **từng** biến thể | 1. Bấm tiêu đề cột của biến thể **một lần**<br>2. Đợi dòng trạng thái nạp lại, đọc 5 dòng đầu của cột đó<br>3. Bấm tiêu đề cột đó **lần nữa**<br>4. Đợi nạp lại, đọc 5 dòng đầu | **Bảng biến thể (cột được bấm):**<br>`a` `Company` — vì đang tăng dần sẵn, lần bấm thứ nhất chuyển sang **giảm** dần<br>`b` `#`<br>`c` `Date Created` | **Mọi biến thể cho cùng kiểu kết quả:**<br>2. Cột vừa bấm hiện biểu tượng mũi tên chỉ chiều sắp; 5 dòng đầu xếp đúng một chiều (số `#` / ngày giờ tăng hoặc giảm đều; tên công ty theo bảng chữ cái)<br>4. Mũi tên **đảo chiều**, thứ tự 5 dòng đầu đảo theo · chỉ **một** cột mang mũi tên đang sắp tại mỗi thời điểm<br><br>`a`: lần bấm thứ nhất ra giảm dần, lần hai về tăng dần · `b`,`c`: lần thứ nhất tăng, lần hai giảm<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): mỗi lần bấm phát sinh `POST /admin/clients/table` với `order[0][column]` = chỉ số cột, `order[0][dir]` = `asc`/`desc` | Medium | Yes | UI | @Regression, @DataTable, @TechCheck, @Customers |
| CRM_CUST_TC_018 | REQ-CUST-04 | CUST | Medium | Sắp xếp tăng/giảm theo các cột Primary Contact, Primary Email, Phone, Active, Groups | Như `CRM_CUST_TC_017` | Như `CRM_CUST_TC_017` | **Bảng biến thể (cột được bấm):**<br>`a` `Primary Contact`<br>`b` `Primary Email`<br>`c` `Phone`<br>`d` `Active`<br>`e` `Groups` | **Mọi biến thể:** như `CRM_CUST_TC_017` bước 2 và 4 — mũi tên hiện ở cột vừa bấm, đảo chiều ở lần bấm thứ hai, dữ liệu 5 dòng đầu xếp đúng chiều. Với `d`: các dòng cùng trạng thái công tắc đứng liền nhau | Low | Yes | UI | @Regression, @DataTable, @Customers |
| CRM_CUST_TC_019 | REQ-CUST-05 | CUST | Medium | Đổi số dòng mỗi trang | `P-ADMIN`, đang ở `/admin/clients`, không có từ khoá tìm kiếm. Chạy lại cho **từng** biến thể | 1. Mở ô chọn số dòng (đang hiện `25`), đọc danh sách lựa chọn<br>2. Chọn giá trị của biến thể<br>3. Đợi dòng trạng thái nạp lại<br>4. Đếm số dòng trong bảng, đọc dòng trạng thái | **Bảng biến thể:**<br>`a` `10`<br>`b` `25`<br>`c` `50`<br>`d` `100`<br>`e` `All` | 1. Đúng 5 lựa chọn theo thứ tự `10` · `25` · `50` · `100` · `All`<br>**Mọi biến thể `a`→`d`:** bảng có đúng số dòng đã chọn · dòng trạng thái `Showing 1 to {số đã chọn} of {tổng} entries`<br>**Biến thể `e`:** bảng hiện **toàn bộ** bản ghi · dòng trạng thái `Showing 1 to {tổng} of {tổng} entries` · thanh phân trang chỉ còn trang `1` · trang **không** treo (cuộn được, bấm được) — xem thêm `CRM_CUST_TC_128` về hiệu năng | Medium | Yes | UI | @Regression, @DataTable, @Boundary, @Customers |
| CRM_CUST_TC_020 | REQ-CUST-06, REQ-CUST-07 | CUST | Medium | Chuyển trang bằng Next/Previous, số trang và ô nhảy trang — bảng nạp tại chỗ | `P-ADMIN`, đang ở `/admin/clients`, 25 dòng/trang, đang ở trang 1 | 1. Ghi lại số `#` dòng đầu trang 1<br>2. Bấm `Next`<br>3. Bấm số trang `3`<br>4. Bấm `Previous`<br>5. Ở ô chọn trang góc dưới phải, chọn trang cuối cùng<br>6. Quan sát toàn bộ quá trình: thanh cuộn, khối `Customers Summary`, thanh địa chỉ | — (chỉ điều hướng) | 2. Dòng trạng thái `Showing 26 to 50 of {tổng} entries`, ô `2` tô xanh, `Previous` bấm được<br>3. `Showing 51 to 75 …`, ô `3` tô xanh<br>4. Về `Showing 26 to 50 …`<br>5. Dòng trạng thái kết thúc bằng `{tổng} of {tổng} entries`, `Next` mờ không bấm được<br>6. Chỉ vùng bảng nạp lại — **không** có hiệu ứng tải lại cả trang, URL **không** đổi, khối Summary giữ nguyên<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): mỗi lần chuyển trang phát sinh `POST /admin/clients/table` trả `200`, body có `start` = 25 × (trang − 1), `length=25`, `csrf_token_name` | Medium | Yes | UI | @Regression, @DataTable, @TechCheck, @Customers |

---

## Nhóm F — Tìm kiếm nhanh và nạp lại bảng

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_021 | REQ-CUST-11, REQ-CUST-12 | CUST | High | Tìm kiếm khớp theo tên đầy đủ, một phần, khác hoa thường và theo số điện thoại | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc021_202609191040`, Phone `0987654021`. Xoá ô tìm kiếm trước **mỗi** biến thể | 1. Gõ từ khoá của biến thể vào ô tìm kiếm của bảng<br>2. Đợi dòng trạng thái nạp lại<br>3. Đọc các dòng kết quả và dòng trạng thái<br>4. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` Tên đầy đủ → `auto_cust_tc021_202609191040`<br>`b` Một phần giữa tên → `tc021_2026`<br>`c` Khác hoa thường → `AUTO_CUST_TC021_202609191040`<br>`d` Số điện thoại → `0987654021` | **Mọi biến thể cho cùng kết quả:**<br>3. Kết quả **có** dòng `auto_cust_tc021_202609191040` · dòng trạng thái dạng `Showing 1 to {n} of {n} entries (filtered from {tổng} total entries)` — `{n}` ≥ 1, `{tổng}` là tổng số khách hàng<br><br>`a`,`c`,`d` kỳ vọng thêm: `{n}` = **1** (từ khoá đủ đặc thù) | High | Yes | UI | @Regression, @DataTable, @Customers |
| CRM_CUST_TC_022 | REQ-CUST-13 | CUST | Medium | Từ khoá không khớp bản ghi nào — kể cả chuỗi tấn công — thì bảng báo rỗng | `P-ADMIN`, đang ở `/admin/clients`. Xoá ô tìm kiếm trước **mỗi** biến thể | 1. Gõ từ khoá của biến thể vào ô tìm kiếm của bảng<br>2. Đợi dòng trạng thái nạp lại<br>3. Quan sát bảng, dòng trạng thái và có hộp thoại bật lên nào không | **Bảng biến thể:**<br>`a` Chuỗi không tồn tại → `zzz_khong_ton_tai_20260919`<br>`b` Chuỗi tiêm mã → `<script>alert('tc022')</script>`<br>`c` Chuỗi tiêm SQL → `' OR 1=1--` | **Mọi biến thể cho cùng kết quả:**<br>3. Bảng có đúng 1 dòng chữ `No matching records found` · dòng trạng thái `Showing 0 to 0 of 0 entries (filtered from {tổng} total entries)` · **không** hộp thoại nào bật lên · trang không báo lỗi<br><br>`c` chứng minh thêm: chuỗi SQL **không** làm bảng trả về toàn bộ khách hàng | Medium | Yes | UI | @Regression, @DataTable, @Security, @Customers |
| CRM_CUST_TC_023 | REQ-CUST-11 | CUST | Medium | Tìm kiếm tên tiếng Việt có dấu bằng từ khoá có dấu và không dấu | `P-ADMIN`. Có sẵn `KH-TEST` Company `Công ty Ánh Dương TC023 202609191045`. Xoá ô tìm kiếm trước **mỗi** biến thể | 1. Gõ từ khoá của biến thể<br>2. Đợi nạp lại, đọc kết quả<br>3. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` Có dấu → `Ánh Dương TC023`<br>`b` Không dấu → `Anh Duong TC023` | `a`: kết quả có đúng dòng `Công ty Ánh Dương TC023 202609191045`<br>`b`: ⚠️ chưa có evidence — **ghi nhận** có tìm ra hay không. Tìm ra = hệ thống bỏ dấu khi so khớp; không ra = so khớp nguyên dấu. Cả hai **không** kết luận FAIL, ghi vào báo cáo để chốt `AMB` | Low | Partial | UI | @Regression, @DataTable, @NeedsVerify, @Customers |
| CRM_CUST_TC_024 | REQ-CUST-10 | CUST | Low | Nút nạp lại làm mới bảng tại chỗ và giữ nguyên từ khoá tìm kiếm | `P-ADMIN`, đang ở `/admin/clients` | 1. Gõ `auto` vào ô tìm kiếm, đợi nạp lại, ghi lại dòng trạng thái<br>2. Bấm nút biểu tượng mũi tên xoay cạnh `Bulk Actions`<br>3. Quan sát bảng, ô tìm kiếm, thanh địa chỉ | Từ khoá: `auto` | 2. Bảng nạp lại tại chỗ, **không** tải lại cả trang<br>3. Ô tìm kiếm vẫn còn `auto` · dòng trạng thái vẫn ở dạng `(filtered from …)` · URL không đổi | Low | Yes | UI | @Regression, @DataTable, @Customers |

---

## Nhóm G — Bộ lọc tuỳ biến và bộ lọc đã lưu

> ⚠️ **Bộ lọc lưu sẵn là dữ liệu dùng chung giữa mọi tester** (`STORY-CUST-02`). TC nào lưu bộ lọc phải **xoá** bộ lọc đó ở bước cuối.

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_025 | REQ-CUST-17 | CUST | Low | Dropdown bộ lọc: lệnh tạo bộ lọc, bộ lọc đã lưu của tài khoản, lệnh xoá/sửa chỉ hiện khi đang áp bộ lọc | Đang ở `/admin/clients`, **chưa** áp bộ lọc nào. Chạy cho **từng** biến thể tài khoản | 1. Rê chuột lên nút biểu tượng phễu góc phải trên, đọc chú giải<br>2. Bấm nút phễu<br>3. Đọc danh sách xổ xuống từ trên xuống | **Bảng biến thể:**<br>`a` `P-ADMIN` — tài khoản **có** bộ lọc đã lưu<br>`b` `P-PM` — tài khoản **chưa** lưu bộ lọc nào | 1. Chú giải `Filter by`<br>3. Mục đầu tiên chữ xanh `New Filter`, dưới có đường kẻ ngăn · **không** có `Clear Filter` và `Edit` (hai lệnh này chỉ hiện khi đang áp bộ lọc)<br>`a` tiếp theo là các bộ lọc đã lưu của tài khoản, VD ảnh `cust_list_saved_filters_dropdown_fullpage.png` có 9 mục `filter by phone` … `zip code` (số lượng có thể đổi)<br>`b` hiện nguyên văn `No saved filters, get started by creating a new filter.` (ảnh `cust_list_filter_dropdown_verify_20260919.png`)<br><br>⚠️ Việc `Clear Filter` / `Edit` **hiện ra khi đã áp bộ lọc** chưa kiểm chứng — xem `CRM_CUST_TC_027` bước 6 | Low | Yes | UI | @Regression, @Filter, @NeedsVerify, @Customers |
| CRM_CUST_TC_026 | REQ-CUST-14, REQ-CUST-16 | CUST | Medium | Hộp thoại Create Filter mở/đóng được và có đủ 15 tiêu chí lọc | `P-ADMIN`, đang ở `/admin/clients` | 1. Bấm nút phễu → `New Filter`<br>2. Nhìn hộp thoại<br>3. Bấm ô `Add Rule`, đọc danh sách tiêu chí, bấm ra ngoài danh sách để đóng<br>4. Bấm `X` góc phải hộp thoại<br>5. Mở lại hộp thoại, bấm phím `Esc` | — (không áp bộ lọc) | 2. Hộp thoại tiêu đề `Create Filter`, có ô chọn chữ mờ `Add Rule` · chân hộp thoại có nhãn `Save Filter` kèm **công tắc gạt đang tắt** và nút `Apply` nền xanh<br>3. Đúng **15** tiêu chí: `Phone` · `Active` · `Invoices` · `Estimates` · `Proposals` · `Projects` · `Contracts Types` · `City` · `Zip Code` · `State` · `Country` · `Responsible admin` · `Groups` · `Customers assigned to me` · `Requires Registration Confirmation`<br>4. Hộp thoại đóng, bảng không đổi<br>5. Hộp thoại đóng<br><br>ℹ️ `Save Filter` hiển thị dạng **công tắc gạt** (bản chất là ô tích — `REQ-CUST-16`) | Medium | Yes | UI | @Regression, @Filter, @Modal, @Customers |
| CRM_CUST_TC_027 | REQ-CUST-14 | CUST | Medium | Áp bộ lọc một điều kiện theo City | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc027_202609191050`, City `Thanh Pho TC027` | 1. Bấm phễu → `New Filter`<br>2. Ở `Add Rule` chọn `City`<br>3. Nhập giá trị `Thanh Pho TC027` cho điều kiện<br>4. Để `Save Filter` **tắt**, bấm `Apply`<br>5. Đợi nạp lại, đọc bảng<br>6. Mở lại dropdown phễu, đọc các lệnh, bấm `Clear Filter`<br>7. Dọn dữ liệu theo `D1` | City: `Thanh Pho TC027` | 2. Xuất hiện dòng điều kiện `City` · `equal ▾` · ô nhập giá trị · nút thùng rác; phía trên hiện cặp nút `and` / `or` (đang chọn `or`)<br>5. Bảng chỉ còn các dòng có City khớp, **có** `auto_cust_tc027_202609191050` · dòng trạng thái dạng `(filtered from …)`<br>6. Dropdown nay có thêm `Clear Filter` · `Edit` (⚠️ chưa kiểm chứng) · sau khi bấm `Clear Filter` bảng trở về đủ bản ghi · danh sách bộ lọc đã lưu **không** thêm mục mới (vì `Save Filter` tắt) | Medium | Partial | UI | @Regression, @Filter, @NeedsVerify, @Customers |
| CRM_CUST_TC_028 | REQ-CUST-14, REQ-CUST-15 | CUST | Medium | Áp bộ lọc hai điều kiện với kiểu khớp "tất cả" và "bất kỳ" | `P-ADMIN`. Có sẵn 2 `KH-TEST`: `auto_cust_tc028a_202609191055` (City `Thanh Pho TC028`, Zip `280001`) · `auto_cust_tc028b_202609191055` (City `Thanh Pho TC028`, Zip `280002`). Chạy lại cho **từng** biến thể | 1. `New Filter` → thêm điều kiện `City` = `Thanh Pho TC028`<br>2. Bấm `Add Rule` thêm điều kiện `Zip Code` = `280001`<br>3. Chọn kiểu khớp theo biến thể<br>4. `Apply`, đợi nạp lại, đọc bảng<br>5. Dọn dữ liệu theo `D1` cho cả 2 khách hàng (sau biến thể cuối) | **Bảng biến thể (kiểu khớp):**<br>`a` Chọn `and` — khớp **tất cả** điều kiện<br>`b` Để nguyên `or` (mặc định) — khớp **bất kỳ** điều kiện | 2. Phía trên danh sách điều kiện có cặp nút `and` / `or`, **đang chọn `or`**; dòng điều kiện thứ 2 có nhãn `or` đứng đầu (ảnh `cust_list_create_filter_two_rules_verify_20260919.png`)<br>4. `a`: chỉ còn `auto_cust_tc028a_…` (không có `tc028b`)<br>4. `b`: có **cả** `auto_cust_tc028a_…` và `auto_cust_tc028b_…`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): cặp radio `match_clients` với `match_type_and` / `match_type_or` | Medium | Partial | UI | @Regression, @Filter, @DecisionTable, @NeedsVerify, @TechCheck, @Customers |
| CRM_CUST_TC_029 | REQ-CUST-16 | CUST | Low | Lưu bộ lọc để dùng lại rồi xoá bộ lọc đó | `P-ADMIN`, đang ở `/admin/clients` | 1. `New Filter` → điều kiện `City` = `Thanh Pho TC029`<br>2. Gạt công tắc `Save Filter` sang bật<br>3. Nhập tên bộ lọc (nếu có ô tên) `auto_filter_tc029_202609191100`<br>4. Bấm `Apply`<br>5. Mở dropdown phễu, đọc danh sách<br>6. Xoá bộ lọc `auto_filter_tc029_…` (qua lệnh `Edit` hoặc lối xoá tương đương)<br>7. Mở lại dropdown phễu | Điều kiện: City `Thanh Pho TC029`<br>Tên bộ lọc: `auto_filter_tc029_202609191100` | 2. Công tắc chuyển sang bật (nền xanh) — ⚠️ chưa có evidence về ô nhập tên xuất hiện sau khi bật<br>5. Dropdown có thêm mục `auto_filter_tc029_202609191100`<br>7. Mục đó **không** còn trong danh sách | Low | Partial | UI | @Regression, @Filter, @NeedsVerify, @Customers |

---

## Nhóm H — Xuất dữ liệu, chọn nhiều dòng, thao tác hàng loạt

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_030 | REQ-CUST-18 | CUST | Medium | Menu Export có đủ 4 lựa chọn đúng thứ tự | `P-ADMIN`, đang ở `/admin/clients` | 1. Bấm nút `Export`<br>2. Đọc menu xổ xuống<br>3. Bấm ra ngoài menu | — (chỉ quan sát) | 2. Menu nhỏ ngay dưới nút `Export`, đúng 4 mục từ trên xuống: `Excel` · `CSV` · `PDF` · `Print`<br>3. Menu đóng, không có tệp nào được tải | Medium | Yes | UI | @Regression, @Export, @Customers |
| CRM_CUST_TC_031 | REQ-CUST-18, REQ-CUST-19 | CUST | Medium | Xuất Excel/CSV/PDF ra tệp đúng dữ liệu đang lọc, không có cột ô chọn | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc031_202609191105`, Phone `0909031031`. Chạy lại cho **từng** biến thể | 1. Tìm `auto_cust_tc031_202609191105` trong ô tìm kiếm của bảng, đợi còn 1 dòng<br>2. Bấm `Export` → chọn định dạng của biến thể<br>3. Mở tệp vừa tải về<br>4. Đọc hàng tiêu đề và các dòng dữ liệu trong tệp<br>5. Dọn dữ liệu theo `D1` (sau biến thể cuối) và xoá tệp đã tải | **Bảng biến thể:**<br>`a` `Excel` → tệp `.xlsx`<br>`b` `CSV` → tệp `.csv`<br>`c` `PDF` → tệp `.pdf` | **Mọi biến thể cho cùng kết quả:**<br>2. Trình duyệt tải về một tệp đúng định dạng của biến thể<br>4. Tiêu đề gồm 8 cột `#` · `Company` · `Primary Contact` · `Primary Email` · `Phone` · `Active` · `Groups` · `Date Created` — **không** có cột ô chọn · dữ liệu chỉ có **1** dòng `auto_cust_tc031_202609191105`, Phone `0909031031` (xuất đúng tập đang lọc) | Medium | Partial | UI | @Regression, @Export, @Customers |
| CRM_CUST_TC_032 | REQ-CUST-18 | CUST | Low | Export → Print mở bản in của danh sách đang xem | `P-ADMIN`, đang ở `/admin/clients`, tìm `auto` để thu hẹp danh sách | 1. Bấm `Export` → `Print`<br>2. Quan sát cửa sổ/tab mới hoặc hộp thoại in<br>3. Huỷ in, quay lại tab danh sách | Từ khoá: `auto` | 2. Hiện bản xem trước in chứa bảng khách hàng đang lọc, **không** có cột ô chọn<br>3. Tab danh sách vẫn nguyên trạng thái lọc | Low | No | N/A | @Regression, @Export, @Customers |
| CRM_CUST_TC_033 | REQ-CUST-20 | CUST | Medium | Chọn từng dòng, chọn tất cả dòng đang hiển thị và bỏ chọn | `P-ADMIN`, đang ở `/admin/clients`, 25 dòng/trang | 1. Tích ô chọn của dòng 1 và dòng 3<br>2. Tích ô chọn ở hàng tiêu đề<br>3. Bỏ tích ô chọn ở hàng tiêu đề<br>4. Chuyển sang trang 2 | — (không thực hiện thao tác hàng loạt) | 1. Chỉ dòng 1 và 3 có dấu tích<br>2. **Cả 25 dòng** trang hiện tại có dấu tích<br>3. Cả 25 dòng bỏ tích<br>4. Các dòng trang 2 **không** có dấu tích nào | Medium | Yes | UI | @Regression, @BulkAction, @Customers |
| CRM_CUST_TC_034 | REQ-CUST-21, REQ-CUST-22 | CUST | High | Hộp thoại Bulk Actions đủ thành phần, cảnh báo nguyên văn và đóng được không thay đổi dữ liệu | `P-ADMIN`, đang ở `/admin/clients`. Chạy bước 4 cho **từng** biến thể cách đóng | 1. Tích ô chọn 1 dòng bất kỳ<br>2. Bấm `Bulk Actions`<br>3. Đọc hộp thoại theo Bảng kiểm<br>4. Đóng hộp thoại theo biến thể — **tuyệt đối không** bấm `Confirm`<br>5. Nhìn lại dòng đã chọn | **Bảng biến thể cách đóng:**<br>`a` Nút `X` góc phải<br>`b` Nút `Close`<br>`c` Phím `Esc` | **Bảng kiểm (bước 3):**<br>`1` Tiêu đề `Bulk Actions`, nút `X` góc phải<br>`2` Ô tích `Mass Delete` đang **rỗng**<br>`3` Nhãn `Groups`, ô chọn hiện chữ mờ `Nothing selected`<br>`4` Dưới ô Groups có dòng **chữ đỏ** nguyên văn: `If you do not select any group all groups assigned to the selected customers will be removed.` (ASM-01)<br>`5` Chân hộp thoại: `Close` (nền trắng) · `Confirm` (nền xanh)<br><br>**Mọi biến thể bước 4–5:** hộp thoại đóng · dòng vẫn còn, cột `Groups` **không** đổi | High | Yes | UI | @Regression, @BulkAction, @Modal, @Customers |
| CRM_CUST_TC_035 | REQ-CUST-22 | CUST | High | Gán một nhóm hàng loạt cho các khách hàng đã chọn | `P-ADMIN`. Có sẵn 2 `KH-TEST` **chưa thuộc nhóm nào**: `auto_cust_tc035a_202609191110` · `auto_cust_tc035b_202609191110` | 1. Tìm `auto_cust_tc035`, đợi còn đúng 2 dòng<br>2. Tích ô chọn ở hàng tiêu đề (chọn cả 2)<br>3. `Bulk Actions` → ô `Groups` gõ tìm `Platinum`, chọn **một** mục `Platinum`<br>4. Để `Mass Delete` **rỗng**, bấm `Confirm`<br>5. Đợi bảng nạp lại, đọc cột `Groups` của 2 dòng<br>6. Dọn dữ liệu theo `D1` cho cả 2 khách hàng | Nhóm: `Platinum` (nhóm có sẵn trong danh sách, không tạo mới) | 3. Ô tìm kiếm trong dropdown lọc danh sách theo chữ `Platinum`<br>4. Hộp thoại đóng, không báo lỗi<br>5. Cả 2 dòng hiện `Platinum` ở cột `Groups`<br><br>⚠️ Chưa thực thi lúc khảo sát (chỉ có evidence giao diện) — ghi lại thông báo xuất hiện sau `Confirm` nếu có | High | Yes | UI | @Regression, @BulkAction, @NeedsVerify, @Customers |
| CRM_CUST_TC_036 | REQ-CUST-22 | CUST | High | Xác nhận Bulk Actions mà không chọn nhóm nào thì gỡ hết nhóm của khách hàng đã chọn | `P-ADMIN`. Có sẵn 2 `KH-TEST` **đã thuộc nhóm** `Platinum`: `auto_cust_tc036a_202609191115` · `auto_cust_tc036b_202609191115` (tạo bằng biểu mẫu, chọn Groups `Platinum`) | 1. Tìm `auto_cust_tc036`, chọn cả 2 dòng<br>2. `Bulk Actions` → để `Groups` = `Nothing selected`, `Mass Delete` **rỗng**<br>3. Bấm `Confirm`<br>4. Đợi nạp lại, đọc cột `Groups`<br>5. Dọn dữ liệu theo `D1` | Nhóm ban đầu: `Platinum` | 4. Cột `Groups` của cả 2 dòng trở về **trống** — đúng như cảnh báo chữ đỏ của hộp thoại (ASM-01)<br><br>⚠️ Hành vi này chỉ được mô tả bằng dòng cảnh báo trên màn hình, chưa thực thi → `@NeedsVerify`. **Chỉ** chạy với khách hàng test — chạy nhầm trên dữ liệu thật là xoá mất phân nhóm của người khác | High | Yes | UI | @Regression, @BulkAction, @NeedsVerify, @Customers |
| CRM_CUST_TC_037 | REQ-CUST-22 | CUST | High | Xoá hàng loạt khách hàng bằng Mass Delete | 🚫 **CHỈ chạy trên môi trường riêng**, không bao giờ trên `crm.anhtester.com` (`RISK-CUST-02`, `RISK-CUST-08`). `P-ADMIN`, có 2 khách hàng test `auto_cust_tc037a_…`, `auto_cust_tc037b_…` | 1. Tìm `auto_cust_tc037`, chọn cả 2 dòng<br>2. `Bulk Actions` → tích `Mass Delete`<br>3. Quan sát ô `Groups` sau khi tích<br>4. Bấm `Confirm`<br>5. Tìm lại `auto_cust_tc037` | Khách hàng: `auto_cust_tc037a_202609191120` · `auto_cust_tc037b_202609191120` | 3. ⚠️ Chưa có evidence — ghi nhận ô `Groups` có bị khoá/ẩn khi đã tích `Mass Delete` không<br>4. Có bước xác nhận trước khi xoá (ghi lại nguyên văn nếu có) → sau xác nhận hiện thông báo thành công<br>5. `No matching records found` · `Total Customers` giảm đúng 2 | High | Partial | UI | @Regression, @BulkAction, @Destructive, @PersonalOnly, @NeedsVerify, @Customers |
| CRM_CUST_TC_038 | REQ-CUST-21 | CUST | Medium | Mở Bulk Actions và bấm Confirm khi chưa chọn dòng nào | `P-ADMIN`, đang ở `/admin/clients`, **không** dòng nào được tích | 1. Bấm `Bulk Actions`<br>2. Để nguyên `Mass Delete` rỗng, `Groups` trống<br>3. Bấm `Confirm`<br>4. Quan sát thông báo và bảng | — | 4. ⚠️ Chưa có evidence. Kỳ vọng tối thiểu: **không** khách hàng nào bị đổi nhóm hay bị xoá · không hiện trang lỗi. Ghi lại hệ thống có nhắc "chưa chọn bản ghi" hay im lặng | Medium | Partial | UI | @Regression, @BulkAction, @ErrorGuessing, @NeedsVerify, @Customers |
