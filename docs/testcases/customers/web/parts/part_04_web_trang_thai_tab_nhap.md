# Test Cases — `CUST` · Web · Part 04 — Vòng 2 · Trạng thái & xoá, tab phụ trợ, Nhập CSV, đoán lỗi (39 TC)

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_CUSTOMERS_SUMMARY.md](../../TEST_CASES_CUSTOMERS_SUMMARY.md) — cách đọc · quy ước `P-*`, `KH-TEST`, `D1`, `CSV-*` · Assumptions · Bảng đối soát |
| **Nguồn requirement** | [requirements_customers_web.md](../../../../requirements/customers/web/requirements_customers_web.md) — mục 3.8 · 3.9 · 3.10 · 4.3 · 4.4 · 8 · Ma trận trạng thái ở [index requirements](../../../../requirements/customers/REQUIREMENTS_CUSTOMERS_SUMMARY.md) mục 7 |
| **Nền tảng** | Web — Google Chrome, viewport desktop `1600×750` |
| **Dải TC ID trong part** | `CRM_CUST_TC_074` → `CRM_CUST_TC_112` |
| **Part liền kề** | ← [part_03_web_bieu_mau.md](part_03_web_bieu_mau.md) · → [part_05_web_ky_thuat_phi_chuc_nang.md](part_05_web_ky_thuat_phi_chuc_nang.md) |

> 🗑️ **Xoá chỉ qua nút `Delete` + hộp thoại xác nhận, chỉ với khách hàng do chính TC tạo** (`RISK-CUST-06`). **Không bao giờ** dán đường dẫn `/admin/clients/delete/{id}` vào thanh địa chỉ — ngoại lệ duy nhất là `CRM_CUST_TC_123`, chạy trên khách hàng test riêng.
>
> 📎 **Tab phụ trợ Notes · Reminders chưa có ảnh evidence** (xem mục "Vùng chưa có evidence" ở index) → TC của hai tab này gắn `@NeedsVerify`, nhãn nguyên văn lấy từ lần đọc DOM ngày 14-08-2026.

---

## Nhóm O — Trạng thái hoạt động (Active ↔ Inactive)

**Bảng chuyển trạng thái** (Ma trận Trạng thái — index requirements mục 7):

| Từ \ Đến | Active | Inactive | *(bị xoá)* |
|---|---|---|---|
| *(chưa tồn tại)* | ✅ `TC_009`, `TC_103` | ❌ không có lối tạo thẳng ở Inactive — `TC_011` xác nhận luôn ra Active | — |
| **Active** | — | ✅ `TC_074` | ✅ `TC_077` · ❌ bị chặn khi có dữ liệu liên quan `TC_081` |
| **Inactive** | ✅ `TC_074` | — | ✅ `TC_075` bước 5 |

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_074 | REQ-CUST-56, REQ-CUST-57, REQ-CUST-58, REQ-CUST-59 | CUST | High | Gạt công tắc Active sang tắt rồi bật lại — đổi tại chỗ, giữ sau khi nạp lại | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc074_202609191400` (vừa tạo, đang Active). Ghi lại `Active Customers` và `Inactive Customers` | 1. Tìm `auto_cust_tc074_202609191400` trong bảng<br>2. Rê chuột lên công tắc cột `Active`, đọc chú giải<br>3. Bấm công tắc<br>4. Quan sát công tắc, thanh cuộn, khối Summary<br>5. Nạp lại trang, tìm lại, đọc công tắc và khối Summary<br>6. Bấm công tắc lần nữa, nạp lại trang, tìm lại<br>7. Dọn dữ liệu theo `D1` | Company: `auto_cust_tc074_202609191400` | 1. Công tắc đang **bật** (nền xanh) — REQ-56<br>2. Chú giải nguyên văn `Won't be shown in dropdowns when creating new records` — REQ-59<br>4. Công tắc chuyển **xám**, nút tròn sang trái · trang **không** tải lại · khối Summary **chưa** đổi số (ASM-06 — Summary chỉ cập nhật khi nạp lại)<br>5. Công tắc vẫn **tắt** · `Inactive Customers` tăng 1, `Active Customers` giảm 1 so với số đã ghi<br>6. Công tắc **bật** trở lại và giữ nguyên sau khi nạp lại · Summary về đúng số ban đầu<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): bước 3 phát sinh `GET /admin/clients/change_client_status/{id}/0?csrf_token_name=…` trả `200`, bước 6 là `…/{id}/1`. Đọc trạng thái công tắc bằng **thuộc tính DOM `.checked`**, KHÔNG đọc thuộc tính HTML `checked` (không đổi sau khi gạt — `RISK-CUST-07`) | Critical | Yes | UI | @Regression, @CriticalPath, @StateTransition, @TechCheck, @Customers |
| CRM_CUST_TC_075 | REQ-CUST-57, REQ-CUST-61 | CUST | Medium | Khách hàng đang Inactive vẫn sửa được hồ sơ và vẫn xoá được | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc075_202609191405`, đã gạt sang **Inactive** | 1. Mở hồ sơ `KH-TEST`<br>2. Đổi Phone thành `0900000075`, bấm `Save`, nạp lại<br>3. Ở danh sách, tìm tên, đọc công tắc và cột `Phone`<br>4. Rê chuột lên dòng, bấm `Delete`, chọn `OK`<br>5. Tìm lại tên | Phone: `0900000075` | 2. Lưu thành công, Phone `0900000075`<br>3. Công tắc vẫn **tắt** (sửa hồ sơ không làm đổi trạng thái) · Phone `0900000075`<br>4. Hiện thông báo xanh `Customer deleted`<br>5. `No matching records found` | Medium | Yes | UI | @Regression, @StateTransition, @Customers |

---

## Nhóm P — Xoá khách hàng

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_076 | REQ-CUST-60 | CUST | High | Bấm Delete thì hỏi xác nhận; chọn huỷ thì không xoá | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc076_202609191410` | 1. Tìm tên trong bảng, rê chuột lên dòng<br>2. Bấm liên kết đỏ `Delete`<br>3. Đọc hộp thoại của trình duyệt<br>4. Bấm `Cancel` (Huỷ)<br>5. Nạp lại, tìm lại tên<br>6. Dọn dữ liệu theo `D1` | Company: `auto_cust_tc076_202609191410` | 3. Hộp thoại xác nhận của trình duyệt, nội dung nguyên văn `Are you sure you want to perform this action?`, có 2 nút OK / Cancel<br>4. Hộp thoại đóng, vẫn ở danh sách<br>5. Dòng `auto_cust_tc076_…` **vẫn còn** | Critical | Yes | UI | @Regression, @CRUD, @Destructive, @Customers |
| CRM_CUST_TC_077 | REQ-CUST-61 | CUST | High | Đồng ý xác nhận thì xoá khách hàng, báo "Customer deleted", tổng giảm 1 | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc077_202609191415`. Ghi lại `Total Customers` | 1. Tìm tên trong bảng, rê chuột lên dòng, bấm `Delete`<br>2. Bấm `OK` ở hộp thoại xác nhận<br>3. Đọc URL và thông báo nổi<br>4. Tìm lại tên<br>5. Nạp lại, đọc `Total Customers` | Company: `auto_cust_tc077_202609191415` | 3. Dừng ở `https://crm.anhtester.com/admin/clients` · thông báo nổi **nền xanh** nguyên văn `Customer deleted`<br>4. `No matching records found`<br>5. `Total Customers` **giảm đúng 1** so với số đã ghi (môi trường có người khác thao tác cùng lúc → ghi chú, không kết luận FAIL) | Critical | Yes | UI | @Regression, @CriticalPath, @CRUD, @Destructive, @Customers |
| CRM_CUST_TC_078 | REQ-CUST-61 | CUST | Medium | Mở lại hồ sơ của khách hàng đã xoá (qua URL cũ hoặc nút Back) | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc078_202609191420`, ghi lại `{id}` | 1. Mở hồ sơ `KH-TEST`, sau đó về danh sách và xoá nó (`Delete` → `OK`)<br>2. Bấm nút **Back** của trình duyệt cho tới khi về trang hồ sơ cũ, nạp lại trang<br>3. Gõ thẳng `https://crm.anhtester.com/admin/clients/client/{id}` | `{id}` của khách hàng đã xoá | 2–3. ⚠️ Chưa có evidence. Kỳ vọng tối thiểu: **không** hiện lại dữ liệu của khách hàng đã xoá, **không** trang lỗi hệ thống trắng — chấp nhận trang báo không tìm thấy hoặc chuyển về danh sách. Ghi lại hành vi thật. **FAIL** nếu mở ra một biểu mẫu sửa trống mà bấm `Save` lại tạo ra bản ghi | Medium | Partial | UI | @Regression, @ErrorGuessing, @NeedsVerify, @Customers |
| CRM_CUST_TC_079 | REQ-CUST-81 | CUST | High | Khách hàng Inactive không xuất hiện trong ô chọn khách hàng khi tạo chứng từ bán hàng (6 module) | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc079_202609191425`, đã gạt sang **Inactive**. Chạy bước 1–3 cho **từng** biến thể, **không** lưu biểu mẫu nào | 1. Mở biểu mẫu tạo mới của module theo biến thể<br>2. Mở ô chọn khách hàng, gõ `auto_cust_tc079`<br>3. Đọc danh sách gợi ý, đóng biểu mẫu **không lưu**<br>4. Dọn dữ liệu theo `D1` (sau biến thể cuối — hoặc chuyển sang `CRM_CUST_TC_080` dùng tiếp) | **Bảng biến thể (module):**<br>`a` Hoá đơn — `Sales → Invoices → Create New Invoice`<br>`b` Báo giá — `Sales → Estimates → Create New Estimate`<br>`c` Đề xuất — `Sales → Proposals → New Proposal` (liên quan tới `Customer`)<br>`d` Giấy báo có — `Sales → Credit Notes → New Credit Note`<br>`e` Đăng ký định kỳ — `Subscriptions → New Subscription`<br>`f` Thanh toán — ghi nhận module có ô chọn khách hàng khi tạo mới hay không | **Mọi biến thể cho cùng kết quả:**<br>3. Danh sách gợi ý **không** có `auto_cust_tc079_202609191425`<br><br>⚠️ **Quyết định PO chưa kiểm chứng** (`AMB-CUST-10` → `REQ-CUST-81` ⚪) → `@AssumptionBased`. Đường menu lấy theo `system_map`, chưa mở lần nào — ghi lại đường thật nếu khác. `f` không có ô chọn khách hàng → chấm `N/A`, không FAIL | High | Partial | UI | @Regression, @BusinessRule, @Dependency, @E2E, @AssumptionBased, @NeedsVerify, @Customers |
| CRM_CUST_TC_080 | REQ-CUST-81, REQ-CUST-58 | CUST | High | Khách hàng Inactive không xuất hiện ở ô chọn khách hàng của 5 module còn lại; bật lại Active thì xuất hiện trở lại | Như `CRM_CUST_TC_079` (dùng chung `KH-TEST` đang Inactive) | 1. Mở biểu mẫu tạo mới của module theo biến thể<br>2. Mở ô chọn khách hàng, gõ `auto_cust_tc079`<br>3. Đọc gợi ý, đóng **không lưu**<br>4. Sau biến thể cuối: gạt `KH-TEST` về **Active**<br>5. Mở lại biểu mẫu Hoá đơn (biến thể `a` của `TC_079`), gõ `auto_cust_tc079`<br>6. Dọn dữ liệu theo `D1` | **Bảng biến thể (module):**<br>`a` Hợp đồng — `Contracts → New Contract`<br>`b` Dự án — `Projects → New Project`<br>`c` Chi phí — `Expenses → Record Expense`<br>`d` Hỗ trợ — `Support → New Ticket`<br>`e` Công việc — `Tasks → New Task`, mục `Related To` = `Customer` | **Mọi biến thể `a`–`e`:**<br>3. Gợi ý **không** có `auto_cust_tc079_202609191425`<br>5. Sau khi bật lại Active, gợi ý **có** `auto_cust_tc079_202609191425`<br><br>⚠️ Như `TC_079` — `@AssumptionBased`, đường menu chưa kiểm chứng | High | Partial | UI | @Regression, @BusinessRule, @Dependency, @E2E, @AssumptionBased, @NeedsVerify, @Customers |
| CRM_CUST_TC_081 | REQ-CUST-82 | CUST | High | Xoá khách hàng đang có dự án thì bị từ chối, dự án không bị xoá theo | `P-ADMIN`. Tự tạo dữ liệu riêng: `KH-TEST` Company `auto_cust_tc081_202609191430` + **1 dự án test** gắn khách hàng đó, tên `auto_prj_tc081_202609191430` (tạo ở `Projects → New Project`). **Không** dùng khách hàng/dự án của người khác | 1. Ở danh sách khách hàng, tìm `auto_cust_tc081`, bấm `Delete` → `OK`<br>2. Đọc thông báo<br>3. Tìm lại khách hàng<br>4. Mở `Projects`, tìm `auto_prj_tc081`<br>5. Dọn dữ liệu: xoá dự án `auto_prj_tc081_…` trước, rồi xoá khách hàng theo `D1` | Khách hàng: `auto_cust_tc081_202609191430`<br>Dự án: `auto_prj_tc081_202609191430` | 2. Hệ thống **từ chối xoá** và hiện thông báo lỗi — ⚠️ nội dung nguyên văn chưa kiểm chứng, ghi lại<br>3. Khách hàng **vẫn còn** trong danh sách<br>4. Dự án **vẫn còn**, vẫn gắn đúng khách hàng — không thành bản ghi mồ côi<br><br>⚠️ **Quyết định PO chưa kiểm chứng** (`AMB-CUST-11` → `REQ-CUST-82` ⚪). Nếu bước 1 xoá được và kéo theo xoá dự án → **FAIL nghiêm trọng**, mở bug mức Critical ngay | Critical | Partial | UI | @Regression, @BusinessRule, @Dependency, @Destructive, @AssumptionBased, @NeedsVerify, @Customers |

---

## Nhóm Q — Tab phụ trợ của hồ sơ: Notes · Statement · Vault · Map · Reminders

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_082 | REQ-CUST-63 | CUST | Medium | Tab Notes: giao diện, thêm ghi chú nội bộ rồi xoá | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc082_202609191435` | 1. Mở hồ sơ → tab `Notes`<br>2. Đọc giao diện<br>3. Bấm `New Note`, nhập ô `Note description`, bấm `Save`<br>4. Đọc bảng<br>5. Xoá ghi chú qua cột `Options`, xác nhận<br>6. Dọn dữ liệu theo `D1` | Note: `Ghi chú kiểm thử TC082 202609191435` | 2. Nút `New Note` · bảng 4 cột `Description` · `Added From` · `Date Added` · `Options` · nội dung `No entries found`<br>3. Hiện ô nhiều dòng nhãn `Note description` và nút `Save`<br>4. Bảng có 1 dòng: `Description` đúng nội dung · `Added From` = tên nhân sự đang đăng nhập · `Date Added` = hôm nay<br>5. Bảng về `No entries found`<br><br>⚠️ Tab chưa có ảnh evidence; bước 3–5 chưa thực thi lúc khảo sát → `@NeedsVerify` | Medium | Yes | UI | @Regression, @CRUD, @NeedsVerify, @Customers |
| CRM_CUST_TC_083 | REQ-CUST-63 | CUST | Medium | Nội dung ghi chú nhiều dòng, dài, có thẻ HTML/mã được lưu nguyên văn và không chạy mã | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc083_202609191440`. Mỗi biến thể: thêm 1 ghi chú → đọc → xoá | 1. Tab `Notes` → `New Note`, nhập theo biến thể, `Save`<br>2. Đọc cột `Description` của dòng mới, quan sát hộp thoại bật lên<br>3. Xoá ghi chú<br>4. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` 3 dòng → `Dòng 1 tc083` ⏎ `Dòng 2` ⏎ `Dòng 3`<br>`b` Mã → `<script>alert('tc083')</script>`<br>`c` Tiếng Việt + emoji → `Khách hẹn gặp thứ Hai 📅 tc083`<br>`d` 5000 ký tự → `tc083 ` + chữ `n` lặp lại cho đủ 5000 ký tự | **Mọi biến thể cho cùng kết quả:**<br>1. Lưu thành công<br>2. Nội dung hiện đúng như nhập (`a` giữ được việc xuống dòng hoặc hiện liền — ghi lại), **không** hộp thoại `alert`, không vỡ bố cục bảng<br><br>⚠️ `@NeedsVerify` như `TC_082`. Ô ghi chú **không** bắt buộc theo đặc tả — ghi lại hệ thống xử lý ra sao nếu bấm `Save` khi ô trống | Medium | Yes | UI | @Regression, @Validation, @Security, @NeedsVerify, @Customers |
| CRM_CUST_TC_084 | REQ-CUST-64, REQ-CUST-65 | CUST | Medium | Tab Statement của khách hàng mới hiển thị đúng bố cục, khoảng tháng hiện tại và số dư 0 | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc084_202609191445`, Address `123 Auto Recon Street`, City `Da Nang`, VAT `VAT-TC084` — **chưa** có hoá đơn/thanh toán | 1. Mở hồ sơ → tab `Statement`<br>2. Đối chiếu Bảng kiểm<br>3. Dọn dữ liệu theo `D1` | Chạy ngày 19-09-2026 → tháng hiện tại `01-09-2026` → `30-09-2026` | **Bảng kiểm:**<br>`1` Tiêu đề vùng phải `Statement`; ô chọn khoảng thời gian góc trái đang hiện `This Month`; góc phải có 3 nút biểu tượng: máy in · tệp PDF · phong bì<br>`2` Dòng tiêu đề `Customer Statement For auto_cust_tc084_202609191445`<br>`3` Khối `To:` có tên công ty, địa chỉ, `VAT Number: VAT-TC084`<br>`4` Khối `Account Summary` ghi khoảng `01-09-2026 To 30-09-2026` (ngày đầu và cuối **tháng chạy TC**, dạng `DD-MM-YYYY`); 4 dòng `Beginning Balance:` · `Invoiced Amount:` · `Amount Paid:` · `Balance Due:` đều `$0.00`<br>`5` Câu `Showing all invoices and payments between 01-09-2026 and 30-09-2026`<br>`6` Bảng 5 cột `Date` · `Details` · `Amount` · `Payments` · `Balance`; dòng đầu `01-09-2026` · `Beginning Balance` · `0.00` · (trống) · `0.00`; dòng cuối `Balance Due` · `$0.00` | Medium | Yes | UI | @Regression, @Display, @Customers |
| CRM_CUST_TC_085 | REQ-CUST-64 | CUST | Medium | Đổi khoảng thời gian sao kê thì khoảng ngày cập nhật đúng | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc085_202609191450`. Đang ở tab `Statement`. Chạy lại cho **từng** biến thể | 1. Bấm ô khoảng thời gian, đọc danh sách lựa chọn<br>2. Chọn lựa chọn của biến thể (`f`: nhập thêm từ ngày / đến ngày xuất hiện sau khi chọn `Period`)<br>3. Đọc khoảng ngày ở `Account Summary` và câu `Showing all invoices and payments between …`<br>4. Dọn dữ liệu theo `D1` (sau biến thể cuối) | Chạy ngày **19-09-2026** — tính lại theo ngày chạy thật:<br>**Bảng biến thể:**<br>`a` `Today` → `19-09-2026` … `19-09-2026`<br>`b` `This Week` → tuần chứa 19-09-2026 (ghi lại ngày bắt đầu tuần hệ thống dùng)<br>`c` `Last Month` → `01-08-2026` … `31-08-2026`<br>`d` `This Year` → `01-01-2026` … `31-12-2026`<br>`e` `Last Year` → `01-01-2025` … `31-12-2025`<br>`f` `Period` từ `01-01-2026` đến `31-01-2026` | 1. Đúng 7 lựa chọn theo thứ tự `Today` · `This Week` · `This Month` · `Last Month` · `This Year` · `Last Year` · `Period`<br>**Mọi biến thể:** 3. Cả hai chỗ đều hiện đúng khoảng ngày của biến thể, dạng `DD-MM-YYYY`; số dư vẫn `$0.00`<br>`f` thêm: chỉ khi chọn `Period` mới xuất hiện 2 ô từ ngày / đến ngày | Medium | Yes | UI | @Regression, @DateRange, @Customers |
| CRM_CUST_TC_086 | REQ-CUST-64 | CUST | Low | Sao kê theo Period với ngày kết thúc trước ngày bắt đầu | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc086_202609191455`. Tab `Statement` | 1. Chọn `Period`<br>2. Nhập từ ngày `31-01-2026`, đến ngày `01-01-2026`<br>3. Quan sát khoảng ngày, thông báo, bảng<br>4. Dọn dữ liệu theo `D1` | Từ `31-01-2026` · Đến `01-01-2026` | 3. ⚠️ Chưa có evidence. Kỳ vọng tối thiểu: không trang lỗi hệ thống; hệ thống chặn / báo khoảng ngày sai, hoặc hiện sao kê rỗng. Ghi lại hành vi thật | Low | Partial | UI | @Regression, @DateRange, @ErrorGuessing, @NeedsVerify, @Customers |
| CRM_CUST_TC_087 | REQ-CUST-66 | CUST | Low | Gửi sao kê qua email khi khách hàng chưa có liên hệ thì danh sách người nhận rỗng | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc087_202609191500`, **chưa** có liên hệ. Tab `Statement` | 1. Bấm nút biểu tượng phong bì góc phải<br>2. Mở ô chọn người nhận<br>3. Đóng hộp thoại, **KHÔNG** bấm gửi<br>4. Dọn dữ liệu theo `D1` | — (không gửi email) | 1. Mở biểu mẫu gửi sao kê có ô chọn người nhận và ô `CC` — ⚠️ chưa có ảnh, nhãn lấy từ đọc DOM<br>2. Danh sách người nhận **rỗng**, không có lựa chọn nào<br>3. Đóng được, không có email nào được gửi | Low | Partial | UI | @Regression, @NeedsVerify, @Customers |
| CRM_CUST_TC_088 | REQ-CUST-67, REQ-CUST-68 | CUST | Medium | Tab Vault: trạng thái rỗng và hộp thoại Vault Entry đủ trường, đúng mặc định | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc088_202609191505` | 1. Mở hồ sơ → tab `Vault`<br>2. Bấm `New Vault Entry`<br>3. Đối chiếu Bảng kiểm<br>4. Đóng hộp thoại không lưu<br>5. Dọn dữ liệu theo `D1` | — (không lưu) | **Bảng kiểm:**<br>`1` (REQ-67) Tab có nút xanh `+ New Vault Entry` và dải xanh nhạt `Vault entries not found for this customer.`<br>`2` (REQ-67) Hộp thoại tiêu đề `Vault Entry`, các ô theo thứ tự `* Server Address` · `Port` · `* Username` · `* Password` · `Short Description` (nhiều dòng)<br>`3` (REQ-67) Gõ vào `Password` hiện dấu chấm che; ô `Port` chỉ nhận số<br>`4` (REQ-68) Nhóm 3 nút chọn một, nguyên văn: `Visible to all staff member who have access to this customer` (**đang chọn**) · `Visible only to administrators` · `Visible only to me (administrators are not excluded)`<br>`5` (REQ-67) Ô tích `Share this vault entry in projects with project members`, đang rỗng<br><br>⚠️ Mục `2`–`5` lấy từ đọc DOM 14-08-2026, chưa có ảnh hộp thoại | Medium | Yes | UI | @Regression, @Modal, @UI, @NeedsVerify, @Customers |
| CRM_CUST_TC_089 | REQ-CUST-67 | CUST | Medium | Vault Entry: bỏ trống từng trường bắt buộc thì không lưu và báo đúng ô | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc089_202609191510`. Mở lại hộp thoại `New Vault Entry` trước **mỗi** biến thể | 1. Điền theo biến thể (ô không nêu thì điền giá trị mẫu: Server `auto-vault-tc089.test`, Username `auto_user_tc089`, Password `VaultTest#089`)<br>2. Bấm `Save`<br>3. Quan sát các ô và hộp thoại<br>4. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` Bỏ trống **cả 3** ô bắt buộc<br>`b` Chỉ bỏ trống `Server Address`<br>`c` Chỉ bỏ trống `Username`<br>`d` Chỉ bỏ trống `Password` | **Mọi biến thể cho cùng kết quả:**<br>3. Hộp thoại **không** đóng, không có mục Vault nào được tạo · thông báo bắt buộc hiện **đúng** dưới (các) ô bị bỏ trống, ô đã điền **không** báo lỗi — `a` báo cả 3 ô<br><br>⚠️ Nội dung thông báo chưa có evidence (kỳ vọng `This field is required.`) — ghi nguyên văn | Medium | Yes | UI | @Regression, @Required, @NeedsVerify, @Customers |
| CRM_CUST_TC_090 | REQ-CUST-67, REQ-CUST-69 | CUST | High | Tạo mục Vault, mật khẩu được che và chỉ xem được sau khi xác nhận mật khẩu của chính mình | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc090_202609191515` | 1. Tab `Vault` → `New Vault Entry`, điền theo Test Data, để mức hiển thị mặc định, `Save`<br>2. Đọc mục vừa tạo<br>3. Bấm lệnh xem mật khẩu của mục<br>4. Nhập **sai** mật khẩu `SaiMatKhau_tc090`, xác nhận<br>5. Bấm xem lại, nhập mật khẩu đúng của tài khoản đang đăng nhập (`ADMIN_PASSWORD` trong `.env`), xác nhận<br>6. Xoá mục Vault, rồi dọn `D1` | Server Address `auto-vault-tc090.test` · Port `22` · Username `auto_user_tc090` · Password `VaultTest#090` · Short Description `Máy chủ thử nghiệm TC090` | 2. Mục hiện Server Address, Port, Username; mật khẩu **không** hiện chữ rõ<br>3. Hộp thoại tiêu đề `View Password` với 1 ô mật khẩu (gõ vào hiện dấu chấm)<br>4. **Không** hiện mật khẩu Vault, có thông báo sai mật khẩu (ghi nguyên văn)<br>5. Hiện đúng `VaultTest#090`<br><br>⚠️ Chưa thực thi lúc khảo sát → `@NeedsVerify`. 🔒 `VaultTest#090` là giá trị giả của TC, KHÔNG dùng mật khẩu thật | High | Partial | UI | @Regression, @Security, @CRUD, @NeedsVerify, @Customers |
| CRM_CUST_TC_091 | REQ-CUST-67 | CUST | Low | Port của Vault tại biên hợp lệ 1 và 65535 được lưu | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc091_202609191520`. Mỗi biến thể: tạo 1 mục → đọc → xoá | 1. `New Vault Entry`: Server `auto-vault-tc091.test`, Username `auto_user_tc091`, Password `VaultTest#091`, Port theo biến thể, `Save`<br>2. Đọc Port của mục vừa tạo<br>3. Xoá mục; dọn `D1` sau biến thể cuối | **Bảng biến thể:**<br>`a` Port `1`<br>`b` Port `65535` | **Mọi biến thể:** 1. Lưu thành công · 2. Port hiện đúng giá trị đã nhập<br><br>⚠️ Miền 1–65535 là giả định theo chuẩn cổng mạng (ASM-09), tài liệu không nêu | Low | Yes | UI | @Regression, @Boundary, @NeedsVerify, @Customers |
| CRM_CUST_TC_092 | REQ-CUST-67 | CUST | Low | Port của Vault ngoài miền hợp lệ | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc092_202609191525`. Mở lại hộp thoại trước **mỗi** biến thể | 1. `New Vault Entry`: Server `auto-vault-tc092.test`, Username `auto_user_tc092`, Password `VaultTest#092`, nhập Port theo biến thể<br>2. Bấm `Save`, quan sát<br>3. Nếu lưu được: đọc Port, xoá mục; dọn `D1` sau biến thể cuối | **Bảng biến thể:**<br>`a` `0`<br>`b` `65536`<br>`c` `-1`<br>`d` `2.5`<br>`e` Gõ chữ `abc` | ⚠️ Chưa có quy tắc (ASM-09) — **ghi nhận** cho từng biến thể: bị chặn (ghi thông báo) hay lưu được (ghi giá trị lưu). Kỳ vọng tối thiểu mọi biến thể: không trang lỗi hệ thống<br>`e`: ô chỉ nhận số nên chữ **không** gõ vào được — nếu gõ được là lệch đặc tả `Port (kiểu số)` | Low | Partial | UI | @Regression, @Boundary, @NeedsVerify, @Customers |
| CRM_CUST_TC_093 | REQ-CUST-70 | CUST | Low | Tab Map lưu được toạ độ và báo thiếu khoá API bản đồ | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc093_202609191530` | 1. Mở hồ sơ → tab `Map`<br>2. Đọc giao diện<br>3. Nhập Latitude `16.0544`, Longitude `108.2022`, bấm `Save`<br>4. Nạp lại, đọc hai ô<br>5. Dọn dữ liệu theo `D1` | Latitude `16.0544` · Longitude `108.2022` | 2. Hai ô `Latitude (Google Maps)` (có nút chữ `G` dính bên phải; rê chuột hiện chú giải nguyên văn `Fetch from google - Fill address, city and country before fetching to get best result.` — **không** bấm, lấy toạ độ từ Google nằm ngoài phạm vi) · `Longitude (Google Maps)` · nút `Save` · dòng chữ nguyên văn `Setup google api key in order to view to customer map` · **không** thấy khung bản đồ nào<br>4. Hai ô giữ đúng `16.0544` và `108.2022`<br><br>ℹ️ Hiển thị bản đồ **ngoài phạm vi** (`AMB-CUST-09`) — chỉ chấm tới việc lưu toạ độ | Low | Yes | UI | @Regression, @Customers |
| CRM_CUST_TC_094 | REQ-CUST-70 | CUST | Low | Latitude tại biên và ngoài miền −90…90 | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc094_202609191535`, tab `Map`, Longitude để trống. Chạy lại cho **từng** biến thể | 1. Nhập `Latitude` theo biến thể, `Save`<br>2. Nạp lại, đọc ô Latitude<br>3. Dọn `D1` sau biến thể cuối | **Bảng biến thể:**<br>`a` `90`<br>`b` `-90`<br>`c` `90.0001`<br>`d` `-90.0001`<br>`e` `abc`<br>`f` Dấu phẩy thập phân `16,0544` | ⚠️ Không có quy tắc trong tài liệu (ASM-09) — **ghi nhận** giá trị lưu được cho từng biến thể. Kỳ vọng theo chuẩn toạ độ: `a`,`b` lưu nguyên; `c`–`f` bị chặn hoặc có cảnh báo. Mọi biến thể: không trang lỗi hệ thống | Low | Partial | UI | @Regression, @Boundary, @NeedsVerify, @Customers |
| CRM_CUST_TC_095 | REQ-CUST-70 | CUST | Low | Longitude tại biên và ngoài miền −180…180 | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc095_202609191540`, tab `Map`, Latitude để trống. Chạy lại cho **từng** biến thể | 1. Nhập `Longitude` theo biến thể, `Save`<br>2. Nạp lại, đọc ô Longitude<br>3. Dọn `D1` sau biến thể cuối | **Bảng biến thể:**<br>`a` `180`<br>`b` `-180`<br>`c` `180.0001`<br>`d` `-180.0001`<br>`e` `abc`<br>`f` Dấu phẩy thập phân `108,2022` | ⚠️ Như `TC_094` — ghi nhận. Kỳ vọng theo chuẩn: `a`,`b` lưu nguyên; `c`–`f` bị chặn hoặc có cảnh báo; không trang lỗi hệ thống | Low | Partial | UI | @Regression, @Boundary, @NeedsVerify, @Customers |
| CRM_CUST_TC_096 | REQ-CUST-71 | CUST | Low | Tab Reminders và hộp thoại Set Reminder hiển thị đủ trường | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc096_202609191545` | 1. Mở hồ sơ → tab `Reminders`<br>2. Đọc bảng<br>3. Bấm `Set Reminder`, đọc hộp thoại<br>4. Mở ô `Set reminder to`, đọc lựa chọn<br>5. Đóng không lưu; dọn `D1` | — (không lưu) | 2. Nút `Set Reminder` · bảng 4 cột `Description` · `Date` · `Remind` · `Is notified?`<br>3. Hộp thoại tiêu đề `Set Reminder`: `* Date to be notified` (chọn ngày giờ) · `* Set reminder to` · `* Description` (nhiều dòng) · ô tích `Send also an email for this reminder` đang rỗng<br>4. Có đủ 3 nhân sự `Project Manager` · `Admin Anh Tester` · `Admin Example` (tổng 4 lựa chọn tính cả dòng rỗng/mặc định — ghi lại)<br><br>⚠️ Tab chưa có ảnh evidence → `@NeedsVerify` | Low | Yes | UI | @Regression, @Modal, @UI, @NeedsVerify, @Customers |
| CRM_CUST_TC_097 | REQ-CUST-71 | CUST | Low | Set Reminder: bỏ trống từng trường bắt buộc thì không lưu và báo đúng ô | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc097_202609191550`. Mở lại hộp thoại trước **mỗi** biến thể | 1. Điền theo biến thể (ô không nêu thì điền mẫu: ngày `20-09-2026 09:00`, nhân sự `Admin Example`, mô tả `Nhắc TC097`)<br>2. Bấm `Save`, quan sát<br>3. Dọn `D1` sau biến thể cuối | **Bảng biến thể:**<br>`a` Bỏ trống cả 3 ô bắt buộc<br>`b` Chỉ bỏ trống `Date to be notified`<br>`c` Chỉ bỏ trống `Set reminder to`<br>`d` Chỉ bỏ trống `Description` | **Mọi biến thể cho cùng kết quả:**<br>2. Hộp thoại không đóng, không có nhắc nhở nào được tạo · thông báo bắt buộc hiện đúng dưới (các) ô bị trống<br><br>⚠️ Nội dung thông báo chưa có evidence — ghi nguyên văn | Low | Yes | UI | @Regression, @Required, @NeedsVerify, @Customers |
| CRM_CUST_TC_098 | REQ-CUST-71 | CUST | Low | Đặt nhắc nhở cho khách hàng rồi xoá | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc098_202609191555` | 1. Tab `Reminders` → `Set Reminder`, điền theo Test Data, **không** tích gửi email, `Save`<br>2. Đọc bảng<br>3. Xoá nhắc nhở vừa tạo<br>4. Dọn `D1` | Date to be notified `20-09-2026 09:00` · Set reminder to `Admin Example` · Description `Gọi lại khách hàng TC098` · Send also an email: **không** tích | 2. Bảng có 1 dòng: `Description` đúng nội dung · `Date` `20-09-2026 09:00` · `Remind` là `Admin Example` · `Is notified?` chưa<br>3. Bảng về trạng thái rỗng<br><br>⚠️ Chưa thực thi lúc khảo sát → `@NeedsVerify` | Low | Yes | UI | @Regression, @CRUD, @NeedsVerify, @Customers |
| CRM_CUST_TC_099 | REQ-CUST-71 | CUST | Low | Ngày nhắc nhở ở quá khứ, ngày không tồn tại và chữ | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc099_202609191600`. Mở lại hộp thoại trước **mỗi** biến thể | 1. Nhập `Date to be notified` theo biến thể (gõ tay, không dùng lịch chọn), nhân sự `Admin Example`, mô tả `Nhắc TC099`<br>2. `Save`, quan sát; nếu lưu được thì đọc ngày trong bảng rồi xoá<br>3. Dọn `D1` sau biến thể cuối | **Bảng biến thể:**<br>`a` Quá khứ → `18-09-2026 09:00`<br>`b` Ngày không tồn tại → `31-02-2026 09:00`<br>`c` Chữ → `abc` | ⚠️ Chưa có quy tắc — **ghi nhận** cho từng biến thể (bị chặn + thông báo, hay lưu với giá trị nào). Kỳ vọng tối thiểu: `b`,`c` **không** lưu thành một ngày khác âm thầm (VD `31-02` thành `03-03`); không trang lỗi hệ thống | Low | Partial | UI | @Regression, @Validation, @NeedsVerify, @Customers |

---

## Nhóm R — Nhập khách hàng từ CSV

> 📄 **Tệp CSV mẫu dùng trong nhóm này** (tự soạn, lưu UTF-8, hàng đầu là 27 tiêu đề đúng thứ tự của bảng mẫu — `CRM_CUST_TC_016`). Chỉ điền 5 cột: `Firstname` · `Lastname` · `Email` · `Company`; các cột khác để trống:
>
> | Tệp | Nội dung dữ liệu (Firstname · Lastname · Email · Company) |
> |---|---|
> | `CSV-2` | `Auto` · `Import1` · `auto_import_tc{nnn}_202609191600_1@auto.test` · `auto_import_cust_tc{nnn}_202609191600_1`<br>`Auto` · `Import2` · `auto_import_tc{nnn}_202609191600_2@auto.test` · `auto_import_cust_tc{nnn}_202609191600_2` |
> | `CSV-DUP` | 2 dòng như `CSV-2` + dòng 3: `Auto` · `Import3` · **email trùng dòng 1** · `auto_import_cust_tc{nnn}_202609191600_3` |
>
> `{nnn}` = số TC đang chạy. 🧹 Mọi khách hàng nhập vào phải dọn theo `D1` — liên hệ đi kèm bị xoá theo khách hàng.

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_100 | REQ-CUST-76 | CUST | Medium | Bấm Import khi chưa chọn tệp thì bị chặn, báo lỗi tại ô tệp | `P-ADMIN`, đang ở `/admin/clients/import` | 1. Không chọn tệp nào<br>2. Bấm `Import`<br>3. Quan sát ô `Choose CSV File` và URL | — | 2. Trang **không** chuyển đi<br>3. Nhãn `* Choose CSV File` chuyển đỏ · viền ô chọn tệp đỏ · dưới ô hiện chữ đỏ `This field is required.` · URL vẫn `…/admin/clients/import`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): sinh `p#file_csv-error`; `input#file_csv` nhận `aria-describedby="file_csv-error"` | High | Yes | UI | @Regression, @Required, @FileUpload, @TechCheck, @Customers |
| CRM_CUST_TC_101 | REQ-CUST-74, REQ-CUST-75 | CUST | Low | Tải tệp CSV mẫu đúng 27 cột như bảng mẫu | `P-ADMIN`, đang ở `/admin/clients/import` | 1. Bấm `Download Sample`<br>2. Mở tệp vừa tải bằng trình soạn văn bản<br>3. Đọc hàng đầu tiên<br>4. Xoá tệp đã tải | — | 1. Trình duyệt tải về một tệp `.csv`, trang không đổi<br>3. Hàng đầu có đúng 27 tiêu đề, cùng thứ tự với bảng mẫu trên màn hình (`CRM_CUST_TC_016` mục `1`) | Low | Yes | UI | @Regression, @FileUpload, @Customers |
| CRM_CUST_TC_102 | REQ-CUST-78, REQ-CUST-80 | CUST | High | Simulate Import hiển thị bản xem trước, không tạo bản ghi và không đánh dấu dòng trùng email | `P-ADMIN`, đang ở `/admin/clients/import`. Ghi lại `Total Customers`. Chuẩn bị `CSV-DUP` với `{nnn}` = `102` | 1. `Choose File` → chọn `CSV-DUP`<br>2. Bấm `Simulate Import`<br>3. Đọc khối kết quả<br>4. Nhìn ô chọn tệp<br>5. Mở `/admin/clients`, đọc `Total Customers`; tìm `auto_import_cust_tc102` | Tệp `CSV-DUP` (3 dòng, dòng 3 trùng email dòng 1) | 2. Trang tải lại<br>3. Khối tiêu đề `Simulation Data` kèm chữ xanh `Max 100 rows are shown` · dòng nguyên văn `If you are satisfied with the results upload the file again and click import.` · bảng **3** dòng như nhau, dòng 3 **không** bị đánh dấu trùng · cột `Country`/`Billing country`/`Shipping country` bỏ trống hiện `0`<br>4. Ô tệp trở về `No file chosen`<br>5. `Total Customers` **không đổi** · `No matching records found` | High | Yes | UI | @Regression, @FileUpload, @Customers |
| CRM_CUST_TC_103 | REQ-CUST-79 | CUST | High | Nhập thật tệp 2 dòng hợp lệ thì tạo 2 khách hàng Active, mỗi khách hàng kèm 1 liên hệ chính | `P-ADMIN`, đang ở `/admin/clients/import`. Ghi lại `Total Customers` và `Active Contacts`. Chuẩn bị `CSV-2` với `{nnn}` = `103` | 1. Chọn `CSV-2`, để `Groups` trống, mật khẩu mặc định trống<br>2. Bấm `Import`<br>3. Đọc thông báo và URL<br>4. Ở `/admin/clients`, tìm `auto_import_cust_tc103`<br>5. Đọc khối Summary<br>6. Dọn **cả 2** khách hàng theo `D1` | Tệp `CSV-2` | 3. Trang tải lại ở `…/admin/clients/import` · thông báo nổi thành công nguyên văn `Total Imported: 2`<br>4. Đúng 2 dòng (ảnh `cust_import_result_2_rows_in_list.png`): Company `…_1`, `…_2` · `Primary Contact` `Auto Import1` / `Auto Import2` · `Primary Email` đúng email trong tệp · công tắc **bật** · `Groups` trống · `Date Created` hôm nay<br>5. `Total Customers` tăng **2** · `Active Contacts` tăng **2** | Critical | Yes | UI | @Regression, @CriticalPath, @FileUpload, @Customers |
| CRM_CUST_TC_104 | REQ-CUST-73 | CUST | High | Nhập thật tệp có dòng trùng email thì dòng trùng bị bỏ qua | `P-ADMIN`, đang ở `/admin/clients/import`. Chuẩn bị `CSV-DUP` với `{nnn}` = `104` | 1. Chọn `CSV-DUP`, bấm `Import`<br>2. Đọc thông báo<br>3. Ở `/admin/clients`, tìm `auto_import_cust_tc104`<br>4. Dọn mọi khách hàng tìm thấy theo `D1` | Tệp `CSV-DUP` | 2. `Total Imported: 2` — dòng 3 bị bỏ qua đúng như hướng dẫn `Duplicate email rows won't be imported.`<br>3. Chỉ có `…_1` và `…_2`, **không** có `…_3`<br><br>⚠️ Cách báo dòng bị bỏ qua **chưa kiểm chứng** ở lần nhập thật (`REQ-CUST-79`) — ghi nguyên văn nếu hệ thống báo thêm | High | Yes | UI | @Regression, @FileUpload, @BusinessRule, @NeedsVerify, @Customers |
| CRM_CUST_TC_105 | REQ-CUST-74, REQ-CUST-76 | CUST | Medium | Nhập tệp sai loại, tệp rỗng, tệp chỉ có tiêu đề, dòng thiếu cột bắt buộc | `P-ADMIN`, đang ở `/admin/clients/import`. Ghi lại `Total Customers`. Chạy lại cho **từng** biến thể | 1. Chọn tệp của biến thể (ô tệp không lọc loại tệp — mục 4.3 đặc tả)<br>2. Bấm `Import`<br>3. Đọc thông báo<br>4. Đọc lại `Total Customers`; dọn mọi bản ghi `auto_import_cust_tc105` nếu có | **Bảng biến thể:**<br>`a` Tệp văn bản `khach_hang_tc105.txt` nội dung `abc`<br>`b` Tệp `.csv` rỗng 0 KB<br>`c` `.csv` chỉ có hàng tiêu đề, 0 dòng dữ liệu<br>`d` `.csv` 1 dòng thiếu `Company` (Firstname `Auto`, Lastname `Missing`, Email `auto_import_tc105_d@auto.test`)<br>`e` `.csv` 1 dòng thiếu `Email` (Company `auto_import_cust_tc105_e`) | ⚠️ Chưa có evidence — kỳ vọng chung: không trang lỗi hệ thống · **không** tạo khách hàng rác · có thông báo cho người dùng biết (ghi nguyên văn, VD `Total Imported: 0`)<br>`d`,`e`: dòng thiếu cột bắt buộc **không** được nhập — nếu được nhập là **FAIL** (sinh khách hàng không tên / liên hệ không email) | Medium | Partial | UI | @Regression, @FileUpload, @NeedsVerify, @Customers |
| CRM_CUST_TC_106 | REQ-CUST-77 | CUST | Medium | Nhập kèm nhóm và mật khẩu mặc định cho liên hệ | `P-ADMIN`, đang ở `/admin/clients/import`. Chuẩn bị `CSV-2` với `{nnn}` = `106` | 1. Chọn `CSV-2`<br>2. `Groups` chọn 1 mục `Platinum`<br>3. Nhập `Default password for all contacts`: `MatKhauMacDinh@2026`, quan sát ô khi gõ<br>4. Bấm `Import`<br>5. Ở danh sách tìm `auto_import_cust_tc106`, đọc cột `Groups`<br>6. Dọn cả 2 khách hàng theo `D1` | Groups: `Platinum` · Mật khẩu mặc định: `MatKhauMacDinh@2026` (giá trị giả của TC) | 3. Mật khẩu **hiện rõ chữ** khi gõ — ghi nhận hiện trạng; đề xuất che bằng dấu chấm (người đứng sau màn hình đọc được mật khẩu dùng chung cho mọi liên hệ)<br>4. `Total Imported: 2`<br>5. Cả 2 dòng có `Platinum` ở cột `Groups` — ⚠️ chưa kiểm chứng việc gán nhóm khi nhập | Medium | Yes | UI | @Regression, @FileUpload, @Security, @NeedsVerify, @Customers |

---

## Nhóm S — Đoán lỗi (Error Guessing) và kịch bản trọn vòng đời

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_107 | REQ-CUST-33 | CUST | High | Bấm Save hai lần liên tiếp thật nhanh chỉ tạo một khách hàng | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Nhập `Company`: `auto_cust_tc107_202609191605`, bấm `Tab`, đợi dải cảnh báo trùng tên (nếu có) hiện xong<br>2. Bấm **đúp** nút `Save` (2 lần trong dưới 1 giây)<br>3. Đợi trang dừng chuyển<br>4. Ở danh sách, tìm `auto_cust_tc107_202609191605`<br>5. Dọn mọi bản ghi tìm thấy theo `D1` | Company: `auto_cust_tc107_202609191605` | 3. Dừng ở một hồ sơ `#{id} auto_cust_tc107_…`, không trang lỗi<br>4. Đúng **1** dòng — FAIL nếu có 2 dòng cùng tên (tạo trùng do bấm đúp) | High | Yes | UI | @Regression, @RaceCondition, @ErrorGuessing, @Customers |
| CRM_CUST_TC_108 | REQ-CUST-44 | CUST | Medium | Bấm Save ngay khi hệ thống còn đang kiểm tra trùng tên | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc108_202609191610`. Đang ở `/admin/clients/client` | 1. Nhập `Company`: `auto_cust_tc108_202609191610` (trùng `KH-TEST`)<br>2. **Không** rời ô bằng phím, bấm thẳng `Save` một lần<br>3. Quan sát trang<br>4. Nếu vẫn ở biểu mẫu: bấm `Save` lần nữa<br>5. Ở danh sách tìm tên<br>6. Dọn mọi bản ghi theo `D1` | Company: `auto_cust_tc108_202609191610` | 3. Theo quy tắc đã chốt (`AMB-CUST-04`): lần bấm đầu **có thể** bị bỏ qua — trang vẫn ở biểu mẫu, dải cảnh báo trùng tên xanh hiện ra. Nếu lưu luôn cũng đạt<br>4. Lưu thành công<br>5. Đúng **2** dòng cùng tên (`KH-TEST` + bản vừa tạo) — **không** được thành 3 dòng<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): automation phải chờ request `check_duplicate_customer_name` kết thúc trước khi bấm `Save` (`RISK-CUST-03`) | Medium | Partial | UI | @Regression, @RaceCondition, @ErrorGuessing, @TechCheck, @Customers |
| CRM_CUST_TC_109 | REQ-CUST-33 | CUST | Medium | Bấm Back của trình duyệt sau khi lưu khách hàng | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Nhập `Company`: `auto_cust_tc109_202609191615`, `Save`<br>2. Ở hồ sơ vừa mở, bấm nút **Back** của trình duyệt<br>3. Quan sát biểu mẫu<br>4. Ở danh sách tìm tên<br>5. Dọn theo `D1` | Company: `auto_cust_tc109_202609191615` | 3. ⚠️ Chưa có evidence — ghi lại biểu mẫu trống hay còn dữ liệu. Kỳ vọng tối thiểu: không trang lỗi hệ thống, **không** tự tạo thêm bản ghi chỉ vì bấm Back (không có hộp thoại "gửi lại biểu mẫu")<br>4. Đúng **1** dòng | Medium | Partial | UI | @Regression, @ErrorGuessing, @NeedsVerify, @Customers |
| CRM_CUST_TC_110 | REQ-CUST-50 | CUST | Medium | Hai tab cùng sửa một khách hàng — thay đổi của tab lưu trước có bị ghi đè không | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc110_202609191620`, Phone `0900000110`, City `Da Nang` | 1. Mở hồ sơ `KH-TEST` ở **tab A** và **tab B**<br>2. Tab A: đổi Phone thành `0911111110`, `Save`<br>3. Tab B (không nạp lại): đổi City thành `Hue`, `Save`<br>4. Nạp lại hồ sơ, đọc Phone và City<br>5. Dọn theo `D1` | Tab A: Phone `0911111110` · Tab B: City `Hue` | 4. ⚠️ Chưa có evidence. Kỳ vọng an toàn: City `Hue` **và** Phone `0911111110`, hoặc tab B bị chặn kèm cảnh báo dữ liệu đã đổi. Nếu Phone quay về `0900000110` (tab B ghi đè thay đổi của tab A mà không cảnh báo) → ghi nhận **mất cập nhật**, đề xuất `AMB` mới — không kết luận FAIL khi chưa có quy tắc | Medium | Partial | UI | @Regression, @Concurrency, @ErrorGuessing, @NeedsVerify, @Customers |
| CRM_CUST_TC_111 | REQ-CUST-33 | CUST | Low | Nạp lại trang giữa chừng khi đang nhập biểu mẫu thì không tạo bản ghi | `P-ADMIN`, đang ở `/admin/clients/client`. Ghi lại `Total Customers` | 1. Nhập `Company`: `auto_cust_tc111_202609191625`, Phone `0900000111`<br>2. Bấm `F5`, nếu trình duyệt hỏi thì chọn tải lại<br>3. Quan sát biểu mẫu<br>4. Ở danh sách, tìm tên và đọc `Total Customers` | Company: `auto_cust_tc111_202609191625` | 3. Biểu mẫu về trạng thái mới mở, con trỏ ở ô Company (trình duyệt có thể tự điền lại — ghi chú, không tính FAIL)<br>4. `No matching records found` · `Total Customers` không đổi | Low | Yes | UI | @Regression, @ErrorGuessing, @Customers |
| CRM_CUST_TC_112 | REQ-CUST-11, REQ-CUST-33, REQ-CUST-50, REQ-CUST-57, REQ-CUST-58, REQ-CUST-61 | CUST | High | Kịch bản trọn vòng đời: tạo → sửa → ngừng hoạt động → tìm lại → kích hoạt lại → xoá | `P-ADMIN`, đang ở `/admin/clients` | 1. `+ New Customer`, Company `auto_cust_tc112_202609191630`, Phone `0900000112`, `Save`<br>2. Sửa Phone thành `0911111112`, `Save`<br>3. Về danh sách, tìm tên, gạt công tắc sang tắt<br>4. Nạp lại, tìm lại bằng số `0911111112`<br>5. Gạt công tắc bật lại<br>6. `Delete` → `OK`<br>7. Tìm lại tên | Company: `auto_cust_tc112_202609191630` · Phone `0900000112` → `0911111112` | 1. Hồ sơ `#{id} auto_cust_tc112_…` mở ra<br>2. Phone mới được lưu<br>3. Công tắc tắt, trang không tải lại<br>4. Tìm bằng số điện thoại mới ra đúng dòng, công tắc vẫn tắt<br>5. Công tắc bật<br>6. `Customer deleted`<br>7. `No matching records found` | High | Yes | UI | @Regression, @CriticalPath, @UseCase, @Customers |
