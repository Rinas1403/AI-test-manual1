# Test Cases — `CUST` · Web · Part 01 — Vòng 1 Smoke (16 TC)

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_CUSTOMERS_SUMMARY.md](../../TEST_CASES_CUSTOMERS_SUMMARY.md) — cách đọc · quy ước tiền đề `P-*` và dọn dữ liệu `D1` · Assumptions · Bảng đối soát |
| **Nguồn requirement** | [requirements_customers_web.md](../../../../requirements/customers/web/requirements_customers_web.md) |
| **Nền tảng** | Web — Google Chrome, viewport desktop `1600×750` |
| **Dải TC ID trong part** | `CRM_CUST_TC_001` → `CRM_CUST_TC_016` |
| **Part kế tiếp** | [part_02_web_danh_sach.md](part_02_web_danh_sach.md) |

> ⚠️ **Vòng 1 chạy TRƯỚC mọi part khác.** Một TC ở đây FAIL (không mở được danh sách, không lưu được khách hàng) thì toàn bộ Vòng 2 coi như BLOCKED — dừng lại báo lỗi, đừng chạy tiếp.
>
> 🔢 **Số liệu tổng (1.571, 2.031…) trên môi trường dùng chung thay đổi liên tục.** TC không bao giờ khẳng định một con số cố định — chỉ khẳng định **quan hệ** (tổng = đang hoạt động + ngừng hoạt động, tăng đúng 1 sau khi tạo…).

---

## Nhóm A — Danh sách khách hàng: mở trang, giao diện, hiển thị

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_001 | REQ-CUST-01 | CUST | High | Mở màn hình danh sách khách hàng bằng mọi lối vào | `P-ADMIN`. Chạy lại từ bước 1 cho **từng** biến thể | 1. Mở trang theo lối vào của biến thể<br>2. Đợi trang tải xong<br>3. Đọc URL trên thanh địa chỉ và chữ trên tab trình duyệt<br>4. Nhìn vùng nội dung chính | **Bảng biến thể:**<br>`a` Bấm mục `Customers` ở menu trái<br>`b` Gõ thẳng `https://crm.anhtester.com/admin/clients` vào thanh địa chỉ | **Mọi biến thể cho cùng kết quả:**<br>2. Trang tải xong, không hiện trang lỗi<br>3. Thanh địa chỉ dừng ở `https://crm.anhtester.com/admin/clients` · tab trình duyệt ghi `Customers`<br>4. Thấy khối `Customers Summary` và bảng danh sách có dữ liệu · mục `Customers` ở menu trái được tô sáng<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): tài liệu trả HTTP `200`; `body` mang class `clients` | Critical | Yes | UI | @Smoke, @CriticalPath, @TechCheck, @Customers |
| CRM_CUST_TC_002 | REQ-CUST-02, REQ-CUST-08 | CUST | Medium | Thanh công cụ và khối tổng quan Customers Summary hiển thị đủ, đúng thứ tự | `P-ADMIN`, đang ở `/admin/clients`, trang vừa nạp | 1. Nhìn hàng nút trên cùng của vùng nội dung<br>2. Nhìn khối `Customers Summary`<br>3. Đối chiếu lần lượt từng mục trong Bảng kiểm | — (chỉ quan sát) | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-08) Từ trái sang phải có đúng 3 nút: `+ New Customer` (nền xanh) · `Import Customers` (nền xanh) · `Contacts` (nền trắng)<br>`2` (REQ-08) Góc phải cùng hàng có 1 nút biểu tượng phễu lọc<br>`3` (REQ-02) Khối có tiêu đề `Customers Summary` và đúng 6 ô, từ trái sang phải: `Total Customers` · `Active Customers` · `Inactive Customers` · `Active Contacts` · `Inactive Contacts` · `Contacts Logged In Today`<br>`4` (REQ-02) Mỗi ô là **một con số đứng trước nhãn**; nhãn `Active …` chữ xanh lá / xanh dương, nhãn `Inactive …` chữ đỏ<br>`5` (REQ-02) Số `Total Customers` **bằng đúng** `Active Customers` + `Inactive Customers` (VD ảnh evidence: 1571 = 1565 + 6) | Medium | Yes | UI | @Smoke, @UI, @Customers |
| CRM_CUST_TC_003 | REQ-CUST-03, REQ-CUST-04, REQ-CUST-05, REQ-CUST-06, REQ-CUST-10, REQ-CUST-11 | CUST | Medium | Bảng danh sách đúng cấu trúc cột và trạng thái mặc định | `P-ADMIN`, đang ở `/admin/clients`, trang vừa nạp, **chưa** bấm gì trên bảng | 1. Nhìn hàng nút phía trên bảng<br>2. Nhìn hàng tiêu đề cột<br>3. Nhìn dòng trạng thái và thanh phân trang dưới bảng<br>4. Đối chiếu lần lượt từng mục trong Bảng kiểm | — (chỉ quan sát) | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-05, 10) Phía trên bảng, từ trái sang: ô chọn số dòng đang hiện `25` · nút `Export` · nút `Bulk Actions` · nút biểu tượng mũi tên xoay (nạp lại)<br>`2` (REQ-11) Góc phải phía trên bảng có ô tìm kiếm với chữ mờ gợi ý `Search...` và biểu tượng kính lúp — ô hẹp nên thường chỉ thấy `Search..` (bị cắt), chấm **đạt** cả hai cách hiển thị<br>`3` (REQ-03) Tiêu đề cột theo đúng thứ tự: ô vuông chọn tất cả · `#` · `Company` · `Primary Contact` · `Primary Email` · `Phone` · `Active` · `Groups` · `Date Created`<br>`4` (REQ-04) Cột `Company` có biểu tượng mũi tên hướng lên (đang sắp tăng dần); cột ô vuông đầu tiên **không** có biểu tượng sắp xếp<br>`5` (REQ-06) Góc dưới trái có dòng dạng `Showing 1 to 25 of {tổng} entries`, `{tổng}` có dấu phẩy phân cách hàng nghìn (VD `1,571`)<br>`6` (REQ-06) Góc dưới phải: `Previous` (mờ, không bấm được ở trang 1) · ô trang `1` tô xanh · các số `2 3 4 5` · `…` · số trang cuối · `Next` · ô chọn trang đang hiện `1` | Medium | Yes | UI | @Smoke, @UI, @Customers |
| CRM_CUST_TC_004 | REQ-CUST-02, REQ-CUST-06, REQ-CUST-09 | CUST | Medium | Dữ liệu trong bảng hiển thị đúng định dạng và rê chuột hiện thao tác nhanh | `P-ADMIN`, đang ở `/admin/clients`. Có sẵn `KH-TEST` (Company `auto_cust_tc004_202609191000`, Phone `0901234567`) | 1. Gõ `auto_cust_tc004_202609191000` vào ô tìm kiếm của bảng<br>2. Đợi dòng trạng thái đổi thành `Showing 1 to 1 of 1 entries …`<br>3. Nhìn dòng kết quả, **chưa** rê chuột vào<br>4. Rê chuột lên dòng đó<br>5. Đối chiếu Bảng kiểm<br>6. Dọn dữ liệu theo `D1` | Company: `auto_cust_tc004_202609191000` | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-06) Cột `Date Created` dạng `DD-MM-YYYY HH:MM:SS` (VD `19-09-2026 10:00:12`), là ngày giờ tạo bản ghi<br>`2` Cột `Company` là liên kết chữ xanh; cột `Phone` là liên kết chữ xanh `0901234567`<br>`3` Cột `Active` là công tắc gạt **đang bật** (nền xanh, nút tròn nằm bên phải)<br>`4` (REQ-09) Khi **chưa** rê chuột: dưới tên công ty **không** thấy dòng liên kết thao tác nào<br>`5` (REQ-09) Khi rê chuột: ngay dưới tên công ty hiện dòng `View` \| `Contacts` \| `Delete` — `View` và `Contacts` chữ xanh, `Delete` chữ **đỏ**<br>`6` (REQ-02) Nạp lại trang: số `Total Customers` vẫn bằng `Active Customers` + `Inactive Customers` | Medium | Yes | UI | @Smoke, @UI, @Customers |

---

## Nhóm B — Biểu mẫu thêm khách hàng: mở, giao diện, trạng thái mặc định

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_005 | REQ-CUST-23, REQ-CUST-24, REQ-CUST-25, REQ-CUST-26, REQ-CUST-27 | CUST | High | Biểu mẫu thêm khách hàng đủ thành phần, đúng thứ tự, con trỏ đặt sẵn ở Company | `P-ADMIN`. Mở `/admin/clients/client`, trang vừa nạp, **không** bấm chuột vào đâu | 1. Mở `/admin/clients/client`, KHÔNG bấm chuột<br>2. Gõ ngay `abc` (không bấm vào ô nào trước)<br>3. Đối chiếu lần lượt từng mục trong Bảng kiểm | Chuỗi gõ ở bước 2: `abc` | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-24) Đầu biểu mẫu có đúng 2 tab: `Customer Details` (đang chọn, chữ xanh, gạch chân) · `Billing & Shipping`. **Không** có tab `Customer Admins`<br>`2` (REQ-26) Chữ `abc` ở bước 2 xuất hiện **trong ô Company** — con trỏ nằm sẵn ở đó, viền ô sáng xanh<br>`3` (REQ-27) Từ trên xuống: `Company` · `VAT Number` · `Phone` · `Website` · `Groups` · hàng đôi `Currency` (trái) + `Default Language` (phải) · `Address` (ô nhiều dòng) · `City` · `State` · `Zip Code` · `Country`<br>`4` (REQ-25) Chỉ nhãn `Company` có dấu `*` đỏ phía trước. **Không** nhãn nào khác có dấu `*`<br>`5` (REQ-23) Chân biểu mẫu góc phải có 2 nút: `Save and create contact` (nền trắng) bên trái · `Save` (nền xanh) bên phải<br>`6` (REQ-23) Tab trình duyệt ghi `Add new customer`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `input#company` mang `autofocus="1"`; biểu mẫu `form.client-form` gửi `POST` về `/admin/clients/client` | Critical | Yes | UI | @Smoke, @CriticalPath, @UI, @TechCheck, @Customers |
| CRM_CUST_TC_006 | REQ-CUST-28, REQ-CUST-30, REQ-CUST-31, REQ-CUST-32, REQ-CUST-84 | CUST | Medium | Các ô chọn trên biểu mẫu thêm mới ở đúng trạng thái mặc định | `P-ADMIN`, đang ở `/admin/clients/client`, trang vừa nạp | 1. Nhìn các ô chọn, **chưa** mở ô nào; rê chuột lên dấu hỏi cạnh nhãn `Currency`<br>2. Mở ô `Currency`, đọc danh sách, đóng lại<br>3. Mở ô `Country`, đọc mục đầu và mục cuối danh sách, đóng lại<br>4. Đối chiếu Bảng kiểm | — (chỉ quan sát, không lưu) | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-28) Ô `Groups` hiện chữ mờ `Nothing selected`, bên phải có nút `+` riêng<br>`2` (REQ-30, 84) Ô `Currency` hiện `System Default` và **chọn được**; cạnh nhãn có biểu tượng dấu hỏi tròn, rê chuột hiện chú giải nguyên văn `If the customer use other currency then the base currency make sure you select the appropriate currency for this customer. Changing the currency is not possible after transactions are recorded.`<br>`3` (REQ-30) Mở `Currency` thấy đúng 3 lựa chọn: `System Default` · `USD` · `EUR`<br>`4` (REQ-31) Ô `Default Language` hiện `System Default`<br>`5` (REQ-32) Ô `Country` hiện chữ mờ `Nothing selected`; mở ra thấy danh sách theo bảng chữ cái, mục quốc gia đầu tiên `Afghanistan`, mục cuối `Zimbabwe`, có mục `Vietnam`<br>`6` Mọi ô nhập chữ (`Company`, `VAT Number`, `Phone`, `Website`, `Address`, `City`, `State`, `Zip Code`) đều **rỗng**, không có chữ gợi ý<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): số `<option>` — `groups_in[]` 305 · `default_currency` 3 · `default_language` 27 · `country` 251 (kể cả option rỗng); `Vietnam` = `243` | Medium | Yes | UI | @Smoke, @UI, @TechCheck, @Customers |
| CRM_CUST_TC_007 | REQ-CUST-35, REQ-CUST-36, REQ-CUST-39 | CUST | Medium | Tab Billing & Shipping hiển thị đủ hai khối địa chỉ và chú giải | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Bấm tab `Billing & Shipping`<br>2. Nhìn hai khối địa chỉ<br>3. Rê chuột lên biểu tượng dấu hỏi tròn cạnh tiêu đề `Shipping Address`<br>4. Đối chiếu Bảng kiểm | — (chỉ quan sát) | **Bảng kiểm — mọi mục phải đạt:**<br>`1` Tab `Billing & Shipping` chuyển sang đang chọn (chữ xanh, gạch chân); tab `Customer Details` về chữ đen<br>`2` (REQ-35) Khối trái tiêu đề `Billing Address`, cùng hàng bên phải có liên kết `Same as Customer Info`; dưới là 5 ô theo thứ tự `Street` (ô nhiều dòng) · `City` · `State` · `Zip Code` · `Country`<br>`3` (REQ-36) Khối phải tiêu đề `Shipping Address` (có biểu tượng dấu hỏi phía trước), cùng hàng bên phải có liên kết `Copy Billing Address`; 5 ô cùng thứ tự như khối trái<br>`4` Hai ô `Country` đều hiện chữ mờ `Nothing selected`; mọi ô còn lại rỗng<br>`5` (REQ-39) Rê chuột lên dấu hỏi hiện chú giải nguyên văn: `Do not fill shipping address information if you won't use shipping address on customer invoices`<br>`6` Hai nút `Save and create contact` · `Save` vẫn ở chân biểu mẫu | Medium | Yes | UI | @Smoke, @UI, @Customers |
| CRM_CUST_TC_008 | REQ-CUST-23 | CUST | High | Mở biểu mẫu thêm khách hàng bằng mọi lối vào | `P-ADMIN`. Chạy lại từ bước 1 cho **từng** biến thể | 1. Mở biểu mẫu theo lối vào của biến thể<br>2. Đọc URL và chữ trên tab trình duyệt<br>3. Nhìn biểu mẫu | **Bảng biến thể:**<br>`a` Ở `/admin/clients` bấm nút `+ New Customer`<br>`b` Gõ thẳng `https://crm.anhtester.com/admin/clients/client` | **Mọi biến thể cho cùng kết quả:**<br>2. Thanh địa chỉ dừng ở `https://crm.anhtester.com/admin/clients/client` · tab trình duyệt ghi `Add new customer`<br>3. Biểu mẫu rỗng, tab `Customer Details` đang chọn, con trỏ nằm sẵn trong ô `Company` | Critical | Yes | UI | @Smoke, @CriticalPath, @Customers |

---

## Nhóm C — Tạo khách hàng: nhập hợp lệ, lưu, đối chiếu dữ liệu

> 🧹 Mọi TC ở nhóm này **tạo bản ghi thật** trên môi trường dùng chung → bước cuối luôn là dọn dữ liệu theo `D1`. Không dọn là vi phạm `RISK-CUST-02`.

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_009 | REQ-CUST-25, REQ-CUST-33, REQ-CUST-56 | CUST | High | Tạo khách hàng chỉ với trường bắt buộc Company thì lưu thành công và mở hồ sơ vừa tạo | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Nhập ô `Company`: `auto_cust_tc009_202609191010`<br>2. Để trống mọi ô khác<br>3. Bấm `Save`<br>4. Đợi trang chuyển xong, đọc URL, tab trình duyệt và tiêu đề trang<br>5. Nhìn ô `Company` trên biểu mẫu hồ sơ<br>6. Dọn dữ liệu theo `D1` | Company: `auto_cust_tc009_202609191010`<br>Mọi ô khác: để trống | 3. **Không** hiện thông báo lỗi nào dưới ô nào<br>4. URL dạng `https://crm.anhtester.com/admin/clients/client/{id}` với `{id}` là số mới · tab trình duyệt ghi `auto_cust_tc009_202609191010` · tiêu đề trang góc trái dạng `#{id} auto_cust_tc009_202609191010` (có thể bị rút gọn bằng `…`)<br>5. Ô `Company` hiện đúng `auto_cust_tc009_202609191010`<br>6. Tìm lại tên này ở danh sách → `No matching records found` | Critical | Yes | UI | @Smoke, @CriticalPath, @Customers |
| CRM_CUST_TC_010 | REQ-CUST-27, REQ-CUST-33, REQ-CUST-50 | CUST | High | Tạo khách hàng với đầy đủ mọi trường rồi mở lại thấy đúng toàn bộ dữ liệu đã nhập | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Ở tab `Customer Details` nhập đủ 12 trường theo Test Data<br>2. Bấm tab `Billing & Shipping`, nhập đủ 10 trường theo Test Data<br>3. Bấm `Save`<br>4. Ở hồ sơ vừa mở, đối chiếu từng ô tab `Customer Details`<br>5. Bấm tab `Billing & Shipping`, đối chiếu từng ô<br>6. Dọn dữ liệu theo `D1` | **Customer Details:** Company `auto_cust_tc010_202609191015` · VAT Number `VAT-TC010-0312345678` · Phone `0901234567` · Website `https://auto-cust-tc010.test` · Groups: chọn 1 nhóm bất kỳ, ghi lại tên · Currency `USD` · Default Language `Vietnamese` · Address `123 Auto Recon Street` · City `Da Nang` · State `Hai Chau` · Zip Code `550000` · Country `Vietnam`<br>**Billing:** Street `45 Billing Street` · City `Ha Noi` · State `Ba Dinh` · Zip Code `100000` · Country `Vietnam`<br>**Shipping:** Street `67 Shipping Road` · City `Ho Chi Minh` · State `Quan 1` · Zip Code `700000` · Country `Vietnam` | 3. Lưu thành công, chuyển sang `/admin/clients/client/{id}`<br>4. Cả 12 ô hiện **đúng từng ký tự** đã nhập; `Currency` hiện `USD`, `Default Language` hiện `Vietnamese`, `Country` hiện `Vietnam`, `Groups` hiện đúng tên nhóm đã chọn<br>5. 10 ô billing/shipping hiện đúng giá trị đã nhập, **không** bị tráo giữa hai khối<br>6. Khách hàng không còn trong danh sách | Critical | Yes | UI | @Smoke, @CriticalPath, @Customers |
| CRM_CUST_TC_011 | REQ-CUST-11, REQ-CUST-33, REQ-CUST-56 | CUST | High | Khách hàng vừa tạo hiện đúng ở danh sách, mặc định đang hoạt động | `P-ADMIN`, đang ở `/admin/clients` | 1. Ghi lại số `Total Customers` và `Active Customers` ở khối `Customers Summary`<br>2. Bấm `+ New Customer`, nhập Company và Phone theo Test Data, bấm `Save`, rồi mở lại `/admin/clients`<br>3. Gõ `auto_cust_tc011_202609191020` vào ô tìm kiếm của bảng<br>4. Nhìn dòng kết quả<br>5. Đọc lại khối `Customers Summary`<br>6. Dọn dữ liệu theo `D1` | Company: `auto_cust_tc011_202609191020`<br>Phone: `0912345678` | 4. Đúng 1 dòng: `Company` = `auto_cust_tc011_202609191020` · `Phone` = `0912345678` · `Primary Contact` và `Primary Email` **trống** (chưa có liên hệ) · `Groups` trống · công tắc `Active` **đang bật** · `Date Created` là ngày giờ hôm nay<br>5. `Total Customers` và `Active Customers` đều **tăng đúng 1** so với số ghi ở bước 1 (nếu môi trường có người khác tạo cùng lúc thì chênh lệch có thể lớn hơn — ghi chú vào báo cáo, không kết luận FAIL) | Critical | Yes | UI | @Smoke, @CriticalPath, @Customers |
| CRM_CUST_TC_012 | REQ-CUST-34 | CUST | High | Bấm "Save and create contact" thì tạo khách hàng và mở ngay hộp thoại thêm liên hệ | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Nhập `Company`: `auto_cust_tc012_202609191025`<br>2. Bấm `Save and create contact`<br>3. Đợi trang chuyển xong, đọc URL<br>4. Nhìn hộp thoại đang mở<br>5. Bấm nút `X` đóng hộp thoại, **không** tạo liên hệ<br>6. Dọn dữ liệu theo `D1` | Company: `auto_cust_tc012_202609191025` | 3. URL dạng `/admin/clients/client/{id}?group=contacts&new_contact=true` · tab `Contacts` ở cột trái đang chọn<br>4. Hộp thoại tự mở, tiêu đề `Add new contact`, ngay dưới tiêu đề là tên công ty `auto_cust_tc012_202609191025`; có các ô `Profile image` · `* First Name` · `* Last Name` · `Position` · `* Email` · `Phone` · `Direction` · `* Password`<br>5. Hộp thoại đóng, khách hàng vẫn đã được tạo (tiêu đề trang dạng `#{id} auto_cust_tc012_…`)<br><br>ℹ️ Nội dung biểu mẫu liên hệ thuộc module `CONT` — TC này chỉ chấm tới việc hộp thoại **tự mở** | High | Yes | UI | @Smoke, @Customers |

---

## Nhóm D — Hồ sơ khách hàng và màn hình Nhập CSV: mở, giao diện

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_013 | REQ-CUST-47, REQ-CUST-48, REQ-CUST-49, REQ-CUST-51, REQ-CUST-52 | CUST | High | Hồ sơ khách hàng (Admin) có đủ 19 tab và biểu mẫu sửa đúng cấu trúc | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc013_202609191030`, Website `https://auto-cust-tc013.test` | 1. Mở hồ sơ của `KH-TEST`<br>2. Đọc tiêu đề góc trên trái và tiêu đề vùng phải<br>3. Đếm và đọc lần lượt các tab ở cột trái từ trên xuống<br>4. Nhìn biểu mẫu tab `Profile`<br>5. Đối chiếu Bảng kiểm<br>6. Dọn dữ liệu theo `D1` | Company: `auto_cust_tc013_202609191030`<br>Website: `https://auto-cust-tc013.test` | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-47) Góc trên trái dạng `#{id} auto_cust_tc013_…` kèm mũi tên xổ xuống; vùng phải có tiêu đề `Profile`<br>`2` (REQ-48) Cột trái có đúng **19** tab theo thứ tự: `Profile` · `Contacts` · `Notes` · `Statement` · `Invoices` · `Payments` · `Proposals` · `Credit Notes` · `Estimates` · `Subscriptions` · `Expenses` · `Contracts` · `Projects` · `Tasks` · `Tickets` · `Files` · `Vault` · `Reminders` · `Map`; `Profile` đang tô xanh<br>`3` (REQ-49) Biểu mẫu có đúng 3 tab: `Customer Details` (đang chọn) · `Billing & Shipping` · `Customer Admins`<br>`4` (REQ-52) Ô `Website` có nút biểu tượng quả cầu dính liền bên phải (ở biểu mẫu thêm mới **không** có nút này)<br>`5` (REQ-51) Chân biểu mẫu chỉ có **1** nút `Save` (nền xanh) — **không** có `Save and create contact`<br>`6` Khách hàng chưa có liên hệ: **không** thấy ô tích `Show primary contact full name…` phía trên ô `Company` | High | Yes | UI | @Smoke, @UI, @Customers |
| CRM_CUST_TC_014 | REQ-CUST-09, REQ-CUST-47 | CUST | High | Mở hồ sơ khách hàng bằng mọi lối vào | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc014_202609191035`, ghi lại `{id}`. Chạy lại từ bước 1 cho **từng** biến thể | 1. Mở hồ sơ theo lối vào của biến thể<br>2. Đọc URL, tab trình duyệt, tiêu đề trang<br>3. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` Ở danh sách, tìm tên rồi bấm vào **tên công ty**<br>`b` Ở danh sách, tìm tên, rê chuột lên dòng, bấm `View`<br>`c` Gõ thẳng `https://crm.anhtester.com/admin/clients/client/{id}` | **Mọi biến thể cho cùng kết quả:**<br>2. URL `…/admin/clients/client/{id}` · tab trình duyệt ghi `auto_cust_tc014_202609191035` · tiêu đề `#{id} auto_cust_tc014_…` · tab `Profile` đang mở | High | Yes | UI | @Smoke, @Customers |
| CRM_CUST_TC_015 | REQ-CUST-72, REQ-CUST-73 | CUST | Medium | Mở màn hình Nhập khách hàng và đọc đủ 4 hướng dẫn nguyên văn | `P-ADMIN`. Chạy bước 1 cho **từng** biến thể | 1. Mở màn hình theo lối vào của biến thể<br>2. Đọc URL, tab trình duyệt, tiêu đề khối<br>3. Đọc khối hướng dẫn nền xanh nhạt ở đầu trang | **Bảng biến thể lối vào:**<br>`a` Ở `/admin/clients` bấm `Import Customers`<br>`b` Gõ thẳng `https://crm.anhtester.com/admin/clients/import` | **Mọi biến thể cho cùng kết quả:**<br>2. URL `…/admin/clients/import` · tab trình duyệt ghi `Import` · tiêu đề khối `Import Customers`<br>3. Đúng 4 mục đánh số, nguyên văn:<br>`1.` `Your CSV data should be in the format below. The first line of your CSV file should be the column headers as in the table example. Also make sure that your file is UTF-8 to avoid unnecessary encoding problems.`<br>`2.` `If the column you are trying to import is date make sure that is formatted in format Y-m-d ({ngày hôm nay}).` — `{ngày hôm nay}` là **ngày chạy TC** dạng năm-tháng-ngày (chạy 19-09-2026 thì hiện `2026-09-19`). ⚠️ **KHÔNG** so với ngày cố định trong ảnh evidence<br>`3.` `Make sure you configure the default contact permission in Setup->Settings->Customers to get the best results like auto assigning contact permissions and email notification settings based on the permission.`<br>`4.` `Duplicate email rows won't be imported.` — mục này **chữ đỏ**, ba mục trên chữ xanh | Medium | Yes | UI | @Smoke, @UI, @Customers |
| CRM_CUST_TC_016 | REQ-CUST-74, REQ-CUST-75, REQ-CUST-77, REQ-CUST-78 | CUST | Medium | Biểu mẫu nhập CSV và bảng cột mẫu hiển thị đủ, đúng thứ tự | `P-ADMIN`, đang ở `/admin/clients/import`, trang vừa nạp | 1. Đọc hàng tiêu đề bảng mẫu từ trái sang phải (kéo thanh cuộn ngang của bảng để đọc hết)<br>2. Nhìn biểu mẫu dưới bảng<br>3. Gõ `MatKhau@2026` vào ô `Default password for all contacts` rồi quan sát, sau đó xoá đi<br>4. Đối chiếu Bảng kiểm | Chuỗi gõ ở bước 3: `MatKhau@2026` (không bấm Import) | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-74) Bảng mẫu có đúng **27** cột theo thứ tự: `Firstname` · `Lastname` · `Email` · `Contact phonenumber` · `Position` · `Company` · `Vat` · `Phonenumber` · `Country` · `City` · `Zip` · `State` · `Address` · `Website` · `Billing street` · `Billing city` · `Billing state` · `Billing zip` · `Billing country` · `Shipping street` · `Shipping city` · `Shipping state` · `Shipping zip` · `Shipping country` · `Longitude` · `Latitude` · `Stripe id`<br>`2` (REQ-74) Đúng 4 cột có dấu `*` đỏ: `Firstname` · `Lastname` · `Email` · `Company`; 5 cột đầu có dòng phụ chữ xanh `Contact field`<br>`3` (REQ-75) Góc phải phía trên bảng có nút `Download Sample` nền **xanh lá**<br>`4` (REQ-77) Dưới bảng theo thứ tự: `* Choose CSV File` (nút `Choose File` + chữ `No file chosen`) · `Groups` (hiện `Nothing selected`, có nút `+`) · `Default password for all contacts` (ô rỗng)<br>`5` (REQ-77) Gõ vào ô mật khẩu mặc định thì **hiện rõ chữ** `MatKhau@2026`, không bị che bằng dấu chấm — ghi nhận hiện trạng (xem `CRM_CUST_TC_106`)<br>`6` (REQ-78) Cuối biểu mẫu có đúng 2 nút nền xanh: `Import` · `Simulate Import` | Medium | Yes | UI | @Smoke, @UI, @Customers |
