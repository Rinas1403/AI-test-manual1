# Test Cases — `CUST` · Web · Part 03 — Vòng 2 · Biểu mẫu khách hàng: bắt buộc, kiểm tra dữ liệu, địa chỉ, sửa hồ sơ (36 TC)

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_CUSTOMERS_SUMMARY.md](../../TEST_CASES_CUSTOMERS_SUMMARY.md) — cách đọc · quy ước `P-*`, `KH-TEST`, `D1` · Assumptions · Bảng đối soát |
| **Nguồn requirement** | [requirements_customers_web.md](../../../../requirements/customers/web/requirements_customers_web.md) — mục 3.4 · 3.5 · 3.6 · 3.7 · 4.1 · 4.2 · 5 |
| **Nền tảng** | Web — Google Chrome, viewport desktop `1600×750` |
| **Dải TC ID trong part** | `CRM_CUST_TC_039` → `CRM_CUST_TC_073` · `CRM_CUST_TC_129` (bổ sung 19-09-2026, đặt cạnh nhóm ô chọn) |
| **Part liền kề** | ← [part_02_web_danh_sach.md](part_02_web_danh_sach.md) · → [part_04_web_trang_thai_tab_nhap.md](part_04_web_trang_thai_tab_nhap.md) |

> 📏 **Quy tắc trường đã chốt** (`AMB-CUST-05`, `AMB-CUST-06` — PO 19-09-2026):
> - Chỉ `Company` bắt buộc. Mọi trường khác tuỳ chọn
> - `VAT Number`, `Phone`, `Website`, `Zip Code` **không** kiểm định dạng — nhập gì cũng lưu nguyên văn
> - Trường chữ giới hạn **255** ký tự theo cơ sở dữ liệu — **chưa kiểm chứng**, TC biên gắn `@NeedsVerify`
>
> 🔁 **Dùng lại một khách hàng test cho nhiều biến thể:** các TC kiểm một trường tuỳ chọn (`TC_050` → `TC_056`) tạo **một** `KH-TEST`, mỗi biến thể: mở hồ sơ → sửa đúng một ô → `Save` → nạp lại trang → đọc ô đó. Dọn `D1` sau biến thể cuối.

---

## Nhóm I — Trường bắt buộc Company

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_039 | REQ-CUST-40, REQ-CUST-41 | CUST | High | Bỏ trống Company rồi bấm Save thì bị chặn, báo lỗi ngay tại ô và nhãn tab đổi đỏ | `P-ADMIN`. Ghi lại `Total Customers` ở `/admin/clients`, rồi mở `/admin/clients/client`, tab `Customer Details`. Nạp lại trang trước **mỗi** biến thể | 1. Làm ô `Company` rỗng theo biến thể<br>2. Nhập `VAT Number`: `VAT-TC039`<br>3. Bấm `Save`<br>4. Quan sát ô Company, nhãn của nó và nhãn tab<br>5. Đọc URL và ô VAT Number, rồi mở `/admin/clients` ở tab mới, đọc `Total Customers` (so với số ghi trước khi chạy) | **Bảng biến thể:**<br>`a` Không nhập gì vào `Company`<br>`b` Gõ `abc` vào `Company` rồi xoá hết | **Mọi biến thể cho cùng kết quả:**<br>3. Trang **không** chuyển đi<br>4. Ngay dưới ô `Company` hiện chữ đỏ `This field is required.` · viền ô chuyển đỏ · nhãn `* Company` chuyển đỏ · nhãn tab `Customer Details` chuyển **chữ đỏ**<br>5. URL vẫn là `…/admin/clients/client` · `Total Customers` **không** tăng · chữ `VAT-TC039` vẫn còn trong ô VAT Number (không mất dữ liệu đã nhập)<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): **không** phát sinh request `POST /admin/clients/client`; khối chứa Company có class `has-error`; màu nhãn tab `rgb(255, 0, 0)` | Critical | Yes | UI | @Regression, @CriticalPath, @Required, @TechCheck, @Customers |
| CRM_CUST_TC_040 | REQ-CUST-43 | CUST | High | 🐞 Company chỉ gồm khoảng trắng phải bị chặn như ô rỗng | `P-ADMIN`, đang ở `/admin/clients/client`. Ghi lại `Total Customers` trước khi chạy | 1. Nhập ô `Company` đúng 3 dấu cách<br>2. Bấm ra ngoài ô Company<br>3. Bấm `Save`<br>4. Quan sát ô Company và URL | Company: `···` (3 dấu cách — ký hiệu `·` = 1 dấu cách) | 3. Trang **không** chuyển đi<br>4. Dưới ô `Company` hiện `This field is required.`, viền ô đỏ · **không** tạo khách hàng · `Total Customers` không đổi<br><br>🐞 **Hiện trạng: FAIL** — hệ thống chấp nhận và tạo khách hàng "tên rỗng" (khảo sát 14-08-2026). PO xác nhận là **lỗi** (`AMB-CUST-02`). FAIL là kết quả **đúng** → mở bug, **KHÔNG** sửa TC.<br>⚠️ Nếu hệ thống tạo được bản ghi: **bắt buộc** xoá ngay bản ghi rỗng đó (tìm theo `Date Created` vừa tạo) — không để lại bản ghi không tên trên môi trường dùng chung<br>ℹ️ Ở bước 2 có thể hiện dải xanh `It looks that a customer with name already exists…` (ảnh `cust_new_form_duplicate_name_warning_fullpage.png`) — dấu hiệu môi trường **đã có** bản ghi tên rỗng do lỗi này | High | Yes | UI | @Regression, @Required, @KnownBug, @Customers |
| CRM_CUST_TC_041 | REQ-CUST-42 | CUST | High | 🐞 Lỗi bắt buộc nằm ở tab đang ẩn thì hệ thống phải tự chuyển về tab chứa lỗi | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Để trống `Company`<br>2. Bấm tab `Billing & Shipping`<br>3. Bấm `Save`<br>4. Quan sát tab nào đang mở và có nhìn thấy thông báo lỗi không | Company: để trống | 3. Không lưu, trang không chuyển đi<br>4. Tab đang mở **tự chuyển sang** `Customer Details` · thông báo `This field is required.` dưới ô Company **nhìn thấy được** ngay, không phải bấm thêm gì<br><br>🐞 **Hiện trạng: FAIL** — vẫn đứng ở tab `Billing & Shipping`, chỉ nhãn tab `Customer Details` đổi đỏ, thông báo lỗi nằm ở tab ẩn (ảnh `cust_new_form_required_error_hidden_tab_fullpage.png`). PO chốt là **lỗi trải nghiệm** (`AMB-CUST-03`) → mở bug, **KHÔNG** sửa TC | High | Yes | UI | @Regression, @Required, @KnownBug, @Customers |
| CRM_CUST_TC_042 | REQ-CUST-40 | CUST | Medium | Thông báo lỗi bắt buộc tự biến mất khi nhập lại Company | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Để trống `Company`, bấm `Save`<br>2. Gõ `auto_cust_tc042_202609191125` vào ô `Company`<br>3. Quan sát thông báo lỗi và màu nhãn<br>4. Bấm `Save`<br>5. Dọn dữ liệu theo `D1` | Company: `auto_cust_tc042_202609191125` | 1. Hiện `This field is required.`<br>3. ⚠️ chưa có evidence — kỳ vọng: thông báo đỏ biến mất, viền ô và nhãn tab trở lại màu thường ngay khi có chữ (không cần bấm Save)<br>4. Lưu thành công, mở hồ sơ `#{id} auto_cust_tc042_…` | Medium | Yes | UI | @Regression, @UIBehavior, @NeedsVerify, @Customers |

---

## Nhóm J — Kiểm tra dữ liệu trường Company và cảnh báo trùng tên

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_043 | REQ-CUST-25, REQ-CUST-33 | CUST | High | Company chứa tiếng Việt, ký tự đặc biệt, emoji, chữ Trung/Nhật được lưu và hiển thị nguyên văn | `P-ADMIN`, đang ở `/admin/clients/client`. Chạy lại cho **từng** biến thể | 1. Nhập `Company` theo biến thể<br>2. Bấm `Save`<br>3. Đọc tiêu đề hồ sơ, tab trình duyệt, ô `Company`<br>4. Tìm lại ở danh sách bằng phần hậu tố `tc043`, đọc cột `Company`<br>5. Dọn dữ liệu theo `D1` | **Bảng biến thể:**<br>`a` Tiếng Việt → `Công ty TNHH Thương Mại Ánh Dương tc043a`<br>`b` Ký tự đặc biệt → `Ánh & Cộng sự "A&B" <VN> tc043b`<br>`c` Emoji → `Cửa hàng Hoa 🌸🎉 tc043c`<br>`d` Chữ Trung/Nhật → `株式会社テスト 上海 tc043d` | **Mọi biến thể cho cùng kết quả:**<br>2. Lưu thành công, không lỗi<br>3. Tiêu đề hồ sơ, tab trình duyệt và ô `Company` hiện **đúng từng ký tự** như đã nhập — không thành `?`, `&amp;`, ô vuông hay chữ lỗi mã hoá<br>4. Cột `Company` ở danh sách hiện đúng như vậy | High | Yes | UI | @Regression, @Validation, @Localization, @Customers |
| CRM_CUST_TC_044 | REQ-CUST-25, REQ-CUST-33 | CUST | High | Company chứa mã tiêm HTML/SQL được lưu dạng chữ thường, không bao giờ chạy mã | `P-ADMIN`, đang ở `/admin/clients/client`. Chạy lại cho **từng** biến thể | 1. Nhập `Company` theo biến thể, bấm `Save`<br>2. Quan sát trang hồ sơ vừa mở<br>3. Mở `/admin/clients`, tìm theo `tc044`<br>4. Mở lại hồ sơ, bấm tab `Statement`<br>5. Dọn dữ liệu theo `D1` | **Bảng biến thể:**<br>`a` `<script>alert('tc044a')</script>`<br>`b` `<img src=x onerror=alert('tc044b')>`<br>`c` `' OR 1=1-- tc044c` | **Mọi biến thể cho cùng kết quả:**<br>2. **Không** hộp thoại `alert` nào bật lên · tiêu đề hồ sơ và ô `Company` hiện **nguyên văn chuỗi** đã nhập như chữ thường (thấy cả dấu `<`, `>`)<br>3. Danh sách hiện đúng 1 dòng có tên nguyên văn, không hộp thoại, không vỡ bố cục bảng · `c` không làm danh sách trả về toàn bộ khách hàng<br>4. Dòng `Customer Statement For …` hiện nguyên văn, không hộp thoại | Critical | Yes | UI | @Regression, @Validation, @Security, @Customers |
| CRM_CUST_TC_045 | REQ-CUST-25 | CUST | High | Company tại giá trị biên độ dài 1 · 254 · 255 ký tự được lưu nguyên vẹn | `P-ADMIN`, đang ở `/admin/clients/client`. Chạy lại cho **từng** biến thể | 1. Nhập `Company` theo biến thể (dán chuỗi đã chuẩn bị sẵn)<br>2. Bấm `Save`<br>3. Ở hồ sơ, bấm vào ô `Company`, bấm phím `End`, so ký tự cuối với chuỗi gốc<br>4. Ghi lại `{id}` từ tiêu đề<br>5. Dọn dữ liệu theo `D1` (tìm bằng `{id}` ở cột `#` nếu tên quá ngắn) | **Bảng biến thể:**<br>`a` 1 ký tự → `Q`<br>`b` 254 ký tự → `auto_cust_tc045b_` + chữ `x` lặp lại cho đủ 254 ký tự, ký tự cuối đổi thành `Z`<br>`c` 255 ký tự → `auto_cust_tc045c_` + chữ `x` lặp lại cho đủ 255 ký tự, ký tự cuối đổi thành `Z` | **Mọi biến thể cho cùng kết quả:**<br>2. Lưu thành công, không lỗi<br>3. Ô `Company` chứa **đủ** chuỗi, ký tự cuối là `Z` (`b`,`c`) / đúng `Q` (`a`) — không bị cắt bớt<br><br>⚠️ Mốc 255 theo giả định `AMB-CUST-05`, **chưa kiểm chứng** → `@NeedsVerify`. Biến thể `c` bị cắt = giới hạn thật nhỏ hơn 255 → ghi lại độ dài lưu được, báo để sửa Đặc tả trường | High | Yes | UI | @Regression, @Validation, @Boundary, @NeedsVerify, @Customers |
| CRM_CUST_TC_046 | REQ-CUST-25 | CUST | Medium | Company dài 256 ký tự — vượt giới hạn một ký tự | `P-ADMIN`, đang ở `/admin/clients/client`, ghi lại `Total Customers` | 1. Nhập `Company` 256 ký tự<br>2. Bấm `Save`<br>3. Quan sát trang<br>4. Nếu lưu được: ở hồ sơ bấm vào ô `Company`, phím `End`, đọc ký tự cuối; ghi lại `{id}`<br>5. Dọn dữ liệu theo `D1` nếu có bản ghi | Company: `auto_cust_tc046_` + chữ `x` lặp lại cho đủ 256 ký tự, ký tự cuối đổi thành `Z` | 3. Kỳ vọng tối thiểu: **không** hiện trang lỗi hệ thống / trang trắng<br>4. ⚠️ Chưa kiểm chứng (`AMB-CUST-05`). Chấp nhận một trong hai: (i) bị chặn kèm thông báo rõ ràng, hoặc (ii) lưu được **nhưng** hệ thống phải báo đã cắt. **FAIL** nếu lưu âm thầm mà ký tự cuối không còn là `Z` (mất dữ liệu không báo). Ghi lại độ dài thực tế lưu được | Medium | Partial | UI | @Regression, @Validation, @Boundary, @NeedsVerify, @Customers |
| CRM_CUST_TC_047 | REQ-CUST-25, REQ-CUST-43 | CUST | Medium | Company có khoảng trắng thừa ở đầu và cuối | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Nhập `Company` theo Test Data<br>2. Bấm `Save`<br>3. Đọc tiêu đề hồ sơ; bấm vào ô `Company`, phím `Home` rồi `End`, xem có khoảng trắng thừa không<br>4. Ở danh sách, tìm bằng `auto_cust_tc047_202609191140` (không khoảng trắng)<br>5. Dọn dữ liệu theo `D1` | Company: `··auto_cust_tc047_202609191140··` (2 dấu cách mỗi đầu, `·` = 1 dấu cách) | 2. Lưu thành công<br>3. ⚠️ Chưa có evidence. Kỳ vọng theo tinh thần `REQ-CUST-43` (cắt khoảng trắng đầu/cuối): ô chỉ còn `auto_cust_tc047_202609191140`. Ghi lại nếu khoảng trắng còn nguyên<br>4. Tìm ra đúng 1 dòng | Medium | Yes | UI | @Regression, @Validation, @NeedsVerify, @Customers |
| CRM_CUST_TC_048 | REQ-CUST-44 | CUST | High | Rời ô Company với tên đã tồn tại thì hiện cảnh báo tham khảo kèm đúng tên; tên chưa tồn tại thì không | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc048_202609191145`. Đang ở `/admin/clients/client` | 1. Nhập `Company`: `auto_cust_tc048_chua_ton_tai_202609191145`<br>2. Bấm phím `Tab` để rời ô<br>3. Quan sát vùng dưới ô Company<br>4. Xoá hết, nhập `auto_cust_tc048_202609191145`, bấm `Tab`<br>5. Đọc dải thông báo xuất hiện<br>6. Rời trang **không** lưu; dọn `KH-TEST` theo `D1` | Tên chưa tồn tại: `auto_cust_tc048_chua_ton_tai_202609191145`<br>Tên đã tồn tại: `auto_cust_tc048_202609191145` | 3. **Không** có dải thông báo nào<br>5. Ngay dưới ô Company hiện dải **nền xanh nhạt** nguyên văn: `It looks that a customer with name auto_cust_tc048_202609191145 already exists, if you still want to create the customer you can ignore this message.` — tên công ty in **đậm**<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): rời ô phát sinh `POST /admin/clients/check_duplicate_customer_name`, phản hồi `{"exists":true,…}` ở bước 4 và `exists:false` ở bước 2 | High | Yes | UI | @Regression, @BusinessRule, @TechCheck, @Customers |
| CRM_CUST_TC_049 | REQ-CUST-45 | CUST | High | Cảnh báo trùng tên không chặn việc lưu — tạo được khách hàng thứ hai cùng tên | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc049_202609191150` | 1. Mở `/admin/clients/client`, nhập `Company`: `auto_cust_tc049_202609191150`, bấm `Tab`<br>2. Đợi dải cảnh báo xanh hiện ra<br>3. Quan sát ô Company<br>4. Bấm `Save`<br>5. Ở danh sách, tìm `auto_cust_tc049_202609191150`<br>6. Dọn **cả 2** khách hàng theo `D1` | Company: `auto_cust_tc049_202609191150` (trùng `KH-TEST`) | 3. Ô Company **không** viền đỏ, không có `This field is required.` — đây chỉ là cảnh báo tham khảo<br>4. Lưu thành công, mở hồ sơ với `{id}` **mới**, khác `{id}` của `KH-TEST`<br>5. Có **2** dòng cùng tên, khác số `#` | High | Yes | UI | @Regression, @BusinessRule, @Customers |

---

## Nhóm K — Kiểm tra dữ liệu các trường tuỳ chọn

> Mỗi TC chỉ kiểm **một** trường. Riêng `CRM_CUST_TC_056` là TC **độ bền** gom 10 ô chữ tự do còn lại — ngoại lệ hợp lệ theo bảng CẤM gộp của skill: mục đích là hệ thống không sập / không chạy mã với dữ liệu bất thường, không phải kiểm quy tắc của từng trường (các ô này không có quy tắc nào).

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_050 | REQ-CUST-27 | CUST | Medium | VAT Number không kiểm định dạng — mọi giá trị đều lưu nguyên văn | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc050_202609191155`. Mỗi biến thể: sửa ô → `Save` → nạp lại | 1. Mở hồ sơ `KH-TEST`<br>2. Nhập `VAT Number` theo biến thể<br>3. Bấm `Save`, nạp lại trang<br>4. Đọc ô `VAT Number`<br>5. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` Chỉ số → `0312345678`<br>`b` Có tiền tố, gạch nối → `VN-0312345678-001`<br>`c` Chữ tiếng Việt, ngoặc → `MST: 0312345678 (CN Đà Nẵng)`<br>`d` Thẻ HTML → `<b>VAT</b>`<br>`e` 255 ký tự → `VAT` + chữ số `9` lặp lại cho đủ 255 ký tự | **Mọi biến thể cho cùng kết quả:**<br>3. Lưu thành công, **không** có thông báo lỗi định dạng<br>4. Ô hiện **nguyên văn** như đã nhập — `d` hiện đủ `<b>VAT</b>` dạng chữ, không in đậm · `e` đủ 255 ký tự (⚠️ mốc 255 chưa kiểm chứng, `AMB-CUST-05`) | Medium | Yes | UI | @Regression, @Validation, @Customers |
| CRM_CUST_TC_051 | REQ-CUST-27 | CUST | Medium | Phone không kiểm định dạng — số, dấu, chữ đều lưu nguyên văn | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc051_202609191200`. Mỗi biến thể: sửa ô → `Save` → nạp lại | 1. Mở hồ sơ `KH-TEST`<br>2. Nhập `Phone` theo biến thể<br>3. Bấm `Save`, nạp lại trang<br>4. Đọc ô `Phone`; ở danh sách đọc cột `Phone` của dòng này<br>5. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` 10 số bắt đầu bằng 0 → `0901234567`<br>`b` Mã quốc gia, khoảng trắng → `+84 901 234 567`<br>`c` Gạch nối → `090-123-4567`<br>`d` Dấu chấm → `0901.234.567`<br>`e` Chữ lẫn số → `abc0901234567`<br>`f` 255 ký tự → chữ số `0` lặp lại 255 lần | **Mọi biến thể cho cùng kết quả:**<br>3. Lưu thành công, **không** có thông báo lỗi định dạng (`AMB-CUST-06`)<br>4. Ô `Phone` và cột `Phone` ở danh sách hiện **nguyên văn** như đã nhập | Medium | Yes | UI | @Regression, @Validation, @Customers |
| CRM_CUST_TC_052 | REQ-CUST-27 | CUST | Medium | Website không kiểm định dạng URL — mọi giá trị đều lưu nguyên văn | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc052_202609191205`. Mỗi biến thể: sửa ô → `Save` → nạp lại | 1. Mở hồ sơ `KH-TEST`<br>2. Nhập `Website` theo biến thể<br>3. Bấm `Save`, nạp lại trang<br>4. Đọc ô `Website`<br>5. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` URL đầy đủ → `https://auto-cust-tc052.test`<br>`b` Thiếu giao thức → `auto-cust-tc052.test`<br>`c` Cổng, đường dẫn, tham số → `http://localhost:8080/a?b=1&c=2`<br>`d` Không phải URL → `khong phai url`<br>`e` 255 ký tự → `https://` + chữ `w` lặp lại cho đủ 255 ký tự | **Mọi biến thể cho cùng kết quả:**<br>3. Lưu thành công, không lỗi định dạng<br>4. Ô hiện **nguyên văn** — `c` giữ đủ `&c=2`, không thành `&amp;c=2` | Medium | Yes | UI | @Regression, @Validation, @Customers |
| CRM_CUST_TC_053 | REQ-CUST-52 | CUST | Low | Nút quả cầu cạnh Website mở trang web của khách hàng | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc053_202609191210`, Website `https://example.com` | 1. Mở hồ sơ `KH-TEST`<br>2. Bấm nút quả cầu dính bên phải ô `Website`<br>3. Quan sát tab trình duyệt<br>4. Đóng tab mới, quay lại hồ sơ<br>5. Dọn dữ liệu theo `D1` | Website: `https://example.com` | 3. Mở **tab mới** tới `https://example.com`; tab hồ sơ vẫn giữ nguyên<br>4. Hồ sơ không đổi, không mất dữ liệu | Low | Yes | UI | @Regression, @UIBehavior, @Customers |
| CRM_CUST_TC_054 | REQ-CUST-27 | CUST | Low | Zip Code là ô chữ tự do — mọi dạng mã bưu chính đều lưu nguyên văn | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc054_202609191215`. Mỗi biến thể: sửa ô → `Save` → nạp lại | 1. Mở hồ sơ `KH-TEST`<br>2. Nhập `Zip Code` theo biến thể<br>3. Bấm `Save`, nạp lại trang, đọc ô<br>4. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` 6 số Việt Nam → `550000`<br>`b` Có chữ và khoảng trắng (Anh) → `SW1A 1AA`<br>`c` Có gạch nối (Mỹ) → `12345-6789`<br>`d` Số 0 đứng đầu → `00501`<br>`e` Chữ thuần → `abc` | **Mọi biến thể cho cùng kết quả:**<br>3. Lưu thành công, ô hiện nguyên văn — `d` **giữ** số `0` đầu (không thành `501`) | Low | Yes | UI | @Regression, @Validation, @Customers |
| CRM_CUST_TC_055 | REQ-CUST-27 | CUST | Medium | Address (ô nhiều dòng) giữ xuống dòng, chữ dài, thẻ HTML và emoji | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc055_202609191220`. Mỗi biến thể: sửa ô → `Save` → nạp lại | 1. Mở hồ sơ `KH-TEST`<br>2. Nhập `Address` theo biến thể (xuống dòng bằng phím `Enter`)<br>3. Bấm `Save`, nạp lại trang, đọc ô `Address`<br>4. Bấm tab `Statement`, đọc khối địa chỉ `To:`<br>5. Dọn dữ liệu theo `D1` (sau biến thể cuối) | **Bảng biến thể:**<br>`a` 3 dòng → `12 Nguyễn Văn Linh` ⏎ `Phường Nam Dương` ⏎ `Quận Hải Châu`<br>`b` 1000 ký tự → `Địa chỉ dài tc055 ` + chữ `a` lặp lại cho đủ 1000 ký tự<br>`c` Thẻ HTML → `<b>đậm</b> <i>nghiêng</i>`<br>`d` Emoji → `Số 12 đường Bạch Đằng 🏢` | **Mọi biến thể cho cùng kết quả:**<br>3. Lưu thành công, ô `Address` hiện nguyên văn — `a` còn đủ 3 dòng · `b` đủ 1000 ký tự · `c` hiện dạng chữ, **không** in đậm/nghiêng<br>4. Khối `To:` hiện nội dung địa chỉ, không hộp thoại, không vỡ bố cục | Medium | Yes | UI | @Regression, @Validation, @Customers |
| CRM_CUST_TC_056 | REQ-CUST-27, REQ-CUST-35, REQ-CUST-36 | CUST | Medium | Độ bền: 10 ô chữ tự do còn lại (City, State, 8 ô địa chỉ thanh toán/giao hàng) chịu được dữ liệu bất thường | `P-ADMIN`, đang ở `/admin/clients/client`. Chạy lại cho **từng** biến thể: tạo khách hàng mới | 1. Nhập `Company`: `auto_cust_tc056{mã biến thể}_202609191225`<br>2. Nhập **cùng** chuỗi của biến thể vào cả 10 ô: `City` · `State` (tab Customer Details) · `Street` · `City` · `State` · `Zip Code` của **Billing** và **Shipping**<br>3. Bấm `Save`<br>4. Nạp lại hồ sơ, đọc lần lượt 10 ô<br>5. Dọn dữ liệu theo `D1` | **Bảng biến thể:**<br>`a` Tiếng Việt → `Thành phố Hồ Chí Minh`<br>`b` Emoji → `Quận 1 🌆`<br>`c` Tiêm mã → `<script>alert('tc056')</script>`<br>`d` Tiêm SQL → `' OR 1=1--`<br>`e` 255 ký tự → chữ `y` lặp lại 255 lần | **Mọi biến thể cho cùng kết quả:**<br>3. Lưu thành công, **không** trang lỗi, **không** hộp thoại `alert`<br>4. **Cả 10 ô** hiện nguyên văn chuỗi đã nhập. Ô nào khác đi → ghi rõ **tên ô** trong báo cáo FAIL (VD `CRM_CUST_TC_056-c FAIL — Shipping City`) | Medium | Yes | UI | @Regression, @Validation, @Security, @Robustness, @Customers |

---

## Nhóm L — Ô chọn: Groups, Currency, Default Language, Country

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_057 | REQ-CUST-28 | CUST | Medium | Ô Groups chọn nhiều: tìm trong danh sách, chọn tất cả, bỏ chọn tất cả, bỏ từng mục | `P-ADMIN`, đang ở `/admin/clients/client` (không lưu) | 1. Bấm ô `Groups`<br>2. Gõ `Platinum` vào ô tìm kiếm trong dropdown<br>3. Xoá chữ tìm kiếm, bấm `Select All`<br>4. Bấm `Deselect All`<br>5. Chọn 2 mục bất kỳ, rồi bấm lại mục thứ nhất<br>6. Bấm ra ngoài dropdown | Từ khoá: `Platinum` | 1. Dropdown mở, có ô tìm kiếm ở trên cùng, hai nút `Select All` · `Deselect All`<br>2. Chỉ còn các mục chứa `Platinum`<br>3. Mọi mục có dấu tích; ô `Groups` đổi từ `Nothing selected` sang dạng tóm tắt số mục đã chọn<br>4. Mọi dấu tích biến mất, ô về `Nothing selected`<br>5. Mục thứ nhất bỏ tích, chỉ còn 1 mục được chọn<br>6. Dropdown đóng, ô `Groups` hiện tên mục còn được chọn | Low | Yes | UI | @Regression, @UIBehavior, @MultiSelect, @Customers |
| CRM_CUST_TC_058 | REQ-CUST-28 | CUST | Medium | Lưu khách hàng thuộc một hoặc nhiều nhóm và gỡ hết nhóm | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Nhập `Company`: `auto_cust_tc058_202609191230`<br>2. Ở `Groups` chọn 2 nhóm `Platinum` và `Information Technology` (mỗi tên chọn 1 mục)<br>3. Bấm `Save`, đọc ô `Groups` trên hồ sơ<br>4. Ở danh sách, tìm tên, đọc cột `Groups`<br>5. Mở lại hồ sơ, `Groups` bấm `Deselect All`, `Save`<br>6. Ở danh sách, đọc cột `Groups`<br>7. Dọn dữ liệu theo `D1` | Nhóm: `Platinum` · `Information Technology` (có sẵn, không tạo mới) | 3. Ô `Groups` hiện cả 2 tên nhóm<br>4. Cột `Groups` hiện cả 2 tên<br>6. Cột `Groups` **trống** — gỡ nhóm được lưu đúng | Medium | Yes | UI | @Regression, @MultiSelect, @Customers |
| CRM_CUST_TC_059 | REQ-CUST-29 | CUST | Medium | Hộp thoại tạo nhanh nhóm: giao diện, trường Name bắt buộc, đóng không tạo gì | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Bấm nút `+` bên phải ô `Groups`<br>2. Đọc hộp thoại<br>3. Để trống `Name`, bấm `Save`<br>4. Bấm `Close`<br>5. Mở dropdown `Groups`, gõ tìm chữ rỗng — xác nhận không có nhóm mới | — (không tạo nhóm) | 2. Hộp thoại có ô nhãn `* Name` (dấu `*` đỏ), nút `Close` và `Save` — ⚠️ tiêu đề hộp thoại chưa có evidence, ghi lại nguyên văn<br>3. Không lưu, có thông báo bắt buộc tại ô Name — ⚠️ nội dung chưa có evidence, ghi lại (kỳ vọng `This field is required.` như ô Company)<br>4. Hộp thoại đóng, biểu mẫu khách hàng giữ nguyên<br>5. Không xuất hiện nhóm mới nào | Medium | Yes | UI | @Regression, @Modal, @Required, @NeedsVerify, @Customers |
| CRM_CUST_TC_060 | REQ-CUST-29 | CUST | Low | Tạo nhanh nhóm mới ngay trên biểu mẫu khách hàng | 🚫 **CHỈ chạy trên môi trường riêng** — nhóm khách hàng là dữ liệu chuẩn dùng chung, tài khoản hiện tại **không** xoá được (khu Setup báo từ chối truy cập, `RISK-CUST-04`) nên không dọn được. `P-ADMIN`, đang ở `/admin/clients/client` | 1. Bấm `+` cạnh `Groups`<br>2. Nhập `Name`: `auto_group_tc060_202609191235`<br>3. Bấm `Save`<br>4. Mở dropdown `Groups`, tìm `auto_group_tc060` | Name: `auto_group_tc060_202609191235` | 3. Hộp thoại đóng, có thông báo thành công (ghi nguyên văn)<br>4. Nhóm mới có trong danh sách, chọn được ngay mà không phải nạp lại trang | Low | Partial | UI | @Regression, @PersonalOnly, @NeedsVerify, @Customers |
| CRM_CUST_TC_061 | REQ-CUST-30, REQ-CUST-65, REQ-CUST-84 | CUST | Medium | Chọn tiền tệ riêng cho khách hàng và sao kê hiển thị theo tiền tệ đó | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Rê chuột lên dấu hỏi cạnh nhãn `Currency`, đọc chú giải<br>2. Nhập `Company`: `auto_cust_tc061_202609191240`, chọn `Currency` = `EUR`<br>3. Bấm `Save`, đọc ô `Currency`<br>4. Bấm tab `Statement`, đọc khối `Account Summary`<br>5. Quay lại `Profile`, đổi `Currency` về `System Default`, `Save`, nạp lại<br>6. Dọn dữ liệu theo `D1` | Currency: `EUR` rồi `System Default` | 1. Hiện chú giải nguyên văn `If the customer use other currency then the base currency make sure you select the appropriate currency for this customer. Changing the currency is not possible after transactions are recorded.`<br>3. Ô `Currency` hiện `EUR`<br>4. Bốn dòng số dư dùng ký hiệu **€** thay cho `$` — ⚠️ chưa có evidence về định dạng chính xác (`€0.00` hay `0.00 €`), ghi lại<br>5. Ô `Currency` hiện `System Default` | Medium | Yes | UI | @Regression, @Dropdown, @NeedsVerify, @Customers |
| CRM_CUST_TC_062 | REQ-CUST-31 | CUST | Low | Chọn ngôn ngữ mặc định cho khách hàng | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Bấm ô `Default Language`, đọc mục đầu và tìm các mục `English`, `Vietnamese`, `Chinese`, `Japanese`<br>2. Nhập `Company`: `auto_cust_tc062_202609191245`, chọn `Vietnamese`<br>3. Bấm `Save`, nạp lại, đọc ô `Default Language`<br>4. Dọn dữ liệu theo `D1` | Default Language: `Vietnamese` | 1. Mục đầu tiên `System Default` (đang chọn); có đủ 4 mục kể trên<br>3. Ô hiện `Vietnamese` (chữ V hoa, như ảnh `cust_detail_after_create_profile_tab_fullpage.png`)<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `default_language` có 27 option, giá trị lưu là `vietnamese` | Low | Yes | UI | @Regression, @Dropdown, @TechCheck, @Customers |
| CRM_CUST_TC_063 | REQ-CUST-32 | CUST | Medium | Chọn, đổi và bỏ chọn quốc gia | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Nhập `Company`: `auto_cust_tc063_202609191250`<br>2. Bấm ô `Country`, gõ `Viet`<br>3. Chọn `Vietnam`, `Save`, đọc ô `Country`<br>4. Đổi sang `Japan`, `Save`, nạp lại, đọc ô<br>5. Chọn mục rỗng đầu danh sách, `Save`, nạp lại, đọc ô<br>6. Dọn dữ liệu theo `D1` | Country: `Vietnam` → `Japan` → rỗng | 2. Dropdown chỉ còn mục chứa `Viet` (có `Vietnam`)<br>3. Ô hiện `Vietnam`<br>4. Ô hiện `Japan`<br>5. Ô về chữ mờ `Nothing selected` — bỏ chọn được lưu | Medium | Yes | UI | @Regression, @Dropdown, @Customers |
| CRM_CUST_TC_129 | REQ-CUST-84 | CUST | High | Khách hàng đã có hoá đơn thì ô Currency bị khoá, không đổi được tiền tệ | `P-ADMIN`. Chỉ **mở xem**, không sửa khách hàng nào của người khác | 1. Mở `Sales → Invoices`, đợi bảng nạp xong<br>2. Ở dòng hoá đơn đầu tiên, bấm vào **tên khách hàng**<br>3. Ở tab `Profile` → `Customer Details`, nhìn ô `Currency`<br>4. Bấm vào ô `Currency`<br>5. Rời trang **không** bấm `Save`<br>6. Đối chứng: mở `/admin/clients/client` (thêm mới), bấm vào ô `Currency` | Khách hàng đã có hoá đơn (VD `13339` lúc khảo sát 19-09-2026 — lấy khách hàng ở dòng đầu bảng hoá đơn khi chạy) | 3. Ô `Currency` **nền xám**, hiện đúng tiền tệ đang dùng (VD `USD`) — ảnh `cust_edit_currency_locked_after_invoice.png`<br>4. **Không** mở được danh sách lựa chọn — không đổi được tiền tệ<br>6. Ở biểu mẫu thêm mới, ô `Currency` mở được và thấy 3 lựa chọn — việc khoá chỉ áp cho khách hàng **đã có giao dịch**<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `select#default_currency` mang thuộc tính `disabled` ở bước 3, không mang ở bước 6 | High | Yes | UI | @Regression, @BusinessRule, @Dependency, @TechCheck, @Customers |

---

## Nhóm M — Sao chép địa chỉ thanh toán / giao hàng

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_064 | REQ-CUST-37 | CUST | Medium | "Same as Customer Info" điền 5 ô địa chỉ thanh toán từ thông tin khách hàng | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Ở `Customer Details` nhập: Address `123 Auto Recon Street` · City `Da Nang` · State `Hai Chau` · Zip Code `550000` · Country `Vietnam`<br>2. Bấm tab `Billing & Shipping`<br>3. Bấm liên kết `Same as Customer Info`<br>4. Đọc 5 ô của khối `Billing Address` và 5 ô khối `Shipping Address` | Như bước 1 (trùng dữ liệu ảnh evidence) | 3. Không tải lại trang<br>4. Billing: Street `123 Auto Recon Street` · City `Da Nang` · State `Hai Chau` · Zip Code `550000` · Country `Vietnam` · khối **Shipping vẫn rỗng** | Medium | Yes | UI | @Regression, @UIBehavior, @Customers |
| CRM_CUST_TC_065 | REQ-CUST-38 | CUST | Medium | "Copy Billing Address" điền 5 ô địa chỉ giao hàng từ địa chỉ thanh toán rồi lưu được | `P-ADMIN`, đang ở `/admin/clients/client`, tab `Billing & Shipping` | 1. Nhập Billing: Street `45 Billing Street` · City `Ha Noi` · State `Ba Dinh` · Zip Code `100000` · Country `Vietnam`<br>2. Bấm liên kết `Copy Billing Address`<br>3. Đọc 5 ô Shipping<br>4. Nhập `Company` `auto_cust_tc065_202609191255`, `Save`, mở lại tab `Billing & Shipping`<br>5. Dọn dữ liệu theo `D1` | Như bước 1 | 3. Shipping: Street `45 Billing Street` · City `Ha Noi` · State `Ba Dinh` · Zip Code `100000` · Country `Vietnam`<br>4. Sau khi lưu, cả 10 ô vẫn đúng giá trị (ảnh `cust_new_form_billing_shipping_filled_fullpage.png`) | Medium | Yes | UI | @Regression, @UIBehavior, @Customers |
| CRM_CUST_TC_066 | REQ-CUST-37, REQ-CUST-38 | CUST | Low | Sao chép địa chỉ khi ô đích đã có dữ liệu khác, hoặc ô nguồn đang trống | `P-ADMIN`, đang ở `/admin/clients/client`. Nạp lại trang trước **mỗi** biến thể | 1. Chuẩn bị theo biến thể<br>2. Bấm liên kết sao chép của biến thể<br>3. Đọc 5 ô đích | **Bảng biến thể:**<br>`a` Billing City đang là `Hue`, Customer City là `Da Nang` → bấm `Same as Customer Info`<br>`b` Thông tin khách hàng trống hết, Billing City đang là `Hue` → bấm `Same as Customer Info`<br>`c` Billing trống hết, Shipping City đang là `Hue` → bấm `Copy Billing Address` | ⚠️ Chưa có evidence — **ghi nhận** hành vi:<br>`a` Kỳ vọng: `Hue` bị **ghi đè** thành `Da Nang`<br>`b`,`c` Ghi lại: ô đích bị **xoá trắng** hay **giữ nguyên** `Hue`. Không có quy tắc để chấm → ghi `AMB` mới nếu hành vi khiến người dùng mất dữ liệu đã nhập mà không cảnh báo | Low | Partial | UI | @Regression, @ErrorGuessing, @NeedsVerify, @Customers |

---

## Nhóm N — Sửa hồ sơ, ô tích liên hệ chính, quản trị viên phụ trách

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_067 | REQ-CUST-50, REQ-CUST-51 | CUST | High | Sửa hồ sơ rồi lưu thì dữ liệu mới hiện ở biểu mẫu và ở danh sách | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc067_202609191300`, Phone `0900000067`, City `Da Nang` | 1. Mở hồ sơ `KH-TEST`<br>2. Đổi Phone thành `0911111067`, City thành `Hue`<br>3. Bấm `Save`<br>4. Quan sát thông báo, URL<br>5. Nạp lại trang, đọc 2 ô vừa sửa và các ô **không** sửa<br>6. Ở danh sách, tìm tên, đọc cột `Phone`<br>7. Dọn dữ liệu theo `D1` | Phone: `0900000067` → `0911111067`<br>City: `Da Nang` → `Hue` | 4. Ở lại hồ sơ cùng `{id}`; có thông báo nổi thành công — ⚠️ nội dung chưa có evidence, ghi nguyên văn<br>5. Phone `0911111067`, City `Hue`; Company và mọi ô khác **giữ nguyên**<br>6. Cột `Phone` hiện `0911111067` | Critical | Yes | UI | @Regression, @CRUD, @NeedsVerify, @Customers |
| CRM_CUST_TC_068 | REQ-CUST-50 | CUST | Medium | Sửa dở rồi rời trang không lưu thì dữ liệu cũ giữ nguyên | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc068_202609191305`, Phone `0900000068` | 1. Mở hồ sơ `KH-TEST`<br>2. Đổi Phone thành `0999999068`, **không** bấm Save<br>3. Bấm mục `Customers` ở menu trái<br>4. Nếu trình duyệt hỏi rời trang, chọn rời đi<br>5. Mở lại hồ sơ, đọc ô Phone<br>6. Dọn dữ liệu theo `D1` | Phone sửa dở: `0999999068` | 4. Ghi lại có hộp thoại cảnh báo "thay đổi chưa lưu" hay không (không bắt buộc)<br>5. Ô Phone vẫn là `0900000068` — thay đổi chưa lưu **không** được ghi | Medium | Yes | UI | @Regression, @CRUD, @Customers |
| CRM_CUST_TC_069 | REQ-CUST-40 | CUST | High | Xoá trắng Company khi sửa hồ sơ thì bị chặn, dữ liệu cũ không đổi | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc069_202609191310` | 1. Mở hồ sơ `KH-TEST`<br>2. Xoá hết ô `Company`<br>3. Bấm `Save`<br>4. Quan sát ô Company<br>5. Nạp lại trang, đọc ô Company và tiêu đề<br>6. Dọn dữ liệu theo `D1` | Company: xoá trắng | 4. Hiện `This field is required.` dưới ô, viền đỏ, không lưu<br>5. Company vẫn là `auto_cust_tc069_202609191310`, tiêu đề `#{id} auto_cust_tc069_…` | High | Yes | UI | @Regression, @Required, @CRUD, @Customers |
| CRM_CUST_TC_070 | REQ-CUST-53 | CUST | Medium | Khách hàng chưa có liên hệ nào thì không thấy ô tích "Show primary contact…" | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc070_202609191315`, **chưa** có liên hệ | 1. Mở hồ sơ `KH-TEST`, tab `Profile` → `Customer Details`<br>2. Nhìn vùng phía trên ô `Company`<br>3. Nhìn tab `Contacts` ở cột trái<br>4. Dọn dữ liệu theo `D1` | — | 2. **Không** có ô tích `Show primary contact full name…` — ô `* Company` là trường đầu tiên<br>3. Tab `Contacts` **không** có số đếm bên cạnh<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `input#show_primary_contact` có trong trang nhưng bị ẩn (`offsetParent` = `null`) | Medium | Yes | UI | @Regression, @UIBehavior, @TechCheck, @Customers |
| CRM_CUST_TC_071 | REQ-CUST-53 | CUST | Medium | Khách hàng có liên hệ chính thì hiện ô tích "Show primary contact…", mặc định không tích, lưu được trạng thái | `P-ADMIN`. Có sẵn khách hàng test **có 1 liên hệ chính** — tạo bằng Nhập CSV 1 dòng theo `CRM_CUST_TC_103` với Company `auto_cust_tc071_202609191320` | 1. Mở hồ sơ khách hàng, tab `Profile` → `Customer Details`<br>2. Đọc vùng phía trên ô `Company`<br>3. Tích ô, bấm `Save`, nạp lại<br>4. Bỏ tích, `Save`, nạp lại<br>5. Dọn dữ liệu theo `D1` (xoá khách hàng — liên hệ đi kèm bị xoá theo) | Company: `auto_cust_tc071_202609191320` (kèm liên hệ `auto_cust_tc071_202609191320@auto.test`) | 1. Tab `Contacts` có số đếm `1`<br>2. Có ô tích nhãn nguyên văn `Show primary contact full name on Invoices, Estimates, Payments, Credit Notes`, đang **rỗng**, nằm trên ô `* Company` (ảnh `cust_detail_show_primary_contact_visible.png`)<br>3. Ô vẫn **tích** sau khi nạp lại — ⚠️ chưa kiểm chứng việc lưu<br>4. Ô trở về rỗng sau khi nạp lại | Medium | Yes | UI | @Regression, @UIBehavior, @Checkbox, @NeedsVerify, @Customers |
| CRM_CUST_TC_072 | REQ-CUST-49, REQ-CUST-54, REQ-CUST-55 | CUST | Medium | Tab Customer Admins và hộp thoại Assign Admin hiển thị đủ, đóng không gán gì | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc072_202609191325` | 1. Mở hồ sơ → tab `Customer Admins`<br>2. Đối chiếu Bảng kiểm mục `1`–`3`<br>3. Bấm `Assign Admin`, đối chiếu mục `4`–`5`<br>4. Bấm `Close`<br>5. Dọn dữ liệu theo `D1` | — (không gán) | **Bảng kiểm:**<br>`1` (REQ-54) Nút `Assign Admin` nền xanh ở trên cùng<br>`2` (REQ-55) Ô số dòng `25` · nút `Export` · ô tìm kiếm chữ gợi ý `Search...` (có thể bị cắt còn `Search..`)<br>`3` (REQ-55) Bảng 3 cột `Staff Member` · `Date Assigned` · `Options`, nội dung `No entries found`<br>`4` (REQ-54) Hộp thoại tiêu đề `Assign Admin`, ô chọn nhiều; mở ra có đúng 3 nhân sự `Project Manager` · `Admin Anh Tester` · `Admin Example`<br>`5` (REQ-54) Chân hộp thoại có `Close` · `Save`<br><br>4. Hộp thoại đóng, bảng vẫn `No entries found` | Medium | Yes | UI | @Regression, @Modal, @UI, @Customers |
| CRM_CUST_TC_073 | REQ-CUST-54, REQ-CUST-55 | CUST | Medium | Gán quản trị viên phụ trách rồi gỡ ra | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc073_202609191330` | 1. Mở hồ sơ → tab `Customer Admins` → `Assign Admin`<br>2. Chọn `Admin Example`, bấm `Save`<br>3. Đọc bảng<br>4. Ở dòng vừa thêm, dùng lệnh xoá trong cột `Options`, xác nhận<br>5. Đọc bảng<br>6. Dọn dữ liệu theo `D1` | Nhân sự: `Admin Example` | 3. Bảng có 1 dòng: `Staff Member` = `Admin Example`, `Date Assigned` = hôm nay dạng `DD-MM-YYYY`<br>5. Bảng về `No entries found`<br><br>⚠️ Chưa thực thi lúc khảo sát (chỉ có evidence giao diện) — ghi nguyên văn thông báo và nhãn lệnh ở cột `Options` | Medium | Yes | UI | @Regression, @CRUD, @NeedsVerify, @Customers |
