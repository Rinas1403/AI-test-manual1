# Test Cases — `CUST` · Web · Part 05 — Vòng 3 Technical & Vòng 4 Non-functional (16 TC)

| Thông tin | Nội dung |
|---|---|
| **Index module** | [../../TEST_CASES_CUSTOMERS_SUMMARY.md](../../TEST_CASES_CUSTOMERS_SUMMARY.md) — cách đọc · quy ước `P-*`, `KH-TEST`, `D1`, `CSV-*` · Assumptions · Bảng đối soát |
| **Nguồn requirement** | [requirements_customers_web.md](../../../../requirements/customers/web/requirements_customers_web.md) · Ma trận phân quyền ở [index requirements](../../../../requirements/customers/REQUIREMENTS_CUSTOMERS_SUMMARY.md) mục 6 |
| **Nền tảng** | Web — Google Chrome, viewport desktop `1600×750` (trừ `TC_125`, `TC_127`) |
| **Dải TC ID trong part** | `CRM_CUST_TC_113` → `CRM_CUST_TC_128` |
| **Part liền kề** | ← [part_04_web_trang_thai_tab_nhap.md](part_04_web_trang_thai_tab_nhap.md) |

> 👥 **Ba vai trò** (danh mục requirements): `Admin` · `Project Manager` (nhân sự **không phải** quản trị viên) · `Customer` (liên hệ khách hàng, cổng `/login` riêng). Tài khoản lấy từ `.env`: `ADMIN_*` · `PM_*` · `CUSTOMER_*`.
>
> ✅ **`AMB-CUST-14` đã trả lời 19-09-2026** — trùng giả định tạm cả hai vế: PM **được** xoá khách hàng (cấu hình cố ý), PM **không** xem được mục Vault mức "chỉ quản trị viên". `TC_119`, `TC_120` gỡ `@AssumptionBased`; FAIL ở hai TC này nay mở bug thẳng, không cần hỏi PO trước.

---

## Nhóm T — Vòng 3 · Phân quyền theo vai trò (Permission)

**Ma trận Phân quyền × TC** (cột `Admin` đã được chứng minh bởi toàn bộ Vòng 1–2, chạy bằng `P-ADMIN`):

| Hành động | Admin | Project Manager | Customer |
|---|---|---|---|
| Xem danh sách · tìm & lọc | ✅ `TC_001`, `TC_021` | ✅ `TC_115` | ❌ `TC_114` |
| Xuất dữ liệu · thao tác hàng loạt | ✅ `TC_031`, `TC_034` | ✅ᵁᴵ `TC_115` | ❌ `TC_114` |
| Tạo mới · sửa hồ sơ · gán quản trị viên · tạo nhóm nhanh | ✅ `TC_009`, `TC_067`, `TC_073` | ✅ᵁᴵ `TC_121` | ❌ `TC_114` |
| Bật/tắt trạng thái | ✅ `TC_074` | ✅ `TC_117` | ❌ `TC_114` |
| Xoá khách hàng | ✅ `TC_077` | ✅ `TC_119` (PO chốt, `AMB-CUST-14` ✅) | ❌ `TC_114` |
| Xem mục Vault mức "chỉ quản trị viên" | ✅ `TC_120` bước 2 | ❌ `TC_120` (PO chốt, `AMB-CUST-14` ✅) | ❌ `TC_114` |
| Nhập CSV | ✅ `TC_103` | ✅ `TC_118` | ❌ `TC_114` |
| Số tab hồ sơ | 19 · `TC_013` | 15 · `TC_116` | ❌ `TC_114` |
| Truy cập khi **chưa đăng nhập** | — | — | — · `TC_113` |

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_113 | REQ-CUST-01 | CUST | High | Chưa đăng nhập mà mở thẳng đường dẫn của module thì bị chuyển về trang đăng nhập | Trình duyệt ẩn danh, **chưa** đăng nhập. Chạy lại cho **từng** biến thể | 1. Gõ thẳng đường dẫn của biến thể vào thanh địa chỉ<br>2. Đợi trang dừng chuyển<br>3. Đọc URL, nhìn nội dung trang | **Bảng biến thể:**<br>`a` `https://crm.anhtester.com/admin/clients`<br>`b` `https://crm.anhtester.com/admin/clients/client`<br>`c` `https://crm.anhtester.com/admin/clients/client/14625`<br>`d` `https://crm.anhtester.com/admin/clients/import` | **Mọi biến thể cho cùng kết quả:**<br>3. Dừng ở trang đăng nhập `https://crm.anhtester.com/admin/authentication` · **không** thấy bất kỳ tên khách hàng, bảng hay biểu mẫu nào của module — kể cả thoáng qua | Critical | Yes | UI | @Regression, @Security, @Permission, @Customers |
| CRM_CUST_TC_114 | REQ-CUST-01 | CUST | High | Tài khoản Customer (cổng khách hàng) không vào được khu quản lý khách hàng | Đã đăng nhập cổng khách hàng ở `https://crm.anhtester.com/login` bằng `CUSTOMER_EMAIL` / `CUSTOMER_PASSWORD` trong `.env`. Chạy lại cho **từng** biến thể | 1. Ở cùng trình duyệt, gõ thẳng đường dẫn của biến thể<br>2. Đợi trang dừng chuyển<br>3. Đọc URL, nhìn nội dung | **Bảng biến thể:**<br>`a` `https://crm.anhtester.com/admin/clients`<br>`b` `https://crm.anhtester.com/admin/clients/import` | **Mọi biến thể cho cùng kết quả:**<br>3. **Không** vào được: dừng ở trang đăng nhập khu quản trị `…/admin/authentication` (phiên cổng khách hàng không dùng được cho `/admin` — `REQ-LOGIN-43`) · không thấy danh sách hay biểu mẫu khách hàng nào | Critical | Yes | UI | @Regression, @Security, @Permission, @Customers |
| CRM_CUST_TC_115 | REQ-CUST-01, REQ-CUST-08, REQ-CUST-09, REQ-CUST-18, REQ-CUST-21 | CUST | High | Project Manager thấy toàn bộ danh sách khách hàng và đủ công cụ như Admin | `P-PM`, đang ở `/admin/clients`. Ở tab khác (hoặc trước đó) ghi lại `Total Customers` khi đăng nhập Admin | 1. Đối chiếu Bảng kiểm<br>2. Với mục `4`: tích 1 dòng, mở `Bulk Actions`, **chỉ nhìn**, bấm `Close` — **tuyệt đối không** `Confirm` | — (chỉ quan sát, không thực thi thao tác phá huỷ) | **Bảng kiểm (ảnh `cust_perm_pm_list.png`, `cust_perm_pm_bulk_actions_mass_delete.png`):**<br>`1` Có đủ 3 nút `+ New Customer` · `Import Customers` · `Contacts` và nút phễu lọc<br>`2` `Total Customers` **bằng** con số Admin thấy — PM thấy **toàn bộ** khách hàng, không chỉ khách được gán<br>`3` Có nút `Export`, `Bulk Actions`, nút nạp lại; công tắc `Active` bấm được<br>`4` Hộp thoại `Bulk Actions` có ô `Mass Delete` và ô `Groups` như của Admin (`RISK-CUST-08`)<br>`5` Rê chuột lên dòng hiện `View` \| `Contacts` \| `Delete`<br>`6` Menu trái của PM **không** có `Subscriptions` và `Expenses` (khác Admin) | High | Yes | UI | @Regression, @Permission, @Customers |
| CRM_CUST_TC_116 | REQ-CUST-83 | CUST | High | Project Manager mở hồ sơ khách hàng chỉ thấy 15 tab | `P-PM`. PM tự nhập `CSV-2` với `{nnn}` = `116` (cách làm như `CRM_CUST_TC_118`) | 1. Mở hồ sơ `auto_import_cust_tc116_202609191600_1`<br>2. Đếm và đọc lần lượt các tab ở cột trái<br>3. Gõ thẳng `…/admin/clients/client/{id}?group=payments`<br>4. Dọn cả 2 khách hàng theo `D1` | Tệp `CSV-2` (`{nnn}` = `116`) | 2. Đúng **15** tab theo thứ tự: `Profile` · `Contacts` · `Notes` · `Statement` · `Invoices` · `Credit Notes` · `Estimates` · `Contracts` · `Projects` · `Tasks` · `Tickets` · `Files` · `Vault` · `Reminders` · `Map` — **không có** `Payments` · `Proposals` · `Subscriptions` · `Expenses` (ảnh `cust_detail_show_primary_contact_visible.png`)<br>3. ⚠️ Chưa có evidence — kỳ vọng: **không** hiện dữ liệu thanh toán (bị chặn / về tab Profile / báo không có quyền). FAIL nếu mở được tab bị ẩn bằng cách gõ URL | High | Yes | UI | @Regression, @Permission, @Security, @NeedsVerify, @Customers |
| CRM_CUST_TC_117 | REQ-CUST-57, REQ-CUST-58 | CUST | Medium | Project Manager bật/tắt trạng thái hoạt động của khách hàng | `P-PM`. PM tự nhập `CSV-2` với `{nnn}` = `117` | 1. Ở danh sách, tìm `auto_import_cust_tc117_202609191600_1`, gạt công tắc sang tắt<br>2. Nạp lại, tìm lại, đọc công tắc và `Inactive Customers`<br>3. Gạt công tắc bật lại, nạp lại<br>4. Dọn cả 2 khách hàng theo `D1` | Tệp `CSV-2` (`{nnn}` = `117`) | 1. Công tắc tắt, trang không tải lại<br>2. Công tắc vẫn tắt · `Inactive Customers` tăng 1 (đã kiểm chứng 19-09-2026: 9 → 10)<br>3. Công tắc bật · `Inactive Customers` về số cũ | Medium | Yes | UI | @Regression, @Permission, @StateTransition, @Customers |
| CRM_CUST_TC_118 | REQ-CUST-79 | CUST | High | Project Manager nhập khách hàng từ CSV — tạo được bản ghi thật | `P-PM`, đang ở `/admin/clients/import`. Chuẩn bị `CSV-2` với `{nnn}` = `118` | 1. Chọn `CSV-2`, bấm `Import`<br>2. Đọc thông báo<br>3. Ở danh sách, tìm `auto_import_cust_tc118`<br>4. Dọn cả 2 khách hàng theo `D1` | Tệp `CSV-2` | 2. `Total Imported: 2`<br>3. Đúng 2 dòng, công tắc bật, mỗi dòng có liên hệ chính | High | Yes | UI | @Regression, @Permission, @FileUpload, @Customers |
| CRM_CUST_TC_119 | REQ-CUST-60, REQ-CUST-61 | CUST | High | Project Manager xoá khách hàng test | `P-PM`. PM tự nhập `CSV-2` với `{nnn}` = `119` — 2 khách hàng `auto_import_cust_tc119_202609191600_1` / `_2` | 1. Tìm `auto_import_cust_tc119`, rê chuột lên dòng `_1`, bấm `Delete`<br>2. Đọc hộp thoại, bấm `OK`<br>3. Lặp lại cho dòng `_2`<br>4. Tìm lại `auto_import_cust_tc119` | Tệp `CSV-2` (`{nnn}` = `119`) | 2. Hộp thoại `Are you sure you want to perform this action?` → sau `OK` hiện `Customer deleted`<br>4. `No matching records found`<br><br>✅ PO chốt 19-09-2026 (`AMB-CUST-14`): PM **được phép** xoá khách hàng — cấu hình quyền cố ý. Bị chặn ở bước 2 → FAIL, mở bug, rồi **dọn lại bằng `P-ADMIN`** | High | Yes | UI | @Regression, @Permission, @Destructive, @Customers |
| CRM_CUST_TC_120 | REQ-CUST-68 | CUST | High | Mục Vault "Visible only to administrators" không hiện với Project Manager | `P-ADMIN` tạo `KH-TEST` Company `auto_cust_tc120_202609191705` và 1 mục Vault: Server `auto-vault-tc120.test`, Username `auto_user_tc120`, Password `VaultTest#120`, mức **`Visible only to administrators`** | 1. Đăng xuất, đăng nhập `P-PM`<br>2. Mở hồ sơ `KH-TEST` → tab `Vault`<br>3. Đăng xuất, đăng nhập lại `P-ADMIN`, mở tab `Vault` của `KH-TEST`<br>4. Xoá mục Vault, dọn `D1` | Mức hiển thị: `Visible only to administrators` | 2. **Không** thấy mục `auto-vault-tc120.test` — tab hiện `Vault entries not found for this customer.` hoặc chỉ các mục mức khác<br>3. Admin **thấy** mục `auto-vault-tc120.test`<br><br>✅ PO chốt 19-09-2026 (`AMB-CUST-14` vế 2): PM **không** được xem mục mức này. FAIL ở bước 2 là lộ thông tin đăng nhập máy chủ của khách hàng cho nhân sự không phải quản trị viên → mở bug mức cao | High | Partial | UI | @Regression, @Permission, @Security, @Customers |
| CRM_CUST_TC_121 | REQ-CUST-23, REQ-CUST-29, REQ-CUST-49, REQ-CUST-54 | CUST | Medium | Project Manager mở được biểu mẫu tạo, biểu mẫu sửa, hộp thoại Assign Admin và tạo nhóm nhanh | `P-PM`. PM tự nhập `CSV-2` với `{nnn}` = `121` | 1. Mở `/admin/clients/client`<br>2. Bấm `+` cạnh `Groups`, nhìn rồi `Close`<br>3. Mở hồ sơ `auto_import_cust_tc121_202609191600_1` → `Profile`<br>4. Bấm tab `Customer Admins` → `Assign Admin`, nhìn rồi `Close`<br>5. **Không** bấm `Save` ở bất kỳ đâu; dọn cả 2 khách hàng theo `D1` | Tệp `CSV-2` (`{nnn}` = `121`) — không gửi biểu mẫu nào khác | 1. Biểu mẫu `Add new customer` mở được, có `Save` và `Save and create contact`<br>2. Hộp thoại tạo nhóm có ô `* Name`, `Save`<br>3. Biểu mẫu sửa 3 tab, có nút `Save`<br>4. Hộp thoại `Assign Admin` mở được với 3 nhân sự<br><br>ℹ️ Mức ✅ᵁᴵ của ma trận: chỉ chứng minh **giao diện** có mặt. Quyền **lưu** thật của PM đã chứng minh qua `TC_117`, `TC_118` | Medium | Yes | UI | @Regression, @Permission, @Customers |

---

## Nhóm U — Vòng 3 · Bảo mật cơ bản (Security)

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_122 | REQ-CUST-46 | CUST | High | Biểu mẫu khách hàng được bảo vệ chống giả mạo yêu cầu (CSRF) | `P-ADMIN`, đang ở `/admin/clients/client` | 1. Nhập `Company`: `auto_cust_tc122_202609191710`, bấm `Tab`<br>2. Bấm `Save`<br>3. Thực hiện phần 🔧 nếu có người chạy được<br>4. Dọn theo `D1` | Company: `auto_cust_tc122_202609191710` | 1. Dải cảnh báo trùng tên (nếu có) hoạt động bình thường<br>2. Lưu thành công — mã chống giả mạo hợp lệ **không** cản người dùng thật<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): (1) biểu mẫu có `input[type=hidden][name=csrf_token_name]`, giá trị khớp hình thái `<32 ký tự hex>`; (2) cùng mã đi kèm request `check_duplicate_customer_name` và `POST /admin/clients/table`; (3) sửa giá trị mã trong DevTools rồi bấm `Save` → yêu cầu **bị từ chối**, không tạo bản ghi (⚠️ chưa đo trên module này — ở `LOGIN` là trang `419 Page Expired!`). 🔒 Chỉ ghi hình thái, KHÔNG chép giá trị mã | High | Partial | UI | @Regression, @Security, @TechCheck, @NeedsVerify, @Customers |
| CRM_CUST_TC_123 | REQ-CUST-62 | CUST | High | Mở thẳng đường dẫn xoá khi đã đăng nhập thì xoá ngay, không qua xác nhận | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc123_202609191715`, ghi lại `{id}`. ⚠️ **Kiểm `{id}` hai lần** — sai số là xoá mất khách hàng thật của người khác | 1. Gõ `https://crm.anhtester.com/admin/clients/delete/{id}` vào thanh địa chỉ, `Enter`<br>2. Quan sát có hộp thoại xác nhận không<br>3. Đọc URL và thông báo<br>4. Tìm lại tên | `{id}` của `auto_cust_tc123_202609191715` | 2. **Không** có hộp thoại xác nhận nào<br>3. Về `…/admin/clients`, hiện `Customer deleted`<br>4. `No matching records found`<br><br>ℹ️ TC xác nhận **hành vi hiện tại** đúng như `REQ-CUST-62` (PASS khi khớp). Đây là **rủi ro đã ghi** (`RISK-CUST-06`): chỉ một cú bấm nhầm liên kết là mất dữ liệu<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): thao tác xoá dùng phương thức `GET`, không có mã CSRF — đề xuất đội Dev chuyển sang `POST` kèm mã CSRF | High | Yes | UI | @Regression, @Security, @Destructive, @TechCheck, @Customers |
| CRM_CUST_TC_124 | REQ-CUST-52 | CUST | High | Website chứa đường dẫn `javascript:` không chạy mã khi bấm nút quả cầu | `P-ADMIN`. Có sẵn `KH-TEST` Company `auto_cust_tc124_202609191720` | 1. Mở hồ sơ, nhập `Website`: `javascript:alert('tc124')`, `Save`<br>2. Nạp lại, bấm nút quả cầu cạnh ô Website<br>3. Quan sát tab hiện tại và tab mới<br>4. Dọn theo `D1` | Website: `javascript:alert('tc124')` | 1. Lưu được (Website không kiểm định dạng — `AMB-CUST-06`)<br>3. **Không** hộp thoại `alert('tc124')` nào bật lên ở bất kỳ tab nào<br><br>⚠️ Chưa có evidence. FAIL = lỗ hổng chèn mã lưu trữ qua liên kết: bất kỳ ai bấm nút quả cầu đều chạy mã do người khác nhập → mở bug mức cao | High | Yes | UI | @Regression, @Security, @NeedsVerify, @Customers |

---

## Nhóm V — Vòng 4 · Phi chức năng

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automation | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_CUST_TC_125 | REQ-CUST-03, REQ-CUST-27 | CUST | Medium | Danh sách và biểu mẫu thêm mới hiển thị dùng được ở các kích thước màn hình đã chốt | `P-ADMIN`. Chạy lại cho **từng** biến thể kích thước | 1. Đặt cửa sổ trình duyệt theo kích thước của biến thể (chế độ thiết bị của trình duyệt)<br>2. Mở `/admin/clients`, cuộn hết trang<br>3. Mở `/admin/clients/client`, cuộn hết trang<br>4. Bấm thử `+ New Customer`, `Save`, tab `Billing & Shipping` | **Bảng biến thể:**<br>`a` `1920×1080`<br>`b` `1366×768`<br>`c` `768×1024` (máy tính bảng)<br>`d` `375×812` (điện thoại) | **Mọi biến thể cho cùng kết quả:**<br>2–3. Trang **không** cuộn ngang (bảng dữ liệu được phép cuộn ngang **bên trong** khung của nó) · chữ không chồng lên nhau · không nút nào bị che mất<br>4. Mọi nút bấm được; ở `c`,`d` menu trái thu gọn thành nút biểu tượng<br><br>⚠️ Chưa có evidence ở `a`–`d` (khảo sát chỉ chạy `1600×750`) → ghi ảnh chụp khi chạy | Medium | Partial | UI | @Regression, @Responsive, @NeedsVerify, @Customers |
| CRM_CUST_TC_126 | REQ-CUST-26, REQ-CUST-27 | CUST | Medium | Điền và lưu biểu mẫu thêm khách hàng chỉ bằng bàn phím | `P-ADMIN`, đang ở `/admin/clients/client`, không dùng chuột | 1. Gõ `auto_cust_tc126_202609191725` (con trỏ đã ở ô Company)<br>2. Bấm `Tab` lần lượt, ghi lại thứ tự ô nhận tiêu điểm<br>3. Ở mỗi ô, quan sát viền tiêu điểm<br>4. Tới ô `Currency` bấm `Space`/`Enter` để mở, mũi tên chọn `USD`, `Enter`<br>5. `Tab` tới nút `Save`, bấm `Enter`<br>6. Dọn theo `D1` | Company: `auto_cust_tc126_202609191725` · Currency `USD` | 2. Thứ tự đi theo thứ tự đọc: `Company` → `VAT Number` → `Phone` → `Website` → `Groups` → nút `+` → `Currency` → `Default Language` → `Address` → `City` → `State` → `Zip Code` → `Country` → `Save and create contact` → `Save` — ⚠️ chưa đo thứ tự thật (bố cục 2 cột ở hàng Currency/Default Language), ghi lại nếu khác<br>3. Ô đang có tiêu điểm luôn có viền nhìn thấy được<br>4. Chọn được `USD` không cần chuột<br>5. Lưu thành công, mở hồ sơ | Medium | Partial | UI | @Regression, @Accessibility, @NeedsVerify, @Customers |
| CRM_CUST_TC_127 | REQ-CUST-01, REQ-CUST-33, REQ-CUST-57, REQ-CUST-61 | CUST | Medium | Luồng chính chạy đúng trên Chrome, Edge, Firefox | `P-ADMIN`. Chạy lại cho **từng** trình duyệt, viewport desktop | 1. Mở `/admin/clients`, xác nhận bảng nạp dữ liệu<br>2. Tạo khách hàng Company `auto_cust_tc127{mã biến thể}_202609191730`<br>3. Gạt công tắc Active tắt rồi bật<br>4. Xoá khách hàng qua `Delete` → `OK` | **Bảng biến thể:**<br>`a` Google Chrome (bản ổn định mới nhất)<br>`b` Microsoft Edge (bản ổn định mới nhất)<br>`c` Mozilla Firefox (bản ổn định mới nhất) | **Mọi biến thể cho cùng kết quả:**<br>1. Bảng nạp đủ, dòng trạng thái `Showing …` đúng<br>2. Lưu thành công, mở hồ sơ<br>3. Công tắc đổi tại chỗ<br>4. Hộp thoại xác nhận của trình duyệt hiện đúng nội dung, sau `OK` có `Customer deleted`<br><br>ℹ️ Danh sách trình duyệt theo chốt 19-09-2026 của module `LOGIN`. Safari **không** kiểm | Medium | Yes | UI | @Regression, @Compatibility, @Customers |
| CRM_CUST_TC_128 | REQ-CUST-05, REQ-CUST-07, REQ-CUST-44 | CUST | Low | Hiệu năng mức quan sát thô: danh sách lớn và kiểm tra trùng tên | `P-ADMIN`, đang ở `/admin/clients`, mạng văn phòng bình thường | 1. Chọn số dòng `All`, bấm đồng hồ từ lúc chọn tới khi dòng trạng thái đổi<br>2. Cuộn xuống cuối bảng, bấm thử 1 liên kết<br>3. Gõ `auto` vào ô tìm kiếm, bấm đồng hồ tới khi bảng nạp xong<br>4. Mở `/admin/clients/client`, nhập tên có sẵn, bấm `Tab`, bấm đồng hồ tới khi dải cảnh báo trùng tên hiện | — | 1. Bảng hiện toàn bộ bản ghi (hơn 2.000) trong **dưới 10 giây**, trang không bị trình duyệt báo "không phản hồi"<br>2. Cuộn mượt, liên kết bấm được<br>3. Kết quả tìm kiếm hiện trong **dưới 3 giây**<br>4. Dải cảnh báo hiện gần như tức thì (đo lúc khảo sát: phản hồi 61–139 ms)<br><br>ℹ️ Ngưỡng 10 s / 3 s là **ngưỡng quan sát thô** do QA đặt, không phải cam kết hiệu năng. Đo tải thật thuộc **đội Hạ tầng** | Low | No | N/A | @Regression, @Performance, @Customers |
